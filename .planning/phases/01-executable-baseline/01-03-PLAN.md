---
phase: 01-executable-baseline
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - .planning/phases/01-executable-baseline/01-BASELINE.md
autonomous: false
requirements: [BASE-03, BASE-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-06: the existing GameOver result shows reason 포기 and the current distance while preserving the prior high score."
    - "D-07: the existing result back path reaches MainMenu and the existing start button creates a clean second solo session in the same Player process."
    - "D-08/D-09: one scene-owned confirmation panel blocks game input without pausing the timer; cancel and second Esc close it; confirm forfeits; timeout priority is covered by lifecycle tests."
    - "D-12/D-13: Codex builds, launches, and attempts the exact Windows Player sequence; terminal completion requires an APPROVED checkpoint with GUI PASS, while rejection, uncertainty, or any non-PASS state is recorded and blocks completion."
  artifacts:
    - path: "Assets/Scenes/SampleScene.unity"
      provides: "Existing game canvas plus one serialized ForfeitConfirmation panel and GameOver reason label"
      contains: "ForfeitConfirmation"
    - path: "Assets/Scripts/UI-Kozeki/GameUIManager.cs"
      provides: "Explicit confirmation and deterministic new-session panel state"
      contains: "ForfeitConfirmation"
    - path: "Assets/Scripts/UI-Kozeki/UiGameOver.cs"
      provides: "GameEndReason-aware existing result presenter"
      contains: "GameEndReason"
    - path: ".planning/phases/01-executable-baseline/01-BASELINE.md"
      provides: "Single truthful build/run/GUI evidence summary"
      contains: "GUI status"
  key_links:
    - from: "Assets/Scenes/SampleScene.unity"
      to: "Assets/Scripts/MahjongGameManager.cs"
      via: "confirm/cancel Button onClick targets public ConfirmForfeit/CancelForfeit callbacks"
      pattern: "ConfirmForfeit|CancelForfeit"
    - from: "Assets/Scripts/UI-Kozeki/UiManager.cs"
      to: "Assets/Scripts/MahjongGameManager.cs"
      via: "the unchanged OnGameStartButton route calls StartNewGame for both first and second session"
      pattern: "OnGameStartButton|StartNewGame"
    - from: ".planning/phases/01-executable-baseline/01-BASELINE.md"
      to: "Builds/phase1/RiichiNya.exe"
      via: "D-11 command/result/PID/checklist evidence captured only after the blocking-human result"
      pattern: "Windows build command|GUI status"
---

<objective>
Complete BASE-03/BASE-05 through the real single-scene Windows Player route and a truthful D-12/D-13 verification gate.

Purpose: prove that the tested lifecycle is actually serialized, visible, controllable, and restartable in the built Player.
Output: confirmation/result/menu scene wiring, deterministic UI reset, built and launched Player, and final GUI evidence.
</objective>

<execution_context>
@C:/Users/user/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-executable-baseline/01-CONTEXT.md
@.planning/phases/01-executable-baseline/01-RESEARCH.md
@.planning/phases/01-executable-baseline/01-PATTERNS.md
@.planning/phases/01-executable-baseline/01-VALIDATION.md
@.planning/phases/01-executable-baseline/01-01-SUMMARY.md
@.planning/phases/01-executable-baseline/01-02-SUMMARY.md
@Assets/Scripts/UI-Kozeki/GameUIManager.cs
@Assets/Scripts/UI-Kozeki/UiManager.cs
@Assets/Scripts/UI-Kozeki/UiGameOver.cs
@Assets/Scenes/SampleScene.unity

<interfaces>
The baseline scene serialized `GameUIManager.panels` states 0-7 under game canvas fileID 1225745916 and the existing GameOver object fileID 1563807079 with `UiGameOver` fileID 1563807082. Plans 01-01/01-02 add and structurally verify exactly one confirmation entry and callbacks. Reuse `GameUIManager.ActivePanel/DeactivePanel`, `UiManager.OnGameStartButton`, and the GameOver Back path; do not create another scene or modal owner.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full licensed test/build gate and launch the Windows Player</name>
  <precondition>`.planning/phases/01-executable-baseline/01-BASELINE.md` records successful EditMode and Windows BuildReport results from Unity 2022.3.29f1 on this host or another licensed Windows host; an unresolved LicensingClient IPC/activation failure halts this task and keeps the phase BLOCKED.</precondition>
  <files>.planning/phases/01-executable-baseline/01-BASELINE.md</files>
  <read_first>
    - `AGENTS.md` evidence preservation, no destructive process/file operations, and Windows process visibility rules
    - `.planning/phases/01-executable-baseline/01-CONTEXT.md` D-10 through D-13
    - `.planning/phases/01-executable-baseline/01-VALIDATION.md` full-suite and D-13 gates
    - `.planning/phases/01-executable-baseline/01-BASELINE.md` current automated evidence/status fields
    - `Assets/Editor/Phase1Build.cs` exact build output and BuildReport behavior
  </read_first>
  <action>Before running Unity, enumerate every `Assets/**/*.asmdef` and `Assets/**/*.asmref` and require exact path/blob equality with the repository tree at `b18320e`; Phase 1 may add none and must preserve any baseline descriptor. Run the complete EditMode suite into `Temp/phase1/editmode.xml` and require the three exact trace names plus all thirteen exact lifecycle names, exactly 16 project cases, `test-run total=16`, `passed=16`, zero skipped/inconclusive/non-Passed cases, and zero failure/error totals. This is the final compile/discovery proof that both fixtures under `Assets/Editor/Tests` are recognized from predefined `Assembly-CSharp-Editor`. Re-run the trace assertion-line contract scan so Phase 1 still makes no score or wall-correctness golden claim, and extract `seed=1557` plus `acceptedActions=<count>` from live trace XML/log output. Execute `Phase1Build.BuildWindowsPlayer`, require successful BuildReport text and `Builds/phase1/RiichiNya.exe`, then launch the built exe visibly with `Start-Process -PassThru`, write `Temp/phase1/player.pid`, and verify that PID is live with a non-empty main window. If LicensingClient IPC/activation fails, record BLOCKED and stop before the GUI checkpoint. Update the single `01-BASELINE.md` only from live Git/XML/trace output/BuildReport/exe/PID evidence, including exact `Test count: 16`, the live seed/accepted-action summary, and summary PID equal to the pid file. Set `Human checkpoint: PENDING` and `GUI status: NOT OBSERVED` because post-expansion D-13 has not yet been approved. Do not pre-write final GUI PASS.</action>
  <acceptance_criteria>
    - Every current descriptor path/blob is identical to `b18320e`; both fixtures compile in the predefined Editor assembly and Unity discovers each of the 16 exact cases once.
    - All 16 cases pass with exact run total/passed counts of 16 and zero failed/errors/skipped/inconclusive; trace assertions contain no score or wall-correctness golden contract.
    - BuildReport is Succeeded, the fixed exe exists, and the launched Player has a numeric live PID and non-empty window title.
    - Before the checkpoint, `01-BASELINE.md` records `Test count: 16`, live seed 1557 and accepted-action count, matching live pid-file/summary PID, EditMode/build/licensing PASS, `Human checkpoint: PENDING`, and `GUI status: NOT OBSERVED`.
    - Licensing IPC/activation failure remains BLOCKED and stops before Task 2.
  </acceptance_criteria>
  <verify>
    <automated>
      New-Item -ItemType Directory -Force -Path 'Temp\phase1' | Out-Null
      $baselineDescriptors = @{}
      git ls-tree -r b18320e -- Assets | ForEach-Object { if ($_ -match '^\d+\s+blob\s+([0-9a-f]+)\t(.+\.(?:asmdef|asmref))$') { $baselineDescriptors[$matches[2]] = $matches[1] } }
      $currentDescriptors = @(Get-ChildItem -LiteralPath Assets -Recurse -File | Where-Object { $_.Extension -in '.asmdef','.asmref' } | ForEach-Object { [IO.Path]::GetRelativePath($PWD.Path, $_.FullName).Replace('\','/') } | Sort-Object)
      if ($currentDescriptors.Count -ne $baselineDescriptors.Count) { exit 1 }
      foreach ($path in $currentDescriptors) { if (-not $baselineDescriptors.ContainsKey($path) -or (git hash-object -- $path) -ne $baselineDescriptors[$path]) { exit 1 } }
      $traceSource = Get-Content -Raw 'Assets/Editor/Tests/MahjongRoundTraceTests.cs'
      $assertLines = @($traceSource -split "`r?`n" | Where-Object { $_ -match 'Assert\.' })
      if (($assertLines -join "`n") -match 'MahjongWin|MahjongYaku|MahjongWinInfo|GetScore|remainingTsumoCount|Yama\.Count') { exit 1 }
      $unity = 'C:\Program Files\Unity\Hub\Editor\2022.3.29f1\Editor\Unity.exe'
      &amp; $unity -batchmode -nographics -projectPath $PWD -runTests -testPlatform EditMode -testResults "$PWD\Temp\phase1\editmode.xml" -quit -logFile "$PWD\Temp\phase1\editmode.log"
      if ($LASTEXITCODE -ne 0 -or -not (Test-Path 'Temp\phase1\editmode.xml')) { exit 1 }
      [xml]$xml = Get-Content -Raw 'Temp\phase1\editmode.xml'
      $traceExpected = @('SameSeedAndActions_ProduceIdenticalTrace','ActionCap_ReportsFirstMismatch','TraceContract_ContainsOnlyDecisionFields')
      $lifeExpected = @('ConfirmForfeit_FinalizesOnceWithoutSavingHighScore','StartNewGame_Twice_DetachesAndResetsSession','RequestForfeit_EntersPendingWithoutPausingTimer','SecondEscape_CancelsPendingForfeit','TimeoutAtConfirmation_WinsAndRejectsLateCallbacks','PlayerHand_Subscriptions_AreSymmetricAndUnique','ForfeitPending_BlocksGameplayInputButAllowsEscape','RemainingTimeIndicator_ReconstructsWithoutDuplicateSubscriptions','ScoreDistanceInfo_ReconstructsWithoutDuplicateSubscriptions','GameUiInitialize_Twice_UsesExplicitPanelState','ForfeitConfirmation_ShowHide_DoesNotPauseTimer','GameOver_Forfeit_RendersReasonAndDistance','ReturnToMainMenu_ThenStart_UsesExistingRoute')
      $traceCases = @($xml.SelectNodes("//test-case[contains(@fullname,'MahjongRoundTraceTests')]"))
      $lifeCases = @($xml.SelectNodes("//test-case[contains(@fullname,'SoloSessionLifecycleTests')]"))
      $cases = @($traceCases + $lifeCases)
      $run = $xml.SelectSingleNode('/test-run')
      if ($traceCases.Count -ne 3 -or $lifeCases.Count -ne 13 -or $cases.Count -ne 16 -or @($cases | Where-Object { $_.result -ne 'Passed' }).Count -ne 0 -or $null -eq $run -or [int]$run.total -ne 16 -or [int]$run.passed -ne 16 -or [int]$run.failed -ne 0 -or ($run.HasAttribute('errors') -and [int]$run.errors -ne 0) -or ($run.HasAttribute('skipped') -and [int]$run.skipped -ne 0) -or ($run.HasAttribute('inconclusive') -and [int]$run.inconclusive -ne 0)) { exit 1 }
      foreach ($name in @($traceExpected + $lifeExpected)) { if (@($cases | Where-Object { $_.fullname -like "*.$name" }).Count -ne 1) { exit 1 } }
      $traceEvidence = (Get-Content -Raw 'Temp\phase1\editmode.xml') + "`n" + (Get-Content -Raw 'Temp\phase1\editmode.log')
      if ($traceEvidence -notmatch '(?is)seed\s*=\s*1557.{0,300}acceptedActions\s*=\s*\d+.{0,300}PASS') { exit 1 }
      &amp; $unity -batchmode -nographics -projectPath $PWD -executeMethod Phase1Build.BuildWindowsPlayer -quit -logFile "$PWD\Temp\phase1\build.log"
      if ($LASTEXITCODE -ne 0 -or -not (Test-Path 'Builds\phase1\RiichiNya.exe') -or -not (Test-Path 'Temp\phase1\build-report.txt') -or -not (Select-String -LiteralPath 'Temp\phase1\build-report.txt' -Pattern 'Succeeded' -Quiet)) { exit 1 }
      $player = Start-Process -FilePath (Resolve-Path 'Builds\phase1\RiichiNya.exe') -PassThru
      Start-Sleep -Seconds 3
      $player.Refresh()
      if ($player.HasExited -or [string]::IsNullOrWhiteSpace($player.MainWindowTitle)) { exit 1 }
      Set-Content -LiteralPath 'Temp\phase1\player.pid' -Value $player.Id
      $text = Get-Content -Raw '.planning/phases/01-executable-baseline/01-BASELINE.md'
      if ($text -notmatch '(?m)^-\s*Test count:\s*16\s*$' -or $text -notmatch '(?m)^-\s*Seeded trace:\s*.*seed\s*=\s*1557.*acceptedActions\s*=\s*\d+.*$' -or $text -notmatch ('(?m)^-\s*Player PID:\s*' + $player.Id + '\s*$')) { exit 1 }
      foreach ($field in @('EditMode status','BuildReport status','Licensing status')) { if ($text -notmatch ('(?m)^-\s*' + [regex]::Escape($field) + ':\s*PASS\s*$')) { exit 1 } }
      if ($text -notmatch '(?m)^-\s*Human checkpoint:\s*PENDING\s*$' -or $text -notmatch '(?m)^-\s*GUI status:\s*NOT OBSERVED\s*$') { exit 1 }
    </automated>
    <human-check>D-12/D-13 interaction and visible state are accepted only by the following blocking-human checkpoint; automated artifact/process existence is not GUI PASS.</human-check>
  </verify>
  <done>All project fixtures are discovered and green, BuildReport is successful, the visible Windows Player remains running, automated evidence is recorded, and any licensing or visual uncertainty remains BLOCKED/NOT OBSERVED.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify the final same-process Windows Player lifecycle</name>
  <what-built>D-12/D-13 Windows Player at `Builds/phase1/RiichiNya.exe`, already launched by Codex after green EditMode and BuildReport gates.</what-built>
  <how-to-verify>
    1. Confirm the PID in `Temp/phase1/player.pid` stays the same for the whole check.
    2. From MainMenu use the existing solo start button; verify timer starts near 03:00 and score/distance/round/hand/river/game panels show a fresh session.
    3. Press Esc; verify the forfeit confirmation appears, gameplay input is blocked, and the visible timer continues decreasing per D-08.
    4. Choose cancel and verify PlayerTurn input returns. Open the confirmation again and press Esc a second time; verify the same cancellation behavior per D-09.
    5. Open it a third time and confirm. Verify the existing result screen shows reason `포기` and the current distance, and the saved high score shown before/after is unchanged per D-06.
    6. Use the existing result back path to return to MainMenu, then the same existing start button to start again without closing the process.
    7. Verify the second session resets timer, score, distance, round, hand, river, transient/result/confirmation panels, input flags, and visible event effects; one action must not produce duplicate UI/domain reactions per D-07.
    8. Do not wait for an actual 180-second timeout; its confirmation-race priority is covered by `SoloSessionLifecycleTests`, consistent with D-13.
  </how-to-verify>
  <resume-signal>Reply `approved` only if every step is observed. Otherwise describe the mismatch or uncertainty exactly; Task 3 records APPROVED/REJECTED and PASS/FAIL/BLOCKED/NOT OBSERVED without inference.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the checkpoint result and seal the final evidence ledger</name>
  <files>.planning/phases/01-executable-baseline/01-BASELINE.md</files>
  <read_first>
    - `AGENTS.md` evidence preservation and no-fabricated-result rules
    - `.planning/phases/01-executable-baseline/01-CONTEXT.md` D-10 through D-13
    - `.planning/phases/01-executable-baseline/01-BASELINE.md` current automated evidence and pending GUI fields
    - `Temp/phase1/editmode.xml`, `Temp/phase1/build-report.txt`, and `Temp/phase1/player.pid` live raw evidence
  </read_first>
  <action>After Task 2 resumes, update only `01-BASELINE.md` with the actual response. Record `Human checkpoint: APPROVED` and `GUI status: PASS` only for an exact `approved` response after all eight same-PID steps; otherwise first record `REJECTED` plus the causally correct `FAIL`, `BLOCKED`, or `NOT OBSERVED` and preserve the user's mismatch or uncertainty text. Independently refresh every automated field from live sources regardless of GUI outcome: require `refs/tags/portfolio-baseline` to remain an annotated tag object whose peeled target is exactly `b18320ec1d9d647900d2173049819bab6bd47175`, require the ledger `Baseline commit` to equal that fixed commit, and refresh target commit and Unity version; require the exact 3 trace + 13 lifecycle names and `test-run total=16`, `passed=16`, zero failed/errors/skipped/inconclusive; exact `Test count: 16`; `seed=1557` plus accepted-action count copied from live trace XML/log output; descriptor path/blob equality with `b18320e`; Succeeded `Temp/phase1/build-report.txt`; existing `Builds/phase1/RiichiNya.exe`; summary PID equal to `Temp/phase1/player.pid`; and that PID resolving to the live built process. Validate licensing from the fresh successful Unity runs and absence of LicensingClient IPC/activation failure in their logs, never from GUI status. After all evidence is durably recorded, require both APPROVED and PASS for Task 3 and Phase 1 completion; rejection, uncertainty, or any non-PASS GUI state exits nonzero. The repair route is to correct the recorded source/environment/interaction cause, rerun Task 1's licensed build/launch gate, repeat Task 2 on one PID, and rerun this validator. Missing licensing evidence, XML, report, exe, pid file, live process, or resolved checkpoint must leave the corresponding field FAIL/BLOCKED/NOT OBSERVED and fail terminal completion; none may become PASS. Raw outputs remain ignored. This task is the sole post-checkpoint writer; let execute-plan commit the updated ledger atomically with the task and do not claim completion if the validator fails.</action>
  <acceptance_criteria>
    - `01-BASELINE.md` contains the explicit Task 2 approval/rejection and matching GUI status; no checkpoint result remains PENDING, and only APPROVED plus PASS permits task/phase completion.
    - Automated status validation runs regardless of GUI status and proves exact 3 + 13 = 16 names/counts/run totals, `Test count: 16`, live seed/accepted-action evidence, descriptor baseline, BuildReport, exe, pid-file/summary equality, and live process identity.
    - BASE-01 terminal validation proves the ref object type is `tag`, its peeled target is exactly `b18320ec1d9d647900d2173049819bab6bd47175`, and the ledger `Baseline commit` equals that same fixed commit.
    - Rejection, uncertainty, or any non-PASS GUI state is recorded first and then exits nonzero; artifact existence cannot unlock completion, and the documented repair/retry route must be followed.
    - Missing licensing/XML/report/exe/PID/checkpoint evidence remains FAIL/BLOCKED/NOT OBSERVED and fails terminal completion, never PASS.
    - `Temp/phase1/**` and `Builds/phase1/**` remain untracked, and the updated ledger is the only committed Phase 1 evidence summary.
  </acceptance_criteria>
  <verify>
    <automated>
      $summary = '.planning/phases/01-executable-baseline/01-BASELINE.md'
      $summaries = @(Get-ChildItem '.planning/phases/01-executable-baseline' -File -Filter '*-BASELINE.md')
      if ($summaries.Count -ne 1 -or -not (Test-Path $summary)) { exit 1 }
      $text = Get-Content -Raw $summary
      $required = @('Baseline tag','Baseline commit','Target commit','Unity version','EditMode command','EditMode status','Test count','Seeded trace','Windows build command','BuildReport','BuildReport status','Player executable','Player PID','Licensing status','Human checkpoint','GUI status','Raw artifacts','Before','After')
      $values = @{}
      foreach ($field in $required) { $match = [regex]::Match($text, '(?m)^-\s*' + [regex]::Escape($field) + ':\s*(\S.*)$'); if (-not $match.Success -or $match.Groups[1].Value -match '^(TBD|TODO|N/A|<.*>|)$') { exit 1 }; $values[$field] = $match.Groups[1].Value.Trim() }
      $fixedBaseline = 'b18320ec1d9d647900d2173049819bab6bd47175'
      $tagType = git cat-file -t refs/tags/portfolio-baseline
      $peeledBaseline = git rev-parse 'refs/tags/portfolio-baseline^{}'
      if ($values['Baseline tag'] -ne 'portfolio-baseline' -or $tagType -ne 'tag' -or $peeledBaseline -ne $fixedBaseline -or $values['Baseline commit'] -ne $fixedBaseline -or $values['Target commit'] -ne (git rev-parse HEAD)) { exit 1 }
      $unityVersion = ((Get-Content 'ProjectSettings/ProjectVersion.txt' | Select-Object -First 1) -replace '^m_EditorVersion:\s*','').Trim()
      if ($values['Unity version'] -ne $unityVersion -or $values['Human checkpoint'] -notmatch '^(APPROVED|REJECTED)$' -or $values['GUI status'] -notmatch '^(PASS|FAIL|BLOCKED|NOT OBSERVED)$') { exit 1 }
      $baselineDescriptors = @{}
      git ls-tree -r b18320e -- Assets | ForEach-Object { if ($_ -match '^\d+\s+blob\s+([0-9a-f]+)\t(.+\.(?:asmdef|asmref))$') { $baselineDescriptors[$matches[2]] = $matches[1] } }
      $currentDescriptors = @(Get-ChildItem -LiteralPath Assets -Recurse -File | Where-Object { $_.Extension -in '.asmdef','.asmref' } | ForEach-Object { [IO.Path]::GetRelativePath($PWD.Path, $_.FullName).Replace('\','/') } | Sort-Object)
      if ($currentDescriptors.Count -ne $baselineDescriptors.Count) { exit 1 }
      foreach ($path in $currentDescriptors) { if (-not $baselineDescriptors.ContainsKey($path) -or (git hash-object -- $path) -ne $baselineDescriptors[$path]) { exit 1 } }
      if (-not (Test-Path 'Temp\phase1\editmode.xml') -or -not (Test-Path 'Temp\phase1\editmode.log')) { exit 1 }
      [xml]$xml = Get-Content -Raw 'Temp\phase1\editmode.xml'
      $traceExpected = @('SameSeedAndActions_ProduceIdenticalTrace','ActionCap_ReportsFirstMismatch','TraceContract_ContainsOnlyDecisionFields')
      $lifeExpected = @('ConfirmForfeit_FinalizesOnceWithoutSavingHighScore','StartNewGame_Twice_DetachesAndResetsSession','RequestForfeit_EntersPendingWithoutPausingTimer','SecondEscape_CancelsPendingForfeit','TimeoutAtConfirmation_WinsAndRejectsLateCallbacks','PlayerHand_Subscriptions_AreSymmetricAndUnique','ForfeitPending_BlocksGameplayInputButAllowsEscape','RemainingTimeIndicator_ReconstructsWithoutDuplicateSubscriptions','ScoreDistanceInfo_ReconstructsWithoutDuplicateSubscriptions','GameUiInitialize_Twice_UsesExplicitPanelState','ForfeitConfirmation_ShowHide_DoesNotPauseTimer','GameOver_Forfeit_RendersReasonAndDistance','ReturnToMainMenu_ThenStart_UsesExistingRoute')
      $traceCases = @($xml.SelectNodes("//test-case[contains(@fullname,'MahjongRoundTraceTests')]"))
      $lifeCases = @($xml.SelectNodes("//test-case[contains(@fullname,'SoloSessionLifecycleTests')]"))
      $cases = @($traceCases + $lifeCases)
      $run = $xml.SelectSingleNode('/test-run')
      $xmlOk = $traceCases.Count -eq 3 -and $lifeCases.Count -eq 13 -and $cases.Count -eq 16 -and @($cases | Where-Object { $_.result -ne 'Passed' }).Count -eq 0 -and $null -ne $run -and [int]$run.total -eq 16 -and [int]$run.passed -eq 16 -and [int]$run.failed -eq 0 -and (-not $run.HasAttribute('errors') -or [int]$run.errors -eq 0) -and (-not $run.HasAttribute('skipped') -or [int]$run.skipped -eq 0) -and (-not $run.HasAttribute('inconclusive') -or [int]$run.inconclusive -eq 0)
      foreach ($name in @($traceExpected + $lifeExpected)) { $xmlOk = $xmlOk -and @($cases | Where-Object { $_.fullname -like "*.$name" }).Count -eq 1 }
      $traceEvidence = (Get-Content -Raw 'Temp\phase1\editmode.xml') + "`n" + (Get-Content -Raw 'Temp\phase1\editmode.log')
      $traceMatch = [regex]::Match($traceEvidence, '(?is)seed\s*=\s*1557.{0,300}acceptedActions\s*=\s*(\d+)')
      $traceSummaryOk = $traceMatch.Success -and $values['Seeded trace'] -match 'seed\s*=\s*1557' -and $values['Seeded trace'] -match ('acceptedActions\s*=\s*' + $traceMatch.Groups[1].Value)
      $reportOk = (Test-Path 'Temp\phase1\build-report.txt') -and (Select-String -LiteralPath 'Temp\phase1\build-report.txt' -Pattern 'Succeeded' -Quiet)
      $exePath = (Resolve-Path 'Builds\phase1\RiichiNya.exe' -ErrorAction SilentlyContinue)
      $pidText = if (Test-Path 'Temp\phase1\player.pid') { (Get-Content -Raw 'Temp\phase1\player.pid').Trim() } else { '' }
      $process = if ($pidText -match '^\d+$') { Get-Process -Id ([int]$pidText) -ErrorAction SilentlyContinue } else { $null }
      $pidOk = $values['Player PID'] -eq $pidText -and $null -ne $process -and $null -ne $exePath -and $process.Path -eq $exePath.Path -and -not [string]::IsNullOrWhiteSpace($process.MainWindowTitle)
      $logsOk = (Test-Path 'Temp\phase1\build.log') -and -not (Select-String -Path 'Temp\phase1\editmode.log','Temp\phase1\build.log' -Pattern '(?i)LicensingClient.*(IPC|timeout|failed|error)|(IPC|timeout|failed|error).*LicensingClient' -Quiet)
      if (-not $xmlOk -or $values['EditMode status'] -ne 'PASS' -or $values['Test count'] -ne '16' -or -not $traceSummaryOk) { exit 1 }
      if (-not $reportOk -or $values['BuildReport status'] -ne 'PASS' -or $values['BuildReport'] -notmatch 'Temp[\\/]phase1[\\/]build-report\.txt') { exit 1 }
      if ($null -eq $exePath -or $values['Player executable'] -notmatch 'Builds[\\/]phase1[\\/]RiichiNya\.exe' -or -not $pidOk) { exit 1 }
      if (-not $logsOk -or $values['Licensing status'] -ne 'PASS') { exit 1 }
      $trackedRaw = git ls-files -- 'Temp/phase1/**' 'Builds/phase1/**'
      if ($trackedRaw) { exit 1 }
      if ($values['Human checkpoint'] -ne 'APPROVED' -or $values['GUI status'] -ne 'PASS') { exit 1 }
    </automated>
  </verify>
  <done>The final ledger records the real human outcome, proves the fixed annotated-tag invariant and all XML/build/exe/PID links, keeps raw artifacts untracked, and reaches terminal Phase 1 completion only with Human checkpoint APPROVED and GUI status PASS; every other result remains recorded but incomplete pending repair and retry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Serialized scene UI -> manager callbacks | Local button/Esc intents cross into session finalization state. |
| Unity build artifacts -> visible Player | Build success alone does not establish that the executable launched or the GUI path worked. |
| Human observation -> committed evidence | A GUI PASS is trusted only after the exact same-process checklist is explicitly approved. |

## STRIDE Threat Register — ASVS L1, block threshold high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-09 | Tampering | scene button wiring | high | mitigate | The existing thirteen-case Editor fixture opens SampleScene and proves one confirmation object, one matching panel entry, and exactly one persistent listener per Button targeting the intended MahjongGameManager methods; the real Player check covers visible behavior. |
| T-01-10 | Repudiation | GUI evidence | high | mitigate | Record build/test outputs, process PID, and the actual checkpoint result before validation; require exact APPROVED plus GUI PASS for successful terminal exit, with every rejection or uncertainty preserved and blocking repair/retry. |
| T-01-11 | Tampering | second-session UI state | medium | mitigate | Explicit panel activation/deactivation replaces TogglePanel initialization and the same-process checklist verifies no stale result/confirmation panels. |
| T-01-12 | Denial of Service | LicensingClient/build launch | medium | mitigate | Treat IPC/activation failure as a hard BLOCKED precondition and never continue to GUI evidence without a licensed successful build. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID | Feature / Constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Repeat the existing solo experience and safely judge changes | 01-01, 01-02, 01-03 | COVERED | Tag/trace/build, lifecycle, and real Player form one reachable path. |
| REQ | BASE-01 | Exact annotated baseline tag | 01-01 | COVERED | Exact tag type and peeled commit gate. |
| REQ | BASE-02 | Fixed seed/action solo reproduction | 01-01 | COVERED | Bounded D-03 compact trace. |
| REQ | BASE-03 | Windows Player compiles and runs without runtime UnityEditor | 01-01, 01-03 | COVERED | Build plus launched/observed Player, not artifact-only. |
| REQ | BASE-04 | Batch EditMode project tests and result file | 01-01, 01-02, 01-03 | COVERED | Positive fixture/case counts and zero failure/error totals. |
| REQ | BASE-05 | Start/end-or-forfeit/restart in one session | 01-02, 01-03 | COVERED | Automated lifecycle regressions plus D-13 checkpoint. |
| RESEARCH | — | Existing Assembly-CSharp/Test Framework and Editor-only build boundary | 01-01 | COVERED | No package or broad assembly migration. |
| RESEARCH | — | One manager-owned, idempotent local session lifecycle | 01-02 | COVERED | Detach-before-replace and one finalization path. |
| RESEARCH | — | Existing panel maps/GameOver/menu start path | 01-03 | COVERED | One scene panel and existing route reuse. |
| RESEARCH | — | Fixed ignored raw paths and one committed summary | 01-01, 01-03 | COVERED | `Temp/phase1`, `Builds/phase1`, `01-BASELINE.md`. |
| RESEARCH | — | LicensingClient is a hard environment gate | 01-01, 01-03 | COVERED | BLOCKED until licensed execution succeeds. |
| CONTEXT | D-01, D-02, D-03, D-04 | Small bounded deterministic trace without scoring claims | 01-01 | COVERED | Exact compact fields resolved. |
| CONTEXT | D-05 | Fresh production random; fixed test seed only | 01-01, 01-02 | COVERED | Separate creation paths. |
| CONTEXT | D-06, D-07 | Forfeit result/high-score exclusion and clean restart | 01-02, 01-03 | COVERED | Tests, presenter, menu/start, checkpoint. |
| CONTEXT | D-08, D-09 | Confirmation, timer continuity, cancel/second Esc, timeout priority | 01-02, 01-03 | COVERED | Automated state tests and visible UI checklist. |
| CONTEXT | D-10, D-11 | Raw/local evidence split and complete one-document ledger | 01-01, 01-03 | COVERED | Field-by-field verification. |
| CONTEXT | D-12, D-13 | Codex-run exact Windows Player GUI order with uncertainty stop | 01-03 | COVERED | Visible launch plus blocking-human checkpoint. |

Deferred Esc-hold and future settings/pause integration are excluded exactly as `01-CONTEXT.md` directs.

<verification>
The final gate requires all project EditMode cases green, successful BuildReport, a live visible Player, the exact same-PID D-13 sequence, and a field-complete single evidence summary. Any licensing or UI uncertainty keeps the phase incomplete.
</verification>

<success_criteria>
- BASE-03 is proven by compilation, executable launch, and observed interaction rather than file existence alone.
- BASE-05 is proven by automated lifecycle tests and the real same-process start/forfeit/menu/restart sequence.
- D-01 through D-13 and every Phase 1 requirement are covered without deferred features or new dependencies.
- Public C# changes follow Korean XML summary, C# 9, Allman, lifecycle grouping, and symmetric subscription rules.
</success_criteria>

<output>
Create `.planning/phases/01-executable-baseline/01-03-SUMMARY.md` when done.
</output>
