---
phase: 01-executable-baseline
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - Assets/Scripts/UI-Kozeki/GameUIManager.cs
  - Assets/Scripts/UI-Kozeki/GameUIManager.cs.meta
  - Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs
  - Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs.meta
  - Assets/Scripts/MahjongGameManager.cs
  - Assets/Scripts/UI-Kozeki/UiManager.cs
  - Assets/Scenes/SampleScene.unity
  - Assets/Editor/Tests/SoloSessionLifecycleTests.cs
autonomous: true
requirements: [BASE-05]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-15/D-16: SoloScoringUIController owns solo output View references and explicit presentation methods; the game manager owns policy and calls it directly."
    - "D-23/D-25/D-26: forfeit confirmation is a scene-local overlay outside the mutually exclusive GameUIState map, and synchronous policy/input blocking precedes its presentation animation."
    - "D-24: the existing EventSystem, StandaloneInputModule, and Button.Navigation drive submit/cancel with Cancel selected when the overlay opens."
    - "D-17/D-27: no generalized multi-mode controller, modal framework, prefab, or second subscription path is introduced during this behavior-preserving refactor."
  artifacts:
    - path: "Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs"
      provides: "Solo presentation boundary and scene-local confirmation overlay"
      contains: "ShowForfeitConfirmation"
    - path: "Assets/Scenes/SampleScene.unity"
      provides: "Existing EventSystem/Button.Navigation and overlay wiring"
      contains: "ForfeitConfirmation"
  key_links:
    - from: "Assets/Scripts/MahjongGameManager.cs"
      to: "Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs"
      via: "explicit Initialize/Show/Hide/result presentation calls"
      pattern: "SoloScoringUIController"
    - from: "Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs"
      to: "UnityEngine.EventSystems.EventSystem"
      via: "SetSelectedGameObject(cancelButton.gameObject) when opening"
      pattern: "SetSelectedGameObject"
---

## Phase Goal

**As a** 플레이어와 개발자, **I want to** 기존 솔로 경험을 반복 재현하고, **so that** 안전하게 변경 여부를 판별할 수 있다.

<objective>
기존 GameUIManager 자산을 SoloScoringUIController로 개명하고, 01-01 확인창을 상호 배타적 panel map에서 분리한 scene-local overlay로 이전한다.

Purpose: 솔로 정책과 표현을 D-15/D-16 경계로 분리하면서 기존 uGUI/EventSystem/DOTween 동작을 그대로 보존한다.
Output: GUID-preserving UI controller rename, explicit solo View references, Cancel-default overlay, GREEN lifecycle gate.
</objective>

<execution_context>
@C:/Users/user/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-executable-baseline/01-CONTEXT.md
@.planning/phases/01-executable-baseline/01-02-SUMMARY.md
@C:/Users/user/.codex/skills/unity-test-gate/SKILL.md
@Assets/Scripts/UI-Kozeki/GameUIManager.cs
@Assets/Scripts/MahjongGameManager.cs
@Assets/Scripts/UI-Kozeki/UiManager.cs
@Assets/Scenes/SampleScene.unity

<interfaces>
`SoloScoringUIController` keeps `Initialize`, `HideAllPanels`, ordinary `GameUIState` panel methods, and volatile win presentation. Add explicit `ShowForfeitConfirmation`, `HideForfeitConfirmation`, and solo View update methods. Confirmation/cancel are UI events consumed by the game manager; the UI controller does not inspect session state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename the solo UI owner with a bounded compatibility seam</name>
  <reversibility rating="costly">The renamed scene component becomes the presentation contract used by later solo and four-player integration work; undoing it would coordinate several serialized and source references.</reversibility>
  <files>Assets/Scripts/UI-Kozeki/GameUIManager.cs, Assets/Scripts/UI-Kozeki/GameUIManager.cs.meta, Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs, Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs.meta, Assets/Editor/Tests/SoloSessionLifecycleTests.cs</files>
  <behavior>
    - The renamed component preserves the former script GUID and initializes the same ordinary gameplay panels twice without toggle inversion.
    - A state-free GameUIManager compatibility facade keeps untouched runtime callers compiling only until Task 2 and owns no MonoBehaviour lifecycle or events.
  </behavior>
  <action>Move `GameUIManager.cs` and its `.meta` together to `SoloScoringUIController.cs`, rename the MonoBehaviour type, preserve ordinary `GameUIState` panel behavior and existing DOTween transitions, and update the fixture to construct the renamed type. Keep a state-free `GameUIManager` static compatibility facade in the renamed source only across this task so untouched callers resolve `Instance`; it must not be a MonoBehaviour, subscribe to events, or own state. Preserve the script GUID and run the GREEN gate before moving presentation ownership. Do not introduce an interface, wrapper manager, service locator, or event bus; D-17 says mode controllers are intentionally not generalized.</action>
  <verify>
    <automated>&amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter SoloSessionLifecycleTests -ExpectedGate Green</automated>
  </verify>
  <done>The D-15 type name and serialized GUID are established, the compatibility seam is state-free and bounded to Task 2, and the behavior-preservation gate is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract the modal overlay from GameUIState and use native navigation</name>
  <files>Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs, Assets/Scripts/MahjongGameManager.cs, Assets/Scripts/UI-Kozeki/UiManager.cs, Assets/Scenes/SampleScene.unity</files>
  <behavior>
    - ForfeitConfirmation is absent from GameUIState/panel map and opens above unchanged gameplay panels.
    - Opening sets manager modal state and controller gameplay-input permission synchronously before presentation; DOTween completion is irrelevant to the block.
    - Existing EventSystem/StandaloneInputModule navigation selects Cancel by default and confirm/cancel Button clicks raise controller events exactly once.
  </behavior>
  <action>Remove the compatibility facade and migrate manager/menu callers to `SoloScoringUIController`. Move the serialized solo output references currently held by `MahjongGameManager`—score, distance, round, win, game-over, remaining-time, call display, and PlayerHandView—to the controller; expose narrowly named display/reset methods and have the manager call them. Remove ForfeitConfirmation from `GameUIState` and its serialized panel list. Give the controller direct serialized references to the existing overlay, confirm Button, and cancel Button; expose ConfirmRequested/CancelRequested events with symmetric lifecycle wiring and select the cancel button through the existing scene EventSystem when showing the overlay. In `MahjongGameManager.RequestForfeit`, perform policy flag assignment, controller gameplay-input disable, and `ShowForfeitConfirmation` synchronously in that order per D-23/D-26; cancel restores input and hides the overlay, while timeout hides it and finalizes TimeExpired before late actions. Keep Timer running. `UiManager` remains the lobby owner. Preserve the Korean TMP/material and visuals, set explicit Button.Navigation between Yes/No, and keep Cancel as the initial selection. Add no input polling, pause manager, prefab, modal registry, new scene, or package.</action>
  <verify>
    <automated>&amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter SoloSessionLifecycleTests -ExpectedGate Green</automated>
  </verify>
  <done>D-23 through D-26 are represented by one scene-local overlay and existing native UI navigation, while the approved forfeit/cancel/result behavior stays GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Session policy → presentation | The UI may render state but must not decide whether gameplay input or the timer is active. |
| EventSystem/Button → manager callback | Scene-configured clicks and submit events cross into session finalization. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-23 | Tampering | Modal policy | high | mitigate | The manager atomically sets modal state and input permission before invoking UI presentation; timeout closes then finalizes once. |
| T-01-24 | Elevation of Privilege | Confirm/cancel buttons | medium | mitigate | Scene tests require one persistent/native route per button and Cancel as the selected default. |
| T-01-25 | Denial of Service | DOTween overlay transition | medium | mitigate | Input blocking is synchronous and independent of tween completion; the timer remains active. |
| T-01-26 | Tampering | Renamed UI asset | medium | mitigate | Preserve `.meta` GUID and validate the real scene plus GREEN Unity compilation after each task. |
</threat_model>

<verification>
The global Unity helper provides XML-backed GREEN classification. Scene assertions in the lifecycle fixture must also prove one overlay, no matching GameUIState entry, existing EventSystem use, exact confirm/cancel wiring, and Cancel default selection.
</verification>

<success_criteria>
- D-15 through D-17 and D-23 through D-26 are implemented without changing lobby ownership or the existing visual assets.
- The overlay is not a mutually exclusive game panel, and no extra input manager, modal abstraction, prefab, scene, or package exists.
</success_criteria>

<output>
Create `.planning/phases/01-executable-baseline/01-03-SUMMARY.md` when done.
</output>
