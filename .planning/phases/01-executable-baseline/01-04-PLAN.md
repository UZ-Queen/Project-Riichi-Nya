---
phase: 01-executable-baseline
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - Assets/Scripts/MahjongGameManager.cs
  - Assets/Scripts/MahjongGameManager.cs.meta
  - Assets/Scripts/SoloScoringGameManager.cs
  - Assets/Scripts/SoloScoringGameManager.cs.meta
  - Assets/Scripts/UI-Kozeki/PlayerHandController.cs
  - Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs
  - Assets/Scripts/UI-Kozeki/UiManager.cs
  - Assets/Scripts/UI-Kozeki/MahjongTileGameObject.cs
  - Assets/Scenes/SampleScene.unity
  - Assets/Editor/Tests/SoloSessionLifecycleTests.cs
autonomous: true
requirements: [BASE-05]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-14/D-16: SoloScoringGameManager is the sole solo lifecycle/policy owner and directly consumes PlayerHandController intents and drives SoloScoringUIController outputs."
    - "D-18/D-19: one inactive-by-default serialized SoloScoringModeRoot is activated by UiManager for play, remains active through result display, and is disabled on lobby return; OnEnable/OnDisable subscriptions are symmetric."
    - "D-23: the manager owns modal-open and gameplay-input policy while the 180-second timer continues and TimeExpired wins races."
    - "D-17/D-27/D-28: shared rules stay in MahjongRound, lobby stays in UiManager, no four-player/generalized layer is added, and the completed 01-01 slice remains preserved."
  artifacts:
    - path: "Assets/Scripts/SoloScoringGameManager.cs"
      provides: "Named solo mode lifecycle, policy, domain-event, timer, score, and finalization owner"
      contains: "class SoloScoringGameManager"
    - path: "Assets/Scenes/SampleScene.unity"
      provides: "Single-scene inactive solo mode root and serialized owner wiring"
      contains: "SoloScoringModeRoot"
  key_links:
    - from: "Assets/Scripts/UI-Kozeki/UiManager.cs"
      to: "Assets/Scripts/SoloScoringGameManager.cs"
      via: "activate mode root then StartNewGame; disable root on lobby return"
      pattern: "StartNewGame|SetActive"
    - from: "Assets/Scripts/SoloScoringGameManager.cs"
      to: "Assets/Scripts/UI-Kozeki/PlayerHandController.cs"
      via: "OnEnable/OnDisable direct intent subscriptions"
      pattern: "ForfeitRequested|OnPlayerDiscard|OnPlayerCall"
---

## Phase Goal

**As a** 플레이어와 개발자, **I want to** 기존 솔로 경험을 반복 재현하고, **so that** 안전하게 변경 여부를 판별할 수 있다.

<objective>
현재 MahjongGameManager를 실제 책임에 맞는 SoloScoringGameManager로 개명하고 단일 씬의 솔로 모드 Root 생명주기를 완성한다.

Purpose: 이후 FourPlayerGameManager가 솔로 타이머·점수·UI 정책과 얽히지 않도록 지금 증명된 솔로 흐름의 소유권을 명시한다.
Output: GUID-preserving manager rename, direct controller/UI wiring, inactive-by-default mode root, symmetric subscriptions, GREEN pre-expansion gate.
</objective>

<execution_context>
@C:/Users/user/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-executable-baseline/01-CONTEXT.md
@.planning/phases/01-executable-baseline/01-03-SUMMARY.md
@C:/Users/user/.codex/skills/unity-test-gate/SKILL.md
@Assets/Scripts/MahjongGameManager.cs
@Assets/Scripts/UI-Kozeki/PlayerHandController.cs
@Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs
@Assets/Scripts/UI-Kozeki/UiManager.cs
@Assets/Scenes/SampleScene.unity

<interfaces>
`SoloScoringGameManager` retains `Instance`, `StartNewGame`, `ConfirmForfeit`, `CancelForfeit`, `OnGameStart`, `OnGameOver`, and `OnStateChange`. It owns `MahjongRound`, Timer, `IScoreDistanceService`, end reason, confirmation state, finalization guard, and gameplay-input permission. `UiManager` activates/deactivates one serialized mode root and calls `StartNewGame`; it does not own session reset.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename the solo lifecycle owner without changing behavior</name>
  <reversibility rating="costly">Later phases depend on the mode-specific manager name and ownership boundary; undoing it would require coordinated source and scene changes.</reversibility>
  <files>Assets/Scripts/MahjongGameManager.cs, Assets/Scripts/MahjongGameManager.cs.meta, Assets/Scripts/SoloScoringGameManager.cs, Assets/Scripts/SoloScoringGameManager.cs.meta, Assets/Editor/Tests/SoloSessionLifecycleTests.cs</files>
  <behavior>
    - The renamed manager preserves the old script GUID and the exact two selected lifecycle cases remain GREEN.
    - Session start, round replacement, timer completion, score/distance, confirmation, persistence, and restart cleanup remain owned by this one type.
    - A temporary source compatibility facade keeps untouched callers compiling only until Task 2 migrates them; it owns no state and is removed in Task 2 before the legacy-name scan.
  </behavior>
  <action>Move `MahjongGameManager.cs` and its `.meta` together to `SoloScoringGameManager.cs`, rename all partial declarations, keep the same serialized fields and public callbacks, and update the lifecycle fixture to construct/assert the renamed type. If compilation requires it, place a state-free `MahjongGameManager` compatibility facade in the renamed source for Task 1 only; it may forward `Instance` but must not be a MonoBehaviour, subscribe to events, or own state. Preserve `MahjongRound` as the rules/round object per D-14. Update Korean XML summaries and group Unity lifecycle methods at the top without changing their behavior. Run the exact two-case GREEN gate before migrating callers.</action>
  <verify>
    <automated>$gateJson = &amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter SoloSessionLifecycleTests -ExpectedGate Green; $gate = $gateJson | ConvertFrom-Json; $expected = @('ConfirmForfeit_FinalizesOnceWithoutSavingHighScore','StartNewGame_Twice_DetachesAndResetsSession') | Sort-Object; $cases = @(([xml](Get-Content -Raw -LiteralPath $gate.resultsPath)).SelectNodes('/test-run//test-case')); $actual = @($cases | ForEach-Object { $_.name } | Sort-Object); if ($gate.status -ne 'GREEN' -or $gate.total -ne 2 -or $cases.Count -ne 2 -or (Compare-Object $expected $actual)) { $gateJson; exit 1 }</automated>
  </verify>
  <done>D-14 naming is present with preserved GUID and one state owner; the temporary facade is explicitly bounded through Task 2 and the exact two-case subset is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all runtime callers to the named mode owner</name>
  <files>Assets/Scripts/SoloScoringGameManager.cs, Assets/Scripts/UI-Kozeki/PlayerHandController.cs, Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs, Assets/Scripts/UI-Kozeki/UiManager.cs, Assets/Scripts/UI-Kozeki/MahjongTileGameObject.cs</files>
  <action>Replace every runtime `MahjongGameManager` singleton/type reference with `SoloScoringGameManager` in the four known callers, then remove the state-free compatibility facade from `SoloScoringGameManager.cs` before scanning. Keep the flow `PlayerHandController → SoloScoringGameManager → SoloScoringUIController` exactly once per D-16: PlayerHandController must not subscribe to UI events, and SoloScoringUIController must not subscribe to player gameplay intents. Keep UiManager as the only menu/start owner and keep tile sprite lookup using the renamed manager's existing TileDB until a later domain-owned asset lookup is explicitly scoped. Run the repository reference scan only after caller migration and facade removal, then run the exact two-case lifecycle subset; generated `.csproj` files are not edited.</action>
  <verify>
    <automated>$hits = rg -n --glob '*.cs' 'MahjongGameManager' Assets/Scripts; if ($LASTEXITCODE -gt 1) { exit $LASTEXITCODE }; if ($hits) { $hits; exit 1 }; $gateJson = &amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter SoloSessionLifecycleTests -ExpectedGate Green; $gate = $gateJson | ConvertFrom-Json; $expected = @('ConfirmForfeit_FinalizesOnceWithoutSavingHighScore','StartNewGame_Twice_DetachesAndResetsSession') | Sort-Object; $cases = @(([xml](Get-Content -Raw -LiteralPath $gate.resultsPath)).SelectNodes('/test-run//test-case')); $actual = @($cases | ForEach-Object { $_.name } | Sort-Object); if ($gate.status -ne 'GREEN' -or $gate.total -ne 2 -or $cases.Count -ne 2 -or (Compare-Object $expected $actual)) { $gateJson; exit 1 }</automated>
  </verify>
  <done>All runtime callers use the decided solo manager name and there is one direct input-up/output-down route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make the solo mode root own component subscriptions</name>
  <files>Assets/Scripts/SoloScoringGameManager.cs, Assets/Scripts/UI-Kozeki/UiManager.cs, Assets/Scenes/SampleScene.unity, Assets/Editor/Tests/SoloSessionLifecycleTests.cs</files>
  <behavior>
    - SoloScoringModeRoot is inactive at lobby startup, activates for solo start, remains active while GameOver is visible, and deactivates only on lobby return.
    - Each enable attaches one controller and UI confirm/cancel handler set; disable removes those exact handlers and round/timer handlers.
    - StartNewGame resets session state but does not perform component-lifetime subscription duplication.
  </behavior>
  <action>In `SampleScene.unity`, create one `SoloScoringModeRoot` using the existing solo manager/game canvas/controller hierarchy, set it inactive by default without duplicating components, and keep the single EventSystem outside that root. Have `UiManager` activate this serialized root before calling `StartNewGame`, leave it active through the result panel, and disable it on the existing back-to-lobby route. In `SoloScoringGameManager`, make OnEnable/OnDisable symmetrically attach/detach PlayerHandController discard/call/ForfeitRequested and SoloScoringUIController ConfirmRequested/CancelRequested events; StartNewGame resets only timer, score/distance, round, hand/river, UI, modal, and finalization state per D-19. Extend the two lifecycle cases with root-cycle and renamed-scene assertions, leaving the named thirteen-case expansion to 01-05 per D-27. Update scene persistent callback type names to `SoloScoringGameManager` and verify all renamed script GUIDs resolve. Add no second scene, DontDestroy mode owner, mode registry, or generalized base class.</action>
  <verify>
    <automated>$gateJson = &amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter SoloSessionLifecycleTests -ExpectedGate Green; $gate = $gateJson | ConvertFrom-Json; $expected = @('ConfirmForfeit_FinalizesOnceWithoutSavingHighScore','StartNewGame_Twice_DetachesAndResetsSession') | Sort-Object; $cases = @(([xml](Get-Content -Raw -LiteralPath $gate.resultsPath)).SelectNodes('/test-run//test-case')); $actual = @($cases | ForEach-Object { $_.name } | Sort-Object); if ($gate.status -ne 'GREEN' -or $gate.total -ne 2 -or $cases.Count -ne 2 -or (Compare-Object $expected $actual)) { $gateJson; exit 1 }</automated>
  </verify>
  <done>D-18/D-19 mode-root lifecycle and subscription symmetry are implemented, the compatibility facade remains absent, and the exact two-case pre-expansion subset is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Lobby root activation → gameplay lifecycle | A menu action enables components whose event subscriptions can otherwise duplicate. |
| Prior session callbacks → replacement session | Stale Timer/MahjongRound events must not mutate a new run. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-27 | Tampering | Renamed manager asset | medium | mitigate | Preserve the `.meta` GUID, update persistent callback type names, and validate through Unity compilation. |
| T-01-28 | Denial of Service | Enable/disable subscriptions | high | mitigate | Pair exact handlers in OnEnable/OnDisable and regression-count invocation lists after repeated root activation. |
| T-01-29 | Tampering | Prior-session callbacks | high | mitigate | Detach timer/round handlers before replacement/finalization and assert late callbacks are inert. |
</threat_model>

<verification>
Every task preserves a runnable GREEN slice using the global XML-backed helper. The final task also requires scene-structure assertions for one inactive root, one external EventSystem, valid script GUIDs, and exact subscription counts.
</verification>

<success_criteria>
- D-14 and D-16 through D-19 are fully implemented with D-23 policy ownership preserved.
- No legacy MahjongGameManager type reference, compatibility facade, duplicate mode owner, generalized multi-mode abstraction, second scene, package, or assembly descriptor remains.
</success_criteria>

<output>
Create `.planning/phases/01-executable-baseline/01-04-SUMMARY.md` when done.
</output>
