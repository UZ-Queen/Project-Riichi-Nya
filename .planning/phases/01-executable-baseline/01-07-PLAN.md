---
phase: 01-executable-baseline
plan: 07
type: execute
wave: 7
depends_on: ["01-06"]
files_modified:
  - Assets/Editor/Tests/SoloSessionLifecycleTests.cs
  - .planning/phases/01-executable-baseline/01-BASELINE.md
  - .planning/phases/01-executable-baseline/01-VALIDATION.md
autonomous: true
requirements: [BASE-04, BASE-05]
gap_closure: true
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-10/D-12/BASE-04: an interrupted lifecycle test cannot leave the player's pre-test Application.persistentDataPath/yaml.json overwritten or deleted."
    - "D-12/BASE-04: fixture setup restores any stale durable backup before another persistence mutation begins, and one focused EditMode case proves that recovery path."
    - "D-06/BASE-05: all three lifecycle persistence paths retain their timeout-save and Forfeit-no-save assertions while using the shared interruption-safe boundary."
    - "D-13: automated save-safety evidence does not replace the pending fresh same-PID Windows Player observation; absent or uncertain GUI evidence remains non-PASS."
  artifacts:
    - path: "Assets/Editor/Tests/SoloSessionLifecycleTests.cs"
      provides: "Fixture-local durable save guard, stale-run recovery, and one recovery regression"
      contains: "RecoverInterruptedSaveTest_RestoresDurableBackupBeforeNextMutation"
    - path: ".planning/phases/01-executable-baseline/01-BASELINE.md"
      provides: "Updated single tracked ledger for the 4 + 15 = 19 aggregate and save-safety closure"
      contains: "Test count: 4 + 15 = 19"
    - path: ".planning/phases/01-executable-baseline/01-VALIDATION.md"
      provides: "Plan 01-07 Nyquist map and unchanged D-13 human-only boundary"
      contains: "01-07"
  key_links:
    - from: "SoloSessionLifecycleTests persistence cases"
      to: "fixture-local durable save guard"
      via: "ConfirmForfeit_FinalizesOnceWithoutSavingHighScore, TimeoutDuringConfirmation_WinsAndRejectsLateActions, and TimeoutNewRecord_RendersAndPersistsUpdatedHighScore enter the same guard before SettingsManager can mutate yaml.json"
      pattern: "ConfirmForfeit_FinalizesOnceWithoutSavingHighScore|TimeoutDuringConfirmation_WinsAndRejectsLateActions|TimeoutNewRecord_RendersAndPersistsUpdatedHighScore"
    - from: "SoloSessionLifecycleTests SetUp/TearDown"
      to: "Application.persistentDataPath/yaml.json"
      via: "stale backup or originally-absent marker recovery runs before each test and again during cleanup"
      pattern: "SetUp|TearDown|File.Move|File.Delete"
    - from: "Unity Test Gate JSON"
      to: "01-BASELINE.md and 01-VALIDATION.md"
      via: "parseable XML with exact positive fixture counts and zero non-passed cases"
      pattern: "GREEN|4 + 15 = 19|RecoverInterruptedSaveTest_RestoresDurableBackupBeforeNextMutation"
---

## Phase Goal

**As a** solo-mode player and maintainer, **I want to** reproduce the existing solo flow and verify changes safely, **so that** I can tell whether established behavior has regressed.

<objective>
현재 검증에서 확인된 저장 안전성 blocker를 `SoloSessionLifecycleTests`의 공통 persistence 경계에서 닫는다.

Purpose: EditMode 실행이 중단되어도 플레이어의 실제 `yaml.json`을 다음 실행에서 복구할 수 있게 하며, 복구가 다음 mutation보다 먼저 일어남을 실행 가능한 회귀로 증명한다.
Output: test-only durable backup/recovery helper, 한 개의 stale recovery 회귀, 세 persistence case의 공통 guard 적용, 갱신된 단일 증거 ledger와 validation map.

Decision coverage: D-01: preserved; D-02: preserved; D-03: preserved; D-04: preserved; D-05: preserved; D-06: retained by the guarded Forfeit regression; D-07: preserved; D-08: preserved; D-09: retained by the guarded timeout regression; D-10: refreshed in the existing ledger; D-11: refreshed with exact counts; D-12: enforced by fail-closed XML and human evidence boundaries; D-13: remains human-only; D-14: preserved; D-15: preserved; D-16: preserved; D-17: preserved; D-18: preserved; D-19: preserved; D-20: preserved; D-21: preserved; D-22: preserved; D-23: retained by the guarded timeout regression; D-24: preserved; D-25: preserved; D-26: preserved; D-27: this gap closure changes only the failed test boundary; D-28: all six completed plans and summaries remain untouched.
</objective>

<execution_context>
@C:/Users/user/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/user/.codex/gsd-core/templates/summary.md
@C:/Users/user/.codex/skills/unity-test-gate/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-executable-baseline/01-CONTEXT.md
@.planning/phases/01-executable-baseline/01-RESEARCH.md
@.planning/phases/01-executable-baseline/01-PATTERNS.md
@.planning/phases/01-executable-baseline/01-VERIFICATION.md
@.planning/phases/01-executable-baseline/01-REVIEW.md
@.planning/phases/01-executable-baseline/01-06-SUMMARY.md
@Assets/Editor/Tests/SoloSessionLifecycleTests.cs
@Assets/Scripts/Configs/SettingsManager.cs

<interfaces>
Keep `SettingsManager.Save`, `SettingsManager.Load`, and the production `Application.persistentDataPath/yaml.json` contract unchanged. Reuse NUnit, `System.IO.File`, fixture `[SetUp]`/`[TearDown]`, and the existing Unity Test Gate. Add no package, asmdef, production interface, public test seam, configurable production save path, or generalized persistence framework.
</interfaces>
</context>

## Planning assumptions

- The blocker is an execution miss at the test boundary: production persistence is intentionally exercised, but `PreserveSaveFile` stores the only original copy in process memory.
- A same-directory durable backup plus an explicit originally-absent marker is sufficient to preserve both possible pre-test states without changing production code.
- Recovery operations are idempotent: a backup remains authoritative until moved back, and an absent marker remains authoritative until the test-created save is removed.
- The existing 14 lifecycle cases remain; one new recovery case raises the fixture to 15 and the project aggregate from 4 + 14 = 18 to 4 + 15 = 19.
- D-13 remains a separate human-only Windows Player observation after automated gap closure.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one interrupted save mutation recovers before the next mutation</name>
  <files>Assets/Editor/Tests/SoloSessionLifecycleTests.cs</files>
  <read_first>
    - `Assets/Editor/Tests/SoloSessionLifecycleTests.cs` — `[TearDown]`, `ConfirmForfeit_FinalizesOnceWithoutSavingHighScore`, `PreserveSaveFile`, and `RestoreSaveFile`
    - `Assets/Scripts/Configs/SettingsManager.cs` — fixed private `SaveFilePath` and the real `Save`/`Load` behavior the fixture must exercise unchanged
    - `.planning/phases/01-executable-baseline/01-VERIFICATION.md` — sole actionable persistence gap and required stale recovery check
    - `.planning/phases/01-executable-baseline/01-REVIEW.md` — CR-01 interruption scenario and same-directory backup recommendation
    - `.planning/phases/01-executable-baseline/01-CONTEXT.md` — D-06, D-10, D-12, D-13, D-27, and D-28 boundaries
    - `C:/Users/user/.codex/skills/unity-test-gate/SKILL.md` — exact GREEN/XML evidence contract
  </read_first>
  <behavior>
    - `RecoverInterruptedSaveTest_RestoresDurableBackupBeforeNextMutation`: after the fixture has durably guarded the real pre-test state and a synthetic test save remains as if the prior run stopped, recovery restores the exact original existence/bytes and clears only fixture-owned recovery artifacts.
    - The same case begins a second guarded mutation only after stale recovery and proves the durable backup-or-absent marker exists before the next write is permitted.
    - `ConfirmForfeit_FinalizesOnceWithoutSavingHighScore` continues to compare the test save byte-for-byte after Forfeit, then restores the player's pre-test state through the durable guard.
  </behavior>
  <action>Write the focused recovery case first. In the fixture only, replace the in-memory `byte[]` preservation contract with private save, backup, and originally-absent paths derived from `Application.persistentDataPath`. Add a `[SetUp]` recovery method that runs before every test and make `[TearDown]` invoke the same idempotent recovery before object cleanup. Before a persistence test can call `SettingsManager`, first recover any stale fixture artifacts; if the live save exists, atomically move it to a same-directory fixture backup, otherwise create the fixture-owned absent marker, and only then allow test mutation. Recovery gives an existing backup priority, removes any test-created live file, and moves the backup back; when only the absent marker exists, it removes any test-created live file and then the marker. Keep recovery artifacts private to this fixture and use `System.IO.File` only. Route `ConfirmForfeit_FinalizesOnceWithoutSavingHighScore` through this guard as the one end-to-end tracer path, retaining its byte-level Forfeit assertion per D-06. Do not edit `SettingsManager`, expose a runtime path override, or introduce a reusable persistence abstraction.</action>
  <verify>
    <automated>$gateJson = &amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter SoloSessionLifecycleTests.RecoverInterruptedSaveTest_RestoresDurableBackupBeforeNextMutation -ExpectedGate Green; $gate = $gateJson | ConvertFrom-Json; if ($gate.status -ne 'GREEN' -or $gate.total -ne 1 -or -not (Test-Path -LiteralPath $gate.resultsPath)) { $gateJson; exit 1 }; [xml]$xml = Get-Content -Raw -LiteralPath $gate.resultsPath; $cases = @($xml.SelectNodes('/test-run//test-case')); if ($cases.Count -ne 1 -or $cases[0].name -ne 'RecoverInterruptedSaveTest_RestoresDurableBackupBeforeNextMutation' -or $cases[0].result -ne 'Passed') { $gateJson; exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - The focused case proves exact original-file restoration for the state present when the test began and proves recovery completes before a second guarded mutation.
    - An interrupted process always leaves either the untouched live save, a same-directory durable backup, or an originally-absent marker that the next `[SetUp]` can recover.
    - The Forfeit case still performs its byte-for-byte no-save assertion and no production file is modified.
    - The focused helper run is GREEN only with one discovered passed XML case; missing XML, zero discovery, licensing failure, BLOCKED, RED, or FAIL does not pass.
  </acceptance_criteria>
  <done>D-06/D-10/D-12 and BASE-04 have one production-path tracer proving interrupted persistence recovery without a production seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard every lifecycle persistence path and refresh exact evidence</name>
  <files>Assets/Editor/Tests/SoloSessionLifecycleTests.cs, .planning/phases/01-executable-baseline/01-BASELINE.md, .planning/phases/01-executable-baseline/01-VALIDATION.md</files>
  <read_first>
    - `Assets/Editor/Tests/SoloSessionLifecycleTests.cs` — `TimeoutDuringConfirmation_WinsAndRejectsLateActions`, `TimeoutNewRecord_RendersAndPersistsUpdatedHighScore`, and the Task 1 durable guard
    - `Assets/Scripts/SoloScoringGameManager.cs` — production timeout save path invoked by both remaining tests
    - `Assets/Scripts/Configs/SettingsManager.cs` — unchanged production read/write path used by the guarded cases
    - `.planning/phases/01-executable-baseline/01-BASELINE.md` — existing 4 + 14 evidence and D-13 status
    - `.planning/phases/01-executable-baseline/01-VALIDATION.md` — existing Plan 01-06 Nyquist map and fail-closed classifications
    - `.planning/phases/01-executable-baseline/01-CONTEXT.md` — D-10 through D-13 and D-28 evidence/preservation contract
    - `C:/Users/user/.codex/skills/unity-test-gate/SKILL.md` — separate filtered gate and XML interpretation
  </read_first>
  <behavior>
    - `TimeoutDuringConfirmation_WinsAndRejectsLateActions` enters the durable guard before timeout can save and restores the player's prior save afterward.
    - `TimeoutNewRecord_RendersAndPersistsUpdatedHighScore` enters the same guard, still proves the rendered and loaded record equal the final score, and restores the player's prior save afterward.
    - All fifteen lifecycle cases pass together, including the recovery case and the unchanged Forfeit byte-preservation assertion.
  </behavior>
  <action>Replace the remaining `PreserveSaveFile`/`RestoreSaveFile(byte[])` call sites with the Task 1 guard and idempotent recovery, so all three persistence-mutating tests cross one shared test boundary before `SettingsManager.Save` or timeout finalization. Keep the existing behavioral assertions unchanged. Run the trace and lifecycle fixtures separately with the existing helper; require GREEN JSON, existing parseable XML, exactly four trace and fifteen lifecycle cases, unique case names, and zero non-passed cases. Refresh only the relevant test count/result and save-safety gap notes in the existing `01-BASELINE.md`, recording `4 + 15 = 19` and the recovery case name. Add the 01-07 recovery row to `01-VALIDATION.md`. Preserve the annotated tag, production build evidence, raw-output policy, earlier plan history, and explicit D-13 status. Do not claim the automated persistence gate observed Windows Player GUI behavior; a fresh same-PID D-13 forfeit → menu → restart observation remains required before Phase 1 can pass.</action>
  <verify>
    <automated>$traceJson = &amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter MahjongRoundTraceTests -ExpectedGate Green; $trace = $traceJson | ConvertFrom-Json; $lifeJson = &amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter SoloSessionLifecycleTests -ExpectedGate Green; $life = $lifeJson | ConvertFrom-Json; if ($trace.status -ne 'GREEN' -or $trace.total -ne 4 -or $life.status -ne 'GREEN' -or $life.total -ne 15) { $traceJson; $lifeJson; exit 1 }; foreach ($gate in @($trace,$life)) { if (-not (Test-Path -LiteralPath $gate.resultsPath)) { exit 1 }; [xml]$xml = Get-Content -Raw -LiteralPath $gate.resultsPath; $cases = @($xml.SelectNodes('/test-run//test-case')); if ($cases.Count -ne $gate.total -or @($cases | Where-Object { $_.result -ne 'Passed' }).Count -ne 0 -or @($cases | Group-Object name | Where-Object Count -ne 1).Count -ne 0) { exit 1 } }; $lifeXml = [xml](Get-Content -Raw -LiteralPath $life.resultsPath); $requiredCases = @('RecoverInterruptedSaveTest_RestoresDurableBackupBeforeNextMutation','ConfirmForfeit_FinalizesOnceWithoutSavingHighScore','TimeoutDuringConfirmation_WinsAndRejectsLateActions','TimeoutNewRecord_RendersAndPersistsUpdatedHighScore'); foreach ($name in $requiredCases) { if (@($lifeXml.SelectNodes('/test-run//test-case') | Where-Object { $_.name -eq $name -and $_.result -eq 'Passed' }).Count -ne 1) { exit 1 } }; $ledger = Get-Content -Raw '.planning/phases/01-executable-baseline/01-BASELINE.md'; $validation = Get-Content -Raw '.planning/phases/01-executable-baseline/01-VALIDATION.md'; foreach ($required in @('Test count: 4 + 15 = 19','RecoverInterruptedSaveTest_RestoresDurableBackupBeforeNextMutation')) { if ($ledger -notmatch [regex]::Escape($required)) { exit 1 } }; foreach ($required in @('01-07','4 + 15 = 19','RecoverInterruptedSaveTest_RestoresDurableBackupBeforeNextMutation','D-13')) { if ($validation -notmatch [regex]::Escape($required)) { exit 1 } }; if (git ls-files -- 'Logs/UnityTestGate/**' 'Temp/phase1/**' 'Builds/phase1/**') { exit 1 }</automated>
    <human-check>End-of-phase verification still must launch a fresh Windows Player and complete the D-13 same-PID forfeit → menu → restart path after selecting a non-default tile, then confirm only tile 6 is highlighted. Missing PID/evidence, licensing failure, or unobserved GUI remains NOT OBSERVED/BLOCKED, never PASS.</human-check>
  </verify>
  <acceptance_criteria>
    - Every lifecycle path that can write or delete `yaml.json` enters the durable fixture guard first and uses the same idempotent recovery.
    - Separate XML-backed GREEN runs prove exactly 4 trace + 15 lifecycle = 19 project-authored cases, including exactly one copy of each named persistence case.
    - `01-BASELINE.md` and `01-VALIDATION.md` record save-safety closure without changing D-13 from human-only pending evidence.
    - Raw helper/build/Player artifacts remain untracked and no production source, package, assembly descriptor, scene, or prefab is changed.
  </acceptance_criteria>
  <done>The sole structured persistence blocker is closed across all three mutation paths, with exact fail-closed automated evidence and the D-13 human boundary preserved.</done>
</task>

</tasks>

## Artifacts this phase produces

- No new production file, public API, package, assembly descriptor, scene, prefab, or persistence framework.
- One fixture-local durable backup/recovery protocol inside `Assets/Editor/Tests/SoloSessionLifecycleTests.cs`.
- One new Editor-only test: `RecoverInterruptedSaveTest_RestoresDurableBackupBeforeNextMutation`.
- Updated ignored Unity Test Gate XML/log artifacts plus refreshed tracked `01-BASELINE.md` and `01-VALIDATION.md` evidence.
- The pending D-13 same-PID Windows Player observation remains a separate end-of-phase human checkpoint.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| EditMode fixture → player persistence | Test-controlled values cross into the same local `yaml.json` path used by the player. |
| Durable recovery artifacts → live save | Backup/absent-marker state determines whether bytes are restored or a test-created save is removed. |
| Unity helper JSON/XML → tracked evidence | Process output is untrusted until exact positive XML discovery and pass counts agree. |

## STRIDE Threat Register — ASVS L1, block threshold high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-38 | Tampering | `SoloSessionLifecycleTests` persistence boundary | high | mitigate | Move an existing live save to a same-directory durable backup, record original absence before mutation, and recover stale state in `[SetUp]` before any later test write. |
| T-01-39 | Denial of Service | interrupted fixture cleanup | high | mitigate | Make recovery idempotent in both `[SetUp]` and `[TearDown]`; keep the authoritative backup/marker until restoration has completed. |
| T-01-40 | Information Disclosure | fixture backup of local save | low | accept | The backup stays beside the same local save under identical user permissions and is removed after recovery; no network or committed artifact is introduced. |
| T-01-41 | Repudiation | Unity test evidence | high | mitigate | Require helper GREEN plus parseable XML, exact positive counts and unique names, zero non-passed cases, and fail closed on missing XML, zero discovery, licensing failure, or BLOCKED/FAIL. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reproduce the solo flow and verify changes without risking established player state | 01-01→01-07 | COVERED | 01-07 closes the live-save safety blocker. |
| REQ | BASE-01 | Exact annotated baseline tag | 01-01, 01-05 | COVERED | Preserved; 01-07 does not touch Git tags. |
| REQ | BASE-02 | Fixed-seed representative trace | 01-01, 01-05, 01-06 | COVERED | Preserved and rerun in the aggregate gate. |
| REQ | BASE-03 | Windows Player without runtime UnityEditor imports | 01-01, 01-05 | COVERED | No runtime source is modified. |
| REQ | BASE-04 | Safe batch EditMode XML results | 01-01→01-07 | COVERED | 01-07 makes lifecycle persistence interruption-safe and requires exact XML. |
| REQ | BASE-05 | Same-process start/end-or-forfeit/restart | 01-01→01-07 | COVERED | Automated lifecycle assertions remain; D-13 remains pending human evidence. |
| RESEARCH | — | Predefined Assembly-CSharp-Editor tests with no descriptor | 01-01→01-07 | COVERED | Test-only edit in the existing fixture. |
| RESEARCH | — | Idempotent lifecycle cleanup | 01-04→01-07 | COVERED | Recovery is idempotent across setup, teardown, and interrupted runs. |
| RESEARCH | — | Fixed local evidence paths and truthful status | 01-01, 01-05→01-07 | COVERED | Raw artifacts stay ignored and GUI evidence is not inferred. |
| RESEARCH | — | No package, runtime save seam, or generalized framework | 01-07 | COVERED | Uses existing .NET/Unity primitives only. |
| CONTEXT | D-01 | Test-only fixed-seed reproduction | 01-01, 01-05, 01-06 | COVERED | Unchanged. |
| CONTEXT | D-02 | Tsumogiri through next-round first draw | 01-01, 01-05 | COVERED | Unchanged. |
| CONTEXT | D-03 | Compact PASS/first-mismatch output | 01-01, 01-05, 01-06 | COVERED | Unchanged. |
| CONTEXT | D-04 | No Phase 2 scoring claim | 01-01, 01-05, 01-06 | COVERED | No scoring assertion added. |
| CONTEXT | D-05 | Fresh production randomness | 01-01, 01-05 | COVERED | Production is untouched. |
| CONTEXT | D-06 | Forfeit result/current distance/no high-score update | 01-01, 01-05→01-07 | COVERED | Byte-level Forfeit no-save regression is retained behind the durable guard. |
| CONTEXT | D-07 | Clean menu return and restart | 01-01, 01-04→01-06 | COVERED | Existing automated path is preserved. |
| CONTEXT | D-08 | Esc modal blocks gameplay while timer runs | 01-01→01-05 | COVERED | Existing case remains in the 15-case fixture. |
| CONTEXT | D-09 | Cancel/second Esc and timeout priority | 01-01, 01-03, 01-05→01-07 | COVERED | Timeout case is guarded without changing behavior. |
| CONTEXT | D-10 | One tracked Markdown; raw local outputs | 01-01, 01-05→01-07 | COVERED | Existing ledger is refreshed; raw XML/logs remain ignored. |
| CONTEXT | D-11 | Complete before/after evidence fields | 01-01, 01-05→01-07 | COVERED | Exact aggregate and recovery case are added to the same ledger. |
| CONTEXT | D-12 | Codex-run verification; uncertainty is not PASS | 01-01, 01-05→01-07 | COVERED | Exact XML and fail-closed status checks are required. |
| CONTEXT | D-13 | Exact same-process Player sequence | 01-01, 01-05→01-07 | COVERED | Human-only observation remains pending and separate. |
| CONTEXT | D-14 | SoloScoringGameManager ownership/name | 01-04, 01-06 | COVERED | Production owner is unchanged. |
| CONTEXT | D-15 | SoloScoringUIController output ownership/name | 01-03, 01-06 | COVERED | Production UI is unchanged. |
| CONTEXT | D-16 | Player→GameManager→UI direct flow | 01-02→01-06 | COVERED | No intermediary is added. |
| CONTEXT | D-17 | No generalized mode controllers | 01-02→01-07 | COVERED | No production controller or framework is added. |
| CONTEXT | D-18 | Single scene, inactive selected mode root | 01-04→01-06 | COVERED | Scene is untouched. |
| CONTEXT | D-19 | Component/session lifecycle separation | 01-04→01-06 | COVERED | Runtime lifecycle is untouched. |
| CONTEXT | D-20 | Separate ForfeitRequested intent | 01-02, 01-05 | COVERED | Existing intent route is unchanged. |
| CONTEXT | D-21 | PlayerHandController/View split only | 01-02, 01-06 | COVERED | Neither component is modified. |
| CONTEXT | D-22 | Esc-first immediate return/input block | 01-02, 01-05 | COVERED | Existing regression remains in the fixture. |
| CONTEXT | D-23 | Manager policy, timer continues, timeout wins | 01-03→01-07 | COVERED | Timeout-priority test is guarded, not rewritten. |
| CONTEXT | D-24 | Existing EventSystem/navigation, Cancel default | 01-03, 01-05 | COVERED | UI is untouched. |
| CONTEXT | D-25 | Scene-local overlay outside panel map | 01-03, 01-05 | COVERED | UI is untouched. |
| CONTEXT | D-26 | Synchronous block before animation | 01-03, 01-05 | COVERED | Existing regression remains in the fixture. |
| CONTEXT | D-27 | Working slice → isolated refactor → final verification | 01-01→01-07 | COVERED | Gap closure follows verified implementation and changes only the failing test boundary. |
| CONTEXT | D-28 | Preserve completed commits/artifacts | 01-01→01-07 | COVERED | Plans 01-01 through 01-06 and their summaries remain untouched. |

Deferred ideas, Phase 2/3 rule work, riichi behavior, malformed-hand handling, and inherited Score-panel wiring are excluded because they are not actionable gaps in the latest `gaps:` section.

<verification>
- Task 1: one focused XML-backed stale-recovery case, with exact discovery and GREEN-only acceptance.
- Task 2: separate exact fixture runs proving 4 trace + 15 lifecycle = 19, all persistence cases present exactly once, updated ledger/validation, and raw outputs untracked.
- End-of-phase: a fresh same-PID D-13 Windows Player forfeit/menu/restart observation is still required; automated persistence safety does not satisfy it.
</verification>

<success_criteria>
- The player's pre-test save existence and exact bytes survive process interruption through durable same-directory state.
- Stale recovery runs before every later persistence mutation and is covered by a focused EditMode regression.
- All three persistence-mutating lifecycle tests use the shared fixture boundary with their original behavioral assertions intact.
- Exact 4 + 15 XML-backed evidence is recorded without turning absent GUI observation into PASS.
- No production source, package, asmdef, public test seam, generalized persistence framework, scene, or prefab is added or modified.
</success_criteria>

<output>
Create `.planning/phases/01-executable-baseline/01-07-SUMMARY.md` when done.
</output>
