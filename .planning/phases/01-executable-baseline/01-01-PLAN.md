---
phase: 01-executable-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Editor/Tests/MahjongRoundTraceTests.cs
  - Assets/Editor/Tests/SoloSessionLifecycleTests.cs
  - Assets/Editor/Phase1Build.cs
  - Assets/Scripts/AL-1S/MahjongTileAndBlock.cs
  - Assets/Scripts/UI-Kozeki/UiScoreDistanceInfo.cs
  - Assets/Scripts/GameEndReason.cs
  - Assets/Scripts/MahjongGameManager.cs
  - Assets/Scripts/UI-Kozeki/PlayerHand.cs
  - Assets/Scripts/UI-Kozeki/GameUIManager.cs
  - Assets/Scripts/UI-Kozeki/UiGameOver.cs
  - Assets/Scenes/SampleScene.unity
  - .planning/phases/01-executable-baseline/01-BASELINE.md
autonomous: false
requirements: [BASE-01, BASE-02, BASE-03, BASE-04, BASE-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-05: production solo starts with fresh randomness, while one EditMode trace repeats fixed seed 1557 through first exhaustive draw and the next round's first draw."
    - "D-03/D-04: trace output identifies seed, accepted action count, compact state, and PASS or the first mismatching action without claiming score correctness."
    - "The annotated tag portfolio-baseline peels exactly to b18320ec1d9d647900d2173049819bab6bd47175 without moving history."
    - "Runtime scripts contain no editor-only namespace imports and the fixed Windows x64 build entry point fails when BuildReport is not successful."
    - "D-10/D-11: raw XML, logs, build report text, and Player output stay under ignored local paths while one Markdown evidence summary is tracked."
    - "D-06/D-07/D-08/D-09: the first production slice starts solo, opens one Esc confirmation, confirms a Forfeit result without updating high score, returns to MainMenu, and starts a fresh second session in the same process while the timer continues during confirmation. A private pending-forfeit flag distinguishes this modal from unrelated Processing work."
    - "D-12/D-13: the first plan builds and launches the real Windows Player, records the named same-PID lifecycle checkpoint truthfully, and unlocks expansion only when the checkpoint is APPROVED with GUI status PASS."
  artifacts:
    - path: "Assets/Editor/Tests/MahjongRoundTraceTests.cs"
      provides: "Predefined Assembly-CSharp-Editor boundary, bounded fixed-seed tsumogiri trace, and first-mismatch diagnostics"
      contains: "MahjongRoundTraceTests"
    - path: "Assets/Editor/Tests/SoloSessionLifecycleTests.cs"
      provides: "The first two lifecycle regressions that anchor later expansion to thirteen cases"
      contains: "ConfirmForfeit_FinalizesOnceWithoutSavingHighScore"
    - path: "Assets/Editor/Phase1Build.cs"
      provides: "StandaloneWindows64 batch build entry point"
      contains: "BuildWindowsPlayer"
    - path: ".planning/phases/01-executable-baseline/01-BASELINE.md"
      provides: "Single committed before/after evidence ledger"
      contains: "portfolio-baseline"
    - path: "Assets/Scenes/SampleScene.unity"
      provides: "Production solo start, forfeit confirmation, result, menu, and restart composition root"
      contains: "ForfeitConfirmation"
  key_links:
    - from: "Assets/Editor/Tests/MahjongRoundTraceTests.cs"
      to: "Assets/Scripts/AL-1S/MahjongRound.cs"
      via: "NewRound(1557), GenerateYama, DiscardTile(13), OnNewRoundStart, and next-round OnTsumoTile"
      pattern: "NewRound|DiscardTile|OnNewRoundStart"
    - from: "Assets/Editor/Phase1Build.cs"
      to: "Assets/Scenes/SampleScene.unity"
      via: "fixed BuildPlayerOptions scene list and StandaloneWindows64 output"
      pattern: "SampleScene|StandaloneWindows64"
    - from: "Assets/Scenes/SampleScene.unity"
      to: "Assets/Scripts/MahjongGameManager.cs"
      via: "serialized existing start/back routes plus confirm/cancel callbacks for the same-process D-13 Player path"
      pattern: "StartNewGame|ConfirmForfeit|CancelForfeit"
  prohibitions:
    - requirement_id: "BASE-01"
      category: "source-preservation"
      statement: "MUST NOT force-move, delete, or overwrite an existing portfolio-baseline tag or rewrite Git history."
    - requirement_id: "BASE-02"
      category: "scope-integrity"
      statement: "MUST NOT turn the trace into runtime replay UI or present it as proof of yaku, han, fu, payment, wall-size, or shuffle correctness."
    - requirement_id: "BASE-03"
      category: "verification-integrity"
      statement: "MUST NOT report a licensing IPC failure, absent BuildReport output, or an unlaunched Player as a source PASS."
---

## Phase Goal

**As a** 플레이어와 개발자, **I want to** 기존 솔로 경험을 반복 재현하고, **so that** 안전하게 변경 여부를 판별할 수 있다.

<objective>
기존 소스를 보존하는 annotated tag에서 고정 시드 EditMode trace, production forfeit/restart slice, Windows Player build·launch, 같은 프로세스 재시작 확인까지 원자적 내부 milestone로 연결된 walking skeleton을 만든다. D-01~D-13의 happy path를 먼저 증명하고 후속 plan은 경합·재구독·반복 초기화 회귀를 확장한다.

Purpose: 이후 수정이 기존 솔로 흐름을 바꿨는지 실제 Unity 명령과 Git 기준으로 판별한다.
Output: exact baseline tag, predefined Editor-assembly EditMode trace, minimum forfeit/result/menu/restart runtime path, Windows build method and launched Player, named D-13 checkpoint, `01-BASELINE.md`.
</objective>

<execution_context>
@C:/Users/user/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-executable-baseline/01-CONTEXT.md
@.planning/phases/01-executable-baseline/01-RESEARCH.md
@.planning/phases/01-executable-baseline/01-PATTERNS.md
@.planning/phases/01-executable-baseline/01-VALIDATION.md
@.planning/codebase/TESTING.md
@Assets/Scripts/AL-1S/MahjongRound.cs
@Assets/Scripts/AL-1S/MahjongTileAndBlock.cs
@Assets/Scripts/UI-Kozeki/UiScoreDistanceInfo.cs
@Assets/Editor/MahjongTileDataGenerator.cs
@ProjectSettings/EditorBuildSettings.asset
@.gitignore

<interfaces>
Use the existing `MahjongRound.NewRound(int, out MahjongPlayer)`, `GenerateYama()`, `DiscardTile(13)`, `OnTsumoTile`, and `OnNewRoundStart` public contracts. Use Unity's built-in `BuildPipeline.BuildPlayer`, `BuildPlayerOptions`, `BuildTarget.StandaloneWindows64`, and `BuildReport.summary.result`; do not add packages or a runtime build dependency.
</interfaces>
</context>

## Flagged Planner Assumptions — spec-less edge probe

| Requirement | Status | Explicit flagged assumption |
|---|---|---|
| BASE-01 | RESOLVED | Annotated-object type plus exact peeled commit equality is the baseline-tag edge contract; Task 1 halts rather than moving a conflicting ref. |
| BASE-02 | RESOLVED | Seed, accepted action index/count, ordered hand tile codes, drawn/discarded tile code, river count, round-transition marker, and next-round first draw form the compact D-03 comparison contract; score and wall-correctness claims remain excluded per D-04. |
| BASE-03 | RESOLVED | Task 1 creates the Player compilation boundary, Task 2 builds/launches the tracer, and `01-03-T1` reruns the full gate before `01-03-T2` observes D-12/D-13 and `01-03-T3` seals evidence. |
| BASE-04 | RESOLVED | Unity exit zero, project fixture test-case count greater than zero, every project case Passed, XML present, and raw failure/error totals zero are all required. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Establish the immutable tag, predefined-Editor trace, and build boundary</name>
  <reversibility rating="costly">The annotated baseline becomes a portfolio comparison anchor; changing it later would invalidate evidence links, although it does not rewrite source history.</reversibility>
  <files>Assets/Editor/Tests/MahjongRoundTraceTests.cs, Assets/Editor/Phase1Build.cs, Assets/Scripts/AL-1S/MahjongTileAndBlock.cs, Assets/Scripts/UI-Kozeki/UiScoreDistanceInfo.cs</files>
  <read_first>
    - `AGENTS.md` project safety, C# 9, Allman, Korean XML summary, and runtime/Editor separation rules
    - `.planning/phases/01-executable-baseline/01-CONTEXT.md` D-01 through D-05 and D-10 through D-12
    - `.planning/phases/01-executable-baseline/01-PATTERNS.md` predefined `Assembly-CSharp-Editor`, trace, and build-entry assignments
    - `Assets/Scripts/AL-1S/MahjongRound.cs` public construction, draw/discard, and next-round events
    - `Assets/Editor/MahjongTileDataGenerator.cs` existing Editor-folder/global-namespace convention
    - `ProjectSettings/EditorBuildSettings.asset`, `ProjectSettings/ProjectVersion.txt`, and `.gitignore`
  </read_first>
  <behavior>
    - `SameSeedAndActions_ProduceIdenticalTrace`: two runs using seed `1557` and only `DiscardTile(13)` produce identical accepted-action/state summaries through `OnNewRoundStart` and the next round's first `OnTsumoTile`.
    - `ActionCap_ReportsFirstMismatch`: a fixed upper bound aborts a stalled trace and reports the first mismatch index with expected/actual compact state.
    - `TraceContract_ContainsOnlyDecisionFields`: the trace record contains only D-03 identity/action/hand/draw-discard/river/transition fields and no Phase 2 score or wall-correctness golden assertion per D-04.
  </behavior>
  <action>Inspect `refs/tags/portfolio-baseline`; create an annotated tag at `b18320e` only when absent, otherwise require annotated-object type and the exact peeled commit and halt rather than moving it (BASE-01). Before adding files and again at verification, enumerate every `Assets/**/*.asmdef` and `Assets/**/*.asmref`, compare path and blob hash with the `b18320e` tree, and halt on any addition, deletion, or modification. Put the three exactly named `MahjongRoundTraceTests` cases under `Assets/Editor/Tests` so they compile through predefined `Assembly-CSharp-Editor`; add no descriptor and perform no runtime assembly migration. Use seed `1557`, `DiscardTile(13)`, a bounded loop, and only the public round/event API per D-01/D-02/D-03/D-04/D-05. Emit a machine-readable success line containing `seed=1557`, `acceptedActions=<count>`, the compact state summary, and `PASS`; on failure include the first action index and expected/actual summaries. Create `Phase1Build.BuildWindowsPlayer()` under `Assets/Editor` with the single `SampleScene`, fixed `Builds/phase1/RiichiNya.exe`, StrictMode Windows x64 target, `Temp/phase1/build-report.txt`, and a thrown failure for every non-success report. Remove only the unused runtime editor imports from `MahjongTileAndBlock.cs` and `UiScoreDistanceInfo.cs`. Run the exact three-case trace discovery gate. Licensing IPC/activation failure or missing XML is `BLOCKED`/`FAIL`, never PASS. Follow the project C# and metadata conventions; do not add packages, generated project files, or an assembly descriptor.</action>
  <acceptance_criteria>
    - The annotated tag peels exactly to `b18320ec1d9d647900d2173049819bab6bd47175`.
    - Every current `Assets/**/*.asmdef` and `Assets/**/*.asmref` path/blob is identical to the repository baseline at `b18320e`; Phase 1 adds none and preserves any baseline descriptors.
    - The three exact trace cases are each discovered once and pass with zero run failures/errors; the success evidence includes seed 1557 and accepted-action count.
    - `Phase1Build` fixes the scene/output target and fails closed on unsuccessful BuildReport; runtime editor-namespace imports are absent.
  </acceptance_criteria>
  <verify>
    <automated>$peeled = git rev-parse 'refs/tags/portfolio-baseline^{}'; if ((git cat-file -t refs/tags/portfolio-baseline) -ne 'tag' -or $peeled -ne 'b18320ec1d9d647900d2173049819bab6bd47175') { exit 1 }; $baselineDescriptors = @{}; git ls-tree -r b18320e -- Assets | ForEach-Object { if ($_ -match '^\d+\s+blob\s+([0-9a-f]+)\t(.+\.(?:asmdef|asmref))$') { $baselineDescriptors[$matches[2]] = $matches[1] } }; $currentDescriptors = @(Get-ChildItem -LiteralPath Assets -Recurse -File | Where-Object { $_.Extension -in '.asmdef','.asmref' } | ForEach-Object { [IO.Path]::GetRelativePath($PWD.Path, $_.FullName).Replace('\','/') } | Sort-Object); if ($currentDescriptors.Count -ne $baselineDescriptors.Count) { exit 1 }; foreach ($path in $currentDescriptors) { if (-not $baselineDescriptors.ContainsKey($path) -or (git hash-object -- $path) -ne $baselineDescriptors[$path]) { exit 1 } }; $hits = rg -n --glob '*.cs' 'using UnityEditor(\.|;)' Assets/Scripts; if ($LASTEXITCODE -eq 0) { $hits; exit 1 }; if ($LASTEXITCODE -ne 1) { exit $LASTEXITCODE }; $buildSource = Get-Content -Raw 'Assets/Editor/Phase1Build.cs'; foreach ($token in @('Assets/Scenes/SampleScene.unity','Builds/phase1/RiichiNya.exe','Temp/phase1/build-report.txt','StandaloneWindows64','StrictMode')) { if ($buildSource -notmatch [regex]::Escape($token)) { exit 1 } }; New-Item -ItemType Directory -Force -Path 'Temp\phase1' | Out-Null; $unity = 'C:\Program Files\Unity\Hub\Editor\2022.3.29f1\Editor\Unity.exe'; &amp; $unity -batchmode -nographics -projectPath $PWD -runTests -testPlatform EditMode -testFilter MahjongRoundTraceTests -testResults "$PWD\Temp\phase1\trace.xml" -quit -logFile "$PWD\Temp\phase1\trace.log"; if ($LASTEXITCODE -ne 0 -or -not (Test-Path 'Temp\phase1\trace.xml')) { exit 1 }; [xml]$xml = Get-Content -Raw 'Temp\phase1\trace.xml'; $expected = @('SameSeedAndActions_ProduceIdenticalTrace','ActionCap_ReportsFirstMismatch','TraceContract_ContainsOnlyDecisionFields'); $cases = @($xml.SelectNodes("//test-case[contains(@fullname,'MahjongRoundTraceTests')]")); $run = $xml.SelectSingleNode('/test-run'); if ($cases.Count -ne 3 -or @($cases | Where-Object { $_.result -ne 'Passed' }).Count -ne 0 -or $null -eq $run -or [int]$run.failed -ne 0 -or ($run.HasAttribute('errors') -and [int]$run.errors -ne 0)) { exit 1 }; foreach ($name in $expected) { if (@($cases | Where-Object { $_.fullname -like "*.$name" }).Count -ne 1) { exit 1 } }; $traceEvidence = (Get-Content -Raw 'Temp\phase1\trace.xml') + "`n" + (Get-Content -Raw 'Temp\phase1\trace.log'); if ($traceEvidence -notmatch '(?is)seed\s*=\s*1557.{0,300}acceptedActions\s*=\s*\d+.{0,300}PASS') { exit 1 }</automated>
  </verify>
  <done>BASE-01/02/04 and the BASE-03 build boundary have an atomic tag/trace/tooling commit with exact descriptor preservation and no runtime gameplay edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the production forfeit/restart tracer and its first two lifecycle regressions</name>
  <files>Assets/Scripts/GameEndReason.cs, Assets/Scripts/MahjongGameManager.cs, Assets/Scripts/UI-Kozeki/PlayerHand.cs, Assets/Scripts/UI-Kozeki/GameUIManager.cs, Assets/Scripts/UI-Kozeki/UiGameOver.cs, Assets/Scenes/SampleScene.unity, Assets/Editor/Tests/SoloSessionLifecycleTests.cs</files>
  <read_first>
    - `AGENTS.md` C# 9, Allman, Korean XML summary, lifecycle grouping, and symmetric event rules
    - `.planning/phases/01-executable-baseline/01-CONTEXT.md` D-05 through D-09 and D-12/D-13
    - `.planning/phases/01-executable-baseline/01-PATTERNS.md` one session owner, overloaded Processing state, scene panel map, and result presenter assignments
    - `Assets/Scripts/MahjongGameManager.cs`, `Assets/Scripts/UI-Kozeki/PlayerHand.cs`, `Assets/Scripts/UI-Kozeki/GameUIManager.cs`, `Assets/Scripts/UI-Kozeki/UiManager.cs`, and `Assets/Scripts/UI-Kozeki/UiGameOver.cs`
    - `Assets/Scenes/SampleScene.unity` existing game canvas, GameOver object, manager references, panel entries, and serialized callbacks
    - `Assets/Editor/Phase1Build.cs` fixed build command contract from Task 1
  </read_first>
  <behavior>
    - `ConfirmForfeit_FinalizesOnceWithoutSavingHighScore`: confirmation finalizes once as Forfeit, emits one game-over event, presents current distance, and performs no high-score write per D-06.
    - `StartNewGame_Twice_DetachesAndResetsSession`: consecutive starts detach the prior round/timer callbacks, replace session state, and leave exactly one current handler set per D-07.
  </behavior>
  <action>Create `SoloSessionLifecycleTests` under the same predefined Editor folder with these two exact failing cases before production edits. Add `GameEndReason` with `TimeExpired` and `Forfeit`; keep fresh production randomness per D-05. `MahjongGameManager` remains the sole owner of start, confirmation, finalization, and persistence. Add a separate private pending-forfeit flag because `GameState.Processing` already represents round transition, discard, and tsumo work: a PlayerTurn Esc sets that flag and enters Processing, a later Esc cancels only when the flag is true, and Esc received during non-forfeit Processing is ignored. Confirm and timeout converge on one idempotent finalizer; Forfeit shows `포기` plus current distance and skips the save mutation. Make repeated StartNewGame detach prior round/timer handlers before replacement and reset the flag/finalization guard. Add exactly one initially inactive `ForfeitConfirmation` panel under the existing game canvas with one confirm and one cancel Button callback, plus one reason label on existing GameOver; reuse the existing result Back route and `OnGameStartButton -> StartNewGame`. Keep the timer running while the confirmation is pending. After the two cases pass, run the combined EditMode suite and require exactly 3 trace + 2 lifecycle cases, build through `Phase1Build`, launch the real Player visibly, and write `Temp/phase1/player.pid` only after a non-empty window is observed. Licensing/XML/report/exe/launch failure blocks Task 3 and cannot be recorded as PASS. Plans 01-02 and 01-03 extend this same fixture and production path; do not create a second lifecycle, modal, or state abstraction.</action>
  <acceptance_criteria>
    - Unity discovers exactly the two named lifecycle cases once, both pass, and the combined project count is exactly five (3 trace + 2 lifecycle).
    - The private pending-forfeit flag, not Processing alone, controls Esc cancellation; non-forfeit Processing ignores Esc.
    - The real scene provides start → Esc confirmation → Forfeit result → menu → second start with fresh randomness and no forfeit high-score write.
    - BuildReport is successful, the fixed exe exists, and `Temp/phase1/player.pid` names a live visible Player process before Task 3.
  </acceptance_criteria>
  <verify>
    <automated>New-Item -ItemType Directory -Force -Path 'Temp\phase1' | Out-Null; $unity = 'C:\Program Files\Unity\Hub\Editor\2022.3.29f1\Editor\Unity.exe'; &amp; $unity -batchmode -nographics -projectPath $PWD -runTests -testPlatform EditMode -testResults "$PWD\Temp\phase1\editmode-initial.xml" -quit -logFile "$PWD\Temp\phase1\editmode-initial.log"; if ($LASTEXITCODE -ne 0 -or -not (Test-Path 'Temp\phase1\editmode-initial.xml')) { exit 1 }; [xml]$xml = Get-Content -Raw 'Temp\phase1\editmode-initial.xml'; $traceExpected = @('SameSeedAndActions_ProduceIdenticalTrace','ActionCap_ReportsFirstMismatch','TraceContract_ContainsOnlyDecisionFields'); $lifeExpected = @('ConfirmForfeit_FinalizesOnceWithoutSavingHighScore','StartNewGame_Twice_DetachesAndResetsSession'); $traceCases = @($xml.SelectNodes("//test-case[contains(@fullname,'MahjongRoundTraceTests')]")); $lifeCases = @($xml.SelectNodes("//test-case[contains(@fullname,'SoloSessionLifecycleTests')]")); $cases = @($traceCases + $lifeCases); $run = $xml.SelectSingleNode('/test-run'); if ($traceCases.Count -ne 3 -or $lifeCases.Count -ne 2 -or $cases.Count -ne 5 -or @($cases | Where-Object { $_.result -ne 'Passed' }).Count -ne 0 -or $null -eq $run -or [int]$run.failed -ne 0 -or ($run.HasAttribute('errors') -and [int]$run.errors -ne 0)) { exit 1 }; foreach ($name in @($traceExpected + $lifeExpected)) { if (@($cases | Where-Object { $_.fullname -like "*.$name" }).Count -ne 1) { exit 1 } }; &amp; $unity -batchmode -nographics -projectPath $PWD -executeMethod Phase1Build.BuildWindowsPlayer -quit -logFile "$PWD\Temp\phase1\build.log"; if ($LASTEXITCODE -ne 0 -or -not (Test-Path 'Temp\phase1\build-report.txt') -or -not (Select-String -LiteralPath 'Temp\phase1\build-report.txt' -Pattern 'Succeeded' -Quiet) -or -not (Test-Path 'Builds\phase1\RiichiNya.exe')) { exit 1 }; $player = Start-Process -FilePath (Resolve-Path 'Builds\phase1\RiichiNya.exe') -PassThru; Start-Sleep -Seconds 3; $player.Refresh(); if ($player.HasExited -or [string]::IsNullOrWhiteSpace($player.MainWindowTitle)) { exit 1 }; Set-Content -LiteralPath 'Temp\phase1\player.pid' -Value $player.Id</automated>
    <human-check>Task 3 must observe the D-13 same-PID Player path; process/executable existence alone is not GUI PASS.</human-check>
  </verify>
  <done>The production tracer, two earliest lifecycle regressions, successful build, and live Player are ready for the blocking same-PID observation, or execution stops with truthful FAIL/BLOCKED evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Verify the tracer same-process Windows Player lifecycle</name>
  <what-built>The production walking skeleton at `Builds/phase1/RiichiNya.exe`: existing solo start → Esc confirmation → confirmed Forfeit result → existing Back route to MainMenu → existing start button again, without closing the Player.</what-built>
  <how-to-verify>
    1. Confirm the PID in `Temp/phase1/player.pid` remains alive and unchanged throughout.
    2. Use the existing solo start button and verify a fresh session appears with the timer running near 03:00.
    3. Press Esc and verify the confirmation appears while the visible timer continues decreasing and gameplay input is blocked.
    4. Confirm forfeit; verify the existing result screen shows `포기` and current distance, and the saved high score is unchanged.
    5. Use the existing result Back path to reach MainMenu, then use the same existing start button without closing the process.
    6. Verify the second session visibly starts fresh and one input causes only one visible reaction. Detailed cancel/race/subscription/reset cases are automated and rechecked by 01-02/01-03.
  </how-to-verify>
  <resume-signal>Reply `approved` only if every step is observed. Describe any mismatch or uncertainty so Task 3 records FAIL/BLOCKED/NOT OBSERVED; no inferred PASS is allowed.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Record and validate the factual tracer evidence ledger</name>
  <files>.planning/phases/01-executable-baseline/01-BASELINE.md</files>
  <read_first>
    - `AGENTS.md` project safety, encoding, and evidence-preservation rules
    - `.planning/phases/01-executable-baseline/01-CONTEXT.md` D-10 through D-12
    - `.planning/phases/01-executable-baseline/01-RESEARCH.md` evidence summary minimum and observed LicensingClient result
    - `.planning/codebase/TESTING.md` existing baseline test absence and command conventions
    - `.gitignore` raw output exclusions
  </read_first>
  <action>Create the only committed Phase 1 verification summary at `01-BASELINE.md` per D-10/D-11. Use one non-empty `- Label: value` line for each of `Baseline tag`, `Baseline commit`, `Target commit`, `Unity version`, `EditMode command`, `EditMode status`, `Test count`, `Seeded trace`, `Windows build command`, `BuildReport`, `BuildReport status`, `Player executable`, `Player PID`, `Licensing status`, `Human checkpoint`, `GUI status`, `Raw artifacts`, `Before`, and `After`; values come from live Git, `ProjectVersion.txt`, the combined five-case XML, trace output, BuildReport, executable/process checks, and Task 3's explicit response. Record `Test count: 5` only when the XML contains exactly three trace plus the two initial lifecycle cases. The seeded trace value must copy seed 1557 and the accepted-action count from the live trace output. `Before` must state all four observed baseline symptoms: no project-authored tests, runtime editor-import/Player-build risk, direct Esc-to-forfeit behavior, and restart duplicate/stale-state risk. Use only PASS/FAIL/BLOCKED/NOT OBSERVED for execution statuses and APPROVED/PENDING/REJECTED for the human checkpoint. First write Task 3's truthful result: an exact approval after every observed step becomes `Human checkpoint: APPROVED` plus `GUI status: PASS`; a pending response, rejection, mismatch, or uncertainty is recorded with its causally correct non-PASS status and the reported detail. After the record is durable, require APPROVED plus PASS for Task 4 and Plan 01 to succeed; any other pair exits nonzero and prevents 01-02 from starting. The repair route is to correct the reported lifecycle/build/environment cause, relaunch the Player through Task 2, repeat Task 3 on one PID, and rerun Task 4 so the prior failure remains visible until replaced by newly observed approval. A GUI PASS also requires successful XML and BuildReport, existing exe, summary PID equal to `Temp/phase1/player.pid`, that PID live, and non-blocked licensing. Do not copy expected values into observed result fields or commit raw outputs.</action>
  <acceptance_criteria>
    - Exactly one committed Markdown verification summary exists for Phase 1.
    - The summary includes every D-11 field and distinguishes source failures, environment blockers, and unobserved GUI behavior.
    - Task 4 and Plan 01 complete only when `Human checkpoint: APPROVED` and `GUI status: PASS`; PENDING, REJECTED, FAIL, BLOCKED, or NOT OBSERVED is recorded first and then blocks 01-02 until the repair/retry route passes.
    - XML/log/report/build paths point only under `Temp/phase1` and `Builds/phase1`; raw outputs are not tracked.
  </acceptance_criteria>
  <verify>
    <automated>$summary = '.planning/phases/01-executable-baseline/01-BASELINE.md'; $summaries = @(Get-ChildItem '.planning/phases/01-executable-baseline' -File -Filter '*-BASELINE.md'); if ($summaries.Count -ne 1 -or -not (Test-Path $summary)) { exit 1 }; $text = Get-Content -Raw $summary; $required = @('Baseline tag','Baseline commit','Target commit','Unity version','EditMode command','EditMode status','Test count','Seeded trace','Windows build command','BuildReport','BuildReport status','Player executable','Player PID','Licensing status','Human checkpoint','GUI status','Raw artifacts','Before','After'); $values = @{}; foreach ($field in $required) { $match = [regex]::Match($text, '(?m)^-\s*' + [regex]::Escape($field) + ':\s*(\S.*)$'); if (-not $match.Success -or $match.Groups[1].Value -match '^(TBD|TODO|N/A|<.*>|)$') { exit 1 }; $values[$field] = $match.Groups[1].Value.Trim() }; $baseline = git rev-parse 'refs/tags/portfolio-baseline^{}'; $target = git rev-parse HEAD; $unityVersion = ((Get-Content 'ProjectSettings/ProjectVersion.txt' | Select-Object -First 1) -replace '^m_EditorVersion:\s*','').Trim(); if ($values['Baseline tag'] -ne 'portfolio-baseline' -or $values['Baseline commit'] -ne $baseline -or $values['Target commit'] -ne $target -or $values['Unity version'] -ne $unityVersion) { exit 1 }; foreach ($symptom in @('no project-authored tests','runtime UnityEditor import','direct Esc forfeit','restart duplicate')) { if ($values['Before'] -notmatch [regex]::Escape($symptom)) { exit 1 } }; foreach ($statusField in @('EditMode status','BuildReport status','Licensing status','GUI status')) { if ($values[$statusField] -notmatch '^(PASS|FAIL|BLOCKED|NOT OBSERVED)$') { exit 1 } }; if ($values['Human checkpoint'] -notmatch '^(APPROVED|PENDING|REJECTED)$') { exit 1 }; $xmlOk = $false; if (Test-Path 'Temp\phase1\editmode-initial.xml') { [xml]$xml = Get-Content -Raw 'Temp\phase1\editmode-initial.xml'; $traceExpected = @('SameSeedAndActions_ProduceIdenticalTrace','ActionCap_ReportsFirstMismatch','TraceContract_ContainsOnlyDecisionFields'); $lifeExpected = @('ConfirmForfeit_FinalizesOnceWithoutSavingHighScore','StartNewGame_Twice_DetachesAndResetsSession'); $traceCases = @($xml.SelectNodes("//test-case[contains(@fullname,'MahjongRoundTraceTests')]")); $lifeCases = @($xml.SelectNodes("//test-case[contains(@fullname,'SoloSessionLifecycleTests')]")); $cases = @($traceCases + $lifeCases); $run = $xml.SelectSingleNode('/test-run'); $xmlOk = $traceCases.Count -eq 3 -and $lifeCases.Count -eq 2 -and $cases.Count -eq 5 -and @($cases | Where-Object { $_.result -ne 'Passed' }).Count -eq 0 -and $null -ne $run -and [int]$run.failed -eq 0 -and (-not $run.HasAttribute('errors') -or [int]$run.errors -eq 0); foreach ($name in @($traceExpected + $lifeExpected)) { $xmlOk = $xmlOk -and @($cases | Where-Object { $_.fullname -like "*.$name" }).Count -eq 1 } }; $traceEvidence = if ((Test-Path 'Temp\phase1\editmode-initial.xml') -and (Test-Path 'Temp\phase1\editmode-initial.log')) { (Get-Content -Raw 'Temp\phase1\editmode-initial.xml') + "`n" + (Get-Content -Raw 'Temp\phase1\editmode-initial.log') } else { '' }; $traceMatch = [regex]::Match($traceEvidence, '(?is)seed\s*=\s*1557.{0,300}acceptedActions\s*=\s*(\d+)'); $traceSummaryOk = $traceMatch.Success -and $values['Seeded trace'] -match 'seed\s*=\s*1557' -and $values['Seeded trace'] -match ('acceptedActions\s*=\s*' + $traceMatch.Groups[1].Value); $reportOk = (Test-Path 'Temp\phase1\build-report.txt') -and (Select-String -LiteralPath 'Temp\phase1\build-report.txt' -Pattern 'Succeeded' -Quiet); $exeOk = Test-Path 'Builds\phase1\RiichiNya.exe'; $pidFileOk = Test-Path 'Temp\phase1\player.pid'; $pidText = if ($pidFileOk) { (Get-Content -Raw 'Temp\phase1\player.pid').Trim() } else { '' }; $pidOk = $values['Player PID'] -match '^\d+$' -and $values['Player PID'] -eq $pidText -and $null -ne (Get-Process -Id ([int]$pidText) -ErrorAction SilentlyContinue); if ($values['EditMode status'] -eq 'PASS' -and (-not $xmlOk -or $values['Test count'] -ne '5' -or -not $traceSummaryOk)) { exit 1 }; if ($values['BuildReport status'] -eq 'PASS' -and -not $reportOk) { exit 1 }; if ($values['Licensing status'] -eq 'BLOCKED' -and ($values['EditMode status'] -eq 'PASS' -or $values['BuildReport status'] -eq 'PASS' -or $values['GUI status'] -eq 'PASS')) { exit 1 }; if ($values['GUI status'] -eq 'PASS' -and ($values['Human checkpoint'] -ne 'APPROVED' -or $values['Licensing status'] -ne 'PASS' -or -not $xmlOk -or -not $traceSummaryOk -or -not $reportOk -or -not $exeOk -or -not $pidOk)) { exit 1 }; $trackedRaw = git ls-files -- 'Temp/phase1/**' 'Builds/phase1/**'; if ($trackedRaw) { exit 1 }; if ($values['EditMode status'] -ne 'PASS' -or $values['BuildReport status'] -ne 'PASS' -or $values['Licensing status'] -ne 'PASS' -or $values['Human checkpoint'] -ne 'APPROVED' -or $values['GUI status'] -ne 'PASS') { exit 1 }</automated>
  </verify>
  <done>D-10/D-11 evidence has non-placeholder live-source fields and all four baseline symptoms; Task 3 is explicitly APPROVED with GUI PASS and executable/PID evidence, so Plan 01 may unlock 01-02. Any other observed result remains recorded but keeps the plan incomplete pending repair and retry.</done>
</task>

</tasks>

## Topology and commit boundaries

Scope-risk acceptance: the user reviewed and retained this tracer-first integration boundary and its four-task shape. The twelve-file plan union is high because the existing single-scene lifecycle crosses the existing round, manager, presenter, scene, build, test, and evidence owners; each task still has narrow ownership, reuses those owners, and introduces no new abstraction. Confidence remains low because no completed-phase actuals exist. A fourth plan would add another sequential dependency around the shared scene/manager/test fixture without enabling parallel work or reducing any task's actual ownership.

## Artifacts this phase produces

- `refs/tags/portfolio-baseline` annotated tag pointing exactly to `b18320ec1d9d647900d2173049819bab6bd47175`.
- `Assets/Editor/Tests/MahjongRoundTraceTests.cs` in predefined `Assembly-CSharp-Editor`, plus its Unity-generated `.meta` file; no Phase 1 descriptor additions and all `b18320e` descriptors preserved.
- `Phase1Build.BuildWindowsPlayer()` and its Unity-generated `.meta` file.
- `Temp/phase1/trace.xml`, `trace.log`, `editmode-initial.xml`, `editmode-initial.log`, `build.log`, and `build-report.txt` as ignored local outputs.
- `Builds/phase1/RiichiNya.exe` and Player data as ignored local output.
- `.planning/phases/01-executable-baseline/01-BASELINE.md` as the single committed evidence summary.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git working tree → portfolio tag | A local mutable ref must preserve the exact historical baseline commit. |
| Test code → synchronous round API | A bounded fixed action sequence drives mutable domain state. |
| Unity Editor → local filesystem | Batch test/build writes only to fixed ignored project-local paths. |

## STRIDE Threat Register — ASVS L1, block threshold high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | `portfolio-baseline` tag | high | mitigate | Verify annotated object type and exact peeled commit; halt instead of force-moving an existing ref. |
| T-01-02 | Denial of Service | seeded trace loop | medium | mitigate | Fixed action cap and first-mismatch failure prevent a stalled transition from hanging batchmode. |
| T-01-03 | Tampering | build/output paths | medium | mitigate | Compile fixed scene and write only under `Builds/phase1` and `Temp/phase1`; accept no external path input. |
| T-01-04 | Repudiation | evidence summary | medium | mitigate | Record exact command, commit, seed, checkpoint result, and raw artifact path before validation; only APPROVED plus GUI PASS may complete Plan 01, while every other result exits nonzero and preserves a repair/retry path. |
</threat_model>

<verification>
Tag type/target, Unity EditMode XML, runtime import scan, StrictMode Windows build, output existence, and Git ignored-output checks all run with exact commands. Licensing IPC failure halts as an environment gate.
</verification>

<success_criteria>
- BASE-01 through BASE-05 have a reachable tracer path with reproducible commands, initial lifecycle tests, and evidence.
- D-01 through D-13 are directly traceable across the tag/trace task, production slice, blocking checkpoint, and ledger.
- No new package, replay system, domain assembly migration, or Phase 2 score fixture is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-executable-baseline/01-01-SUMMARY.md` when done.
</output>
