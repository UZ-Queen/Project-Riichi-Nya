---
phase: 01-executable-baseline
plan: 06
type: execute
wave: 6
depends_on: ["01-05"]
files_modified:
  - Assets/Editor/Tests/MahjongRoundTraceTests.cs
  - Assets/Editor/Tests/SoloSessionLifecycleTests.cs
  - Assets/Scripts/SoloScoringGameManager.cs
  - Assets/Scripts/UI-Kozeki/PlayerHandController.cs
  - .planning/phases/01-executable-baseline/01-BASELINE.md
  - .planning/phases/01-executable-baseline/01-VALIDATION.md
autonomous: true
requirements: [BASE-02, BASE-04, BASE-05]
gap_closure: true
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-03/BASE-02: trace record counts may diverge without losing the first mismatch index or compact expected/actual state."
    - "D-06/BASE-05: a TimeExpired run that beats the stored record renders and persists the new record, while a Forfeit still performs no save mutation."
    - "D-07/D-19/D-21/BASE-05: the actual lobby/root restart resets both PlayerHandController.currentIndex and the real PlayerHandView highlight to tile 6."
    - "D-10/D-12/BASE-04: focused and aggregate Unity results are accepted only from GREEN helper JSON plus parseable XML with positive exact case counts; missing XML or zero discovery fails closed."
  artifacts:
    - path: "Assets/Editor/Tests/MahjongRoundTraceTests.cs"
      provides: "Length-divergence first-mismatch regression in the existing trace fixture"
      contains: "LengthDivergence_ReportsFirstMismatchState"
    - path: "Assets/Editor/Tests/SoloSessionLifecycleTests.cs"
      provides: "Value-level timeout-record regression and strengthened real-root restart regression"
      contains: "TimeoutNewRecord_RendersAndPersistsUpdatedHighScore"
    - path: "Assets/Scripts/SoloScoringGameManager.cs"
      provides: "Correct timeout record mutation, rendering, and persistence order"
      contains: "FinalizeGame"
    - path: "Assets/Scripts/UI-Kozeki/PlayerHandController.cs"
      provides: "New-session selected-index propagation to the existing PlayerHandView"
      contains: "HandleGameStart"
    - path: ".planning/phases/01-executable-baseline/01-BASELINE.md"
      provides: "Updated single tracked ledger for the 4 + 14 = 18 project-authored test aggregate"
      contains: "Test count: 4 + 14 = 18"
    - path: ".planning/phases/01-executable-baseline/01-VALIDATION.md"
      provides: "Updated Nyquist map and D-13 manual boundary for Plan 01-06"
      contains: "01-06"
  key_links:
    - from: "Assets/Editor/Tests/MahjongRoundTraceTests.cs"
      to: "FindFirstMismatch/BuildMismatchMessage"
      via: "a shorter synthetic TraceResult exercises the same diagnostic path used by SameSeedAndActions_ProduceIdenticalTrace"
      pattern: "FindFirstMismatch|BuildMismatchMessage|<missing>"
    - from: "Assets/Scripts/SoloScoringGameManager.cs"
      to: "Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs"
      via: "FinalizeGame applies a TimeExpired record before ShowGameOver and persists that same value"
      pattern: "TimeExpired|ShowGameOver|SettingsManager.Save"
    - from: "Assets/Scripts/UI-Kozeki/PlayerHandController.cs"
      to: "Assets/Scripts/UI-Kozeki/PlayerHandView.cs"
      via: "HandleGameStart resets currentIndex then calls the existing UpdateHand/UpdateSelectedIndex route"
      pattern: "HandleGameStart|UpdateHand|UpdateSelectedIndex"
---

## Phase Goal

**As a** solo-mode player and maintainer, **I want to** reproduce the existing solo flow and verify changes safely, **so that** I can tell whether established behavior has regressed.

<objective>
현재 검증에서 드러난 trace 진단, 시간 종료 최고기록 표시, 재시작 선택 강조의 세 결함을 기존 소유자와 기존 EditMode fixture 안에서 닫는다.

Purpose: Phase 1의 재현·종료·재시작 증거가 실제 오류를 놓치지 않게 한다.
Output: 한 개의 길이 불일치 trace 회귀, 한 개의 시간 종료 값 회귀, 실제 scene root를 사용하는 강화된 재시작 회귀, 두 개의 최소 production 수정, 갱신된 단일 증거 ledger.
</objective>

<execution_context>
@C:/Users/user/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/user/.codex/gsd-core/templates/summary.md
@C:/Users/user/.codex/skills/unity-test-gate/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-executable-baseline/01-CONTEXT.md
@.planning/phases/01-executable-baseline/01-RESEARCH.md
@.planning/phases/01-executable-baseline/01-PATTERNS.md
@.planning/phases/01-executable-baseline/01-VALIDATION.md
@.planning/phases/01-executable-baseline/01-VERIFICATION.md
@.planning/phases/01-executable-baseline/01-REVIEW.md
@.planning/phases/01-executable-baseline/01-05-SUMMARY.md
@Assets/Editor/Tests/MahjongRoundTraceTests.cs
@Assets/Editor/Tests/SoloSessionLifecycleTests.cs
@Assets/Scripts/SoloScoringGameManager.cs
@Assets/Scripts/UI-Kozeki/PlayerHandController.cs
@Assets/Scripts/UI-Kozeki/PlayerHandView.cs
@Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs

<interfaces>
Reuse `FindFirstMismatch`, `BuildMismatchMessage`, `SoloScoringGameManager.FinalizeGame`, `PlayerHandController.HandleGameStart`, `PlayerHandController.UpdateHand`, and `PlayerHandView.UpdateSelectedIndex`. Keep both fixtures under predefined `Assembly-CSharp-Editor`; add no asmdef, asmref, package, production interface, or public test seam.
</interfaces>
</context>

## Planning assumptions

- `BASE-01` through `BASE-05` are spec-less/unclassified in the probe fallback; this plan uses the live verification gaps and locked D-XX decisions as authority rather than inventing probe predicates.
- The existing 13 lifecycle cases remain in place. One new timeout case raises that fixture to 14; the existing `RestartAfterLobby_UsesFreshStateAndSingleHandlers` case is strengthened rather than duplicated.
- UI-SPEC is not required for this gap run because the UI safety gate classified these changes as regression fixes to established presentation behavior (`frontend: true`, `hasUiSpec: false`, `block: false`).
- Codebase drift is handled by the live files listed in `<read_first>`; stale legacy paths in earlier artifacts are not implementation targets.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Route trace length divergence through the promised first-mismatch diagnostic</name>
  <files>Assets/Editor/Tests/MahjongRoundTraceTests.cs</files>
  <read_first>
    - `Assets/Editor/Tests/MahjongRoundTraceTests.cs` — current count assertion, `FindFirstMismatch`, `BuildMismatchMessage`, `TraceResult`, and `TraceRecord`
    - `.planning/phases/01-executable-baseline/01-VERIFICATION.md` — first gap truth and exact missing diagnostic behavior
    - `.planning/phases/01-executable-baseline/01-CONTEXT.md` — D-01 through D-04 trace scope and output contract
    - `C:/Users/user/.codex/skills/unity-test-gate/SKILL.md` — XML-backed GREEN/BLOCKED/FAIL interpretation
  </read_first>
  <behavior>
    - `LengthDivergence_ReportsFirstMismatchState`: when expected has one additional record, `FindFirstMismatch` returns the common length and `BuildMismatchMessage` includes that action index, the existing expected record, and `actual=&lt;missing&gt;`.
    - `SameSeedAndActions_ProduceIdenticalTrace`: compares the mismatch result before any standalone count assertion, so both value and length divergence use the same D-03 diagnostic.
    - Existing seed 1557, bounded tsumogiri trace, and Phase 2 score/wall exclusions remain unchanged per D-01/D-02/D-04.
  </behavior>
  <action>Add the focused failing case first using the fixture's existing nested `TraceResult`/`TraceRecord` types and private helpers; do not run another real round merely to manufacture unequal list lengths. Then remove the early record-count assertion from `SameSeedAndActions_ProduceIdenticalTrace` and let `FindFirstMismatch` plus `BuildMismatchMessage` be the single equality gate. Keep the existing success log and action-cap case. This is the shortest root-cause repair for D-03: one shared diagnostic path covers record-value and record-count divergence without a new helper or test framework.</action>
  <verify>
    <automated>$gateJson = &amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter MahjongRoundTraceTests.LengthDivergence_ReportsFirstMismatchState -ExpectedGate Green; $gate = $gateJson | ConvertFrom-Json; if ($gate.status -ne 'GREEN' -or $gate.total -ne 1 -or -not (Test-Path -LiteralPath $gate.resultsPath)) { $gateJson; exit 1 }; [xml]$xml = Get-Content -Raw -LiteralPath $gate.resultsPath; $cases = @($xml.SelectNodes('/test-run//test-case')); if ($cases.Count -ne 1 -or $cases[0].name -ne 'LengthDivergence_ReportsFirstMismatchState' -or $cases[0].result -ne 'Passed') { $gateJson; exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - A list-length mismatch reaches `BuildMismatchMessage` and reports the first divergent index plus expected/actual compact state.
    - `SameSeedAndActions_ProduceIdenticalTrace` has no generic count assertion before its mismatch assertion.
    - The focused helper run is GREEN only with one discovered, passed XML case; missing XML or zero discovery fails.
  </acceptance_criteria>
  <done>D-03 and the trace-related BASE-02/BASE-04 gap are closed by one tested diagnostic path without changing runtime behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make timeout results and real-root restart presentation consistent</name>
  <files>Assets/Editor/Tests/SoloSessionLifecycleTests.cs, Assets/Scripts/SoloScoringGameManager.cs, Assets/Scripts/UI-Kozeki/PlayerHandController.cs</files>
  <read_first>
    - `Assets/Scripts/SoloScoringGameManager.cs` — `HandleTimerFinished` and `FinalizeGame` ordering, plus the existing Forfeit save policy
    - `Assets/Scripts/UI-Kozeki/PlayerHandController.cs` — `HandleGameStart`, `UpdateHand`, discard resets, and manager subscriptions
    - `Assets/Scripts/UI-Kozeki/PlayerHandView.cs` — `tilesInHand` and `UpdateSelectedIndex` presentation contract
    - `Assets/Scripts/UI-Kozeki/MahjongTileGameObject.cs` — real `isSelected` state changed by `SetSelected`
    - `Assets/Scripts/UI-Kozeki/UiManager.cs` — actual start and lobby-return routes
    - `Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs` and `Assets/Scripts/UI-Kozeki/UiGameOver.cs` — result/high-score rendering boundary
    - `Assets/Scripts/Configs/SettingsManager.cs` and `Assets/Scripts/ScoreManagerDistance.cs` — persisted record and final-distance value sources
    - `Assets/Editor/Tests/SoloSessionLifecycleTests.cs` — save restoration helpers, real-scene helpers, and the existing restart case
    - `.planning/phases/01-executable-baseline/01-CONTEXT.md` — D-06, D-07, D-18, D-19, D-21, and D-27
  </read_first>
  <behavior>
    - `TimeoutNewRecord_RendersAndPersistsUpdatedHighScore`: a TimeExpired run above the stored record sends the new value to `UiGameOver` and persists that identical value.
    - Existing `ConfirmForfeit_FinalizesOnceWithoutSavingHighScore` still proves byte-for-byte no-save behavior for Forfeit per D-06.
    - Strengthened `RestartAfterLobby_UsesFreshStateAndSingleHandlers`: the loaded `SampleScene` start route selects a non-default real hand tile, the actual lobby/root cycle runs, and the second start leaves only tile 6 selected while the existing fresh-state and handler-count assertions remain.
  </behavior>
  <action>Write the timeout and strengthened restart assertions before production edits. For the timeout case, reuse `CreateManager`, `UiGameOver`, TextMeshPro test objects, `PreserveSaveFile`/`RestoreSaveFile`, and the real `ScoreManagerDistance`; seed a lower saved record, earn a higher distance, invoke the production timer-finalization route, and assert both the rendered record text and `SettingsManager.Load().highScore` equal the final score. Keep the existing Forfeit byte comparison unchanged. In `FinalizeGame`, update `saveData.highScore` only for `TimeExpired` and only when the run beats it, then call `ShowGameOver`, then save for `TimeExpired`; never save for `Forfeit` per D-06. For restart, replace the synthetic-only body of `RestartAfterLobby_UsesFreshStateAndSingleHandlers` with the additive `SampleScene` path already used by fixture helpers: use the real `UiManager`, `SoloScoringModeRoot`, manager, controller, and view; start through the existing menu route, move selection to a non-default index through the controller, verify the real tile state changed, return through the existing lobby route, start again, and inspect the real view's thirteen `MahjongTileGameObject.isSelected` values. Preserve its timer, score, round replacement, modal, and exact handler-count assertions. Fix `HandleGameStart` by calling the existing `UpdateHand()` immediately after assigning index 6; do not change `PlayerHandView`, add a reset API, add a generalized input system, or add a scene abstraction.</action>
  <verify>
    <automated>$gateJson = &amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter SoloSessionLifecycleTests -ExpectedGate Green; $gate = $gateJson | ConvertFrom-Json; if ($gate.status -ne 'GREEN' -or $gate.total -ne 14 -or -not (Test-Path -LiteralPath $gate.resultsPath)) { $gateJson; exit 1 }; [xml]$xml = Get-Content -Raw -LiteralPath $gate.resultsPath; $cases = @($xml.SelectNodes('/test-run//test-case')); foreach ($name in @('TimeoutNewRecord_RendersAndPersistsUpdatedHighScore','RestartAfterLobby_UsesFreshStateAndSingleHandlers','ConfirmForfeit_FinalizesOnceWithoutSavingHighScore')) { if (@($cases | Where-Object { $_.name -eq $name -and $_.result -eq 'Passed' }).Count -ne 1) { $gateJson; exit 1 } }; if (@($cases | Where-Object { $_.result -ne 'Passed' }).Count -ne 0) { $gateJson; exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - A new TimeExpired record is the same value rendered and saved; a Forfeit still leaves the save file unchanged.
    - The real scene/root restart leaves `currentIndex == 6` and exactly one selected real hand tile at index 6.
    - The strengthened restart still proves fresh timer, score/distance, round, modal state, and one handler set.
    - The lifecycle helper is GREEN with exactly 14 discovered XML cases; missing XML, zero discovery, RED, BLOCKED, or FAIL does not pass.
  </acceptance_criteria>
  <done>D-06/D-07/D-19/D-21 and BASE-05 are closed at their existing manager/controller/view seams with value-level and real-root regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the aggregate gates and refresh the single evidence ledger</name>
  <files>.planning/phases/01-executable-baseline/01-BASELINE.md, .planning/phases/01-executable-baseline/01-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/01-executable-baseline/01-BASELINE.md` — existing D-10/D-11 live evidence fields and raw artifact paths
    - `.planning/phases/01-executable-baseline/01-VALIDATION.md` — separate filtered fixture policy and fail-closed classification
    - `Assets/Editor/Tests/MahjongRoundTraceTests.cs` and `Assets/Editor/Tests/SoloSessionLifecycleTests.cs` — final exact case names and counts
    - `C:/Users/user/.codex/skills/unity-test-gate/SKILL.md` — official Windows Unity gate and XML interpretation
  </read_first>
  <action>Run the trace and lifecycle fixtures separately through the existing helper, require GREEN JSON, existing parseable XML, exactly four trace and fourteen lifecycle cases, unique names, and zero non-passed cases, and record the new project-authored aggregate as `4 + 14 = 18`. Refresh only the test command/result/count and gap-closure notes in the existing single `01-BASELINE.md`; preserve the baseline tag, prior Windows build, prior approved GUI observation, raw-output policy, and before/after ownership history. Update `01-VALIDATION.md` from 3+13 to 4+14, add a Plan 01-06 verification-map row for the three regressions, and state that D-13 manual verification still covers forfeit/menu/restart plus the visible selection reset—not an actual 180-second timeout. Record new raw results under the helper's ignored output directory and do not commit them. Do not claim that this automated rerun itself re-observed Windows Player GUI behavior; the end-of-phase verifier must repeat only the D-13 same-PID forfeit/menu/restart path with a non-default selection before changing the phase verdict to PASS. The timeout record ordering remains an XML-backed value regression and does not expand D-13.</action>
  <verify>
    <automated>$traceJson = &amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter MahjongRoundTraceTests -ExpectedGate Green; $trace = $traceJson | ConvertFrom-Json; $lifeJson = &amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter SoloSessionLifecycleTests -ExpectedGate Green; $life = $lifeJson | ConvertFrom-Json; if ($trace.status -ne 'GREEN' -or $trace.total -ne 4 -or $life.status -ne 'GREEN' -or $life.total -ne 14) { $traceJson; $lifeJson; exit 1 }; foreach ($gate in @($trace,$life)) { if (-not (Test-Path -LiteralPath $gate.resultsPath)) { exit 1 }; [xml]$xml = Get-Content -Raw -LiteralPath $gate.resultsPath; $cases = @($xml.SelectNodes('/test-run//test-case')); if ($cases.Count -ne $gate.total -or @($cases | Where-Object { $_.result -ne 'Passed' }).Count -ne 0 -or @($cases | Group-Object name | Where-Object Count -ne 1).Count -ne 0) { exit 1 } }; $ledger = Get-Content -Raw '.planning/phases/01-executable-baseline/01-BASELINE.md'; foreach ($required in @('Test count: 4 + 14 = 18','LengthDivergence_ReportsFirstMismatchState','TimeoutNewRecord_RendersAndPersistsUpdatedHighScore','RestartAfterLobby_UsesFreshStateAndSingleHandlers')) { if ($ledger -notmatch [regex]::Escape($required)) { exit 1 } }; $validation = Get-Content -Raw '.planning/phases/01-executable-baseline/01-VALIDATION.md'; foreach ($required in @('01-06','4 + 14 = 18','TimeoutNewRecord_RendersAndPersistsUpdatedHighScore','RestartAfterLobby_UsesFreshStateAndSingleHandlers')) { if ($validation -notmatch [regex]::Escape($required)) { exit 1 } }; if (git ls-files -- 'Logs/UnityTestGate/**' 'Temp/phase1/**' 'Builds/phase1/**') { exit 1 }</automated>
    <human-check>End-of-phase verification must launch a fresh Windows Player and repeat the established D-13 same-PID forfeit → menu → restart path after selecting a non-default tile, then confirm only tile 6 is highlighted. Actual 180-second timeout is excluded by D-13; absence or uncertainty on the required path remains NOT OBSERVED/BLOCKED, never PASS.</human-check>
  </verify>
  <acceptance_criteria>
    - Separate XML-backed GREEN runs prove exactly 4 trace + 14 lifecycle = 18 project-authored cases.
    - The one tracked baseline ledger names the three closure regressions and the new aggregate without implying fresh GUI observation.
    - `01-VALIDATION.md` records Plan 01-06, the 4+14 aggregate, and the unchanged D-13 manual scope.
    - Raw helper/build/Player outputs remain untracked.
  </acceptance_criteria>
  <done>D-10/D-11/D-12 evidence is current and fail-closed; automated closure is ready for the end-of-phase same-PID human check.</done>
</task>

</tasks>

## Artifacts this phase produces

- No new production file, public API, package, assembly descriptor, prefab, scene, or generalized abstraction.
- One new Editor-only test method: `MahjongRoundTraceTests.LengthDivergence_ReportsFirstMismatchState`.
- One new Editor-only test method: `SoloSessionLifecycleTests.TimeoutNewRecord_RendersAndPersistsUpdatedHighScore`.
- One strengthened existing test method: `SoloSessionLifecycleTests.RestartAfterLobby_UsesFreshStateAndSingleHandlers`, now using the real `SampleScene` root/controller/view path.
- Updated ignored Unity Test Gate XML/log artifacts and one updated tracked `.planning/phases/01-executable-baseline/01-BASELINE.md` ledger.
- Updated `.planning/phases/01-executable-baseline/01-VALIDATION.md` counts, verification map, and D-13 manual boundary; actual 180-second timeout remains outside Phase 1 GUI completion scope.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Trace comparison → diagnostic evidence | Unequal list lengths must not bypass the compact evidence path. |
| Local save/score service → result UI | Mutable local data crosses from persistence and session score into player-visible result text. |
| Controller session reset → serialized View | A fresh controller index must propagate to the existing real tile presentation objects. |
| Unity helper JSON/XML → tracked ledger | Process output is untrusted until exact positive XML discovery and pass counts agree. |

## STRIDE Threat Register — ASVS L1, block threshold high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-34 | Repudiation | trace diagnostic | medium | mitigate | One focused length-divergence case exercises the same mismatch formatter as the production trace equality assertion. |
| T-01-35 | Tampering | `FinalizeGame` record value | medium | mitigate | Apply the TimeExpired record before rendering, save that same object afterward, and retain the existing byte-level Forfeit no-save test. |
| T-01-36 | Tampering | restart selection presentation | medium | mitigate | Drive the real scene root/controller/view lifecycle and assert exactly one selected tile at the default index. |
| T-01-37 | Repudiation | Unity test evidence | high | mitigate | Require helper GREEN plus existing parseable XML, exact positive counts/names, no non-passed case, and keep missing XML/no tests fail-closed. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reproduce the solo flow and detect established regressions safely | 01-01→01-06 | COVERED | 01-06 closes the three verified evidence/behavior gaps. |
| REQ | BASE-01 | Exact annotated baseline tag | 01-01, 01-05 | COVERED | Already satisfied; 01-06 does not touch the tag. |
| REQ | BASE-02 | Fixed-seed representative trace | 01-01, 01-05, 01-06 | COVERED | 01-06 repairs its length-divergence diagnostic edge. |
| REQ | BASE-03 | Windows Player without runtime UnityEditor imports | 01-01, 01-05 | COVERED | Existing gate preserved; no runtime Editor import is introduced. |
| REQ | BASE-04 | Batch EditMode XML results | 01-01→01-06 | COVERED | 01-06 requires exact positive XML-backed discovery. |
| REQ | BASE-05 | Same-process start/end-or-forfeit/restart | 01-01→01-06 | COVERED | 01-06 fixes timeout result data and real View reset. |
| RESEARCH | — | Public-API fixed-seed trace with bounded diagnostics | 01-01, 01-05, 01-06 | COVERED | No runtime replay or scoring golden data. |
| RESEARCH | — | Predefined Assembly-CSharp-Editor tests; no descriptor | 01-01→01-06 | COVERED | Existing test location only. |
| RESEARCH | — | Idempotent one-owner session lifecycle | 01-04→01-06 | COVERED | Fixes stay in manager/controller owners. |
| RESEARCH | — | Fixed local evidence paths and truthful status | 01-01, 01-05, 01-06 | COVERED | Exact XML gate and one ledger remain. |
| CONTEXT | D-01 | Test-only fixed-seed reproduction | 01-01, 01-05, 01-06 | COVERED | |
| CONTEXT | D-02 | Tsumogiri through next-round first draw | 01-01, 01-05 | COVERED | Unchanged. |
| CONTEXT | D-03 | Compact PASS/first-mismatch output | 01-01, 01-05, 01-06 | COVERED | Length divergence now uses the formatter. |
| CONTEXT | D-04 | No Phase 2 scoring claim | 01-01, 01-05, 01-06 | COVERED | Test-only synthetic length divergence. |
| CONTEXT | D-05 | Fresh production randomness | 01-01, 01-05 | COVERED | Unchanged. |
| CONTEXT | D-06 | Forfeit result/current distance/no high-score update | 01-01, 01-05, 01-06 | COVERED | No-save preserved; timeout order corrected. |
| CONTEXT | D-07 | Clean menu return and restart | 01-01, 01-04→01-06 | COVERED | Real View selection becomes part of restart proof. |
| CONTEXT | D-08 | Esc modal blocks gameplay while timer runs | 01-01→01-05 | COVERED | Existing cases preserved. |
| CONTEXT | D-09 | Cancel/second Esc and timeout priority | 01-01, 01-03, 01-05 | COVERED | Existing cases preserved. |
| CONTEXT | D-10 | One tracked Markdown; raw local outputs | 01-01, 01-05, 01-06 | COVERED | Existing ledger refreshed, not duplicated. |
| CONTEXT | D-11 | Complete before/after evidence fields | 01-01, 01-05, 01-06 | COVERED | Aggregate updated to 18. |
| CONTEXT | D-12 | Codex-run verification; uncertainty is not PASS | 01-01, 01-05, 01-06 | COVERED | Missing XML/no tests and absent GUI remain non-PASS. |
| CONTEXT | D-13 | Exact same-process Player sequence | 01-01, 01-05, 01-06 | COVERED | End-of-phase human check retained. |
| CONTEXT | D-14 | SoloScoringGameManager ownership/name | 01-04, 01-06 | COVERED | Record-order fix stays in this owner. |
| CONTEXT | D-15 | SoloScoringUIController output ownership/name | 01-03, 01-06 | COVERED | Existing ShowGameOver boundary reused. |
| CONTEXT | D-16 | Player→GameManager→UI direct flow | 01-02→01-06 | COVERED | No new intermediary. |
| CONTEXT | D-17 | No generalized mode controllers | 01-02→01-06 | COVERED | No abstraction added. |
| CONTEXT | D-18 | Single scene, inactive selected mode root | 01-04→01-06 | COVERED | Existing real root is the regression target. |
| CONTEXT | D-19 | Component/session lifecycle separation | 01-04→01-06 | COVERED | Start event now propagates selection state. |
| CONTEXT | D-20 | Separate ForfeitRequested intent | 01-02, 01-05 | COVERED | Existing route unchanged. |
| CONTEXT | D-21 | PlayerHandController/View split only | 01-02, 01-06 | COVERED | Controller uses existing View method. |
| CONTEXT | D-22 | Esc-first immediate return/input block | 01-02, 01-05 | COVERED | Existing regression preserved. |
| CONTEXT | D-23 | Manager policy, timer continues, timeout wins | 01-03→01-06 | COVERED | Timeout presentation order corrected in manager. |
| CONTEXT | D-24 | Existing EventSystem/navigation, Cancel default | 01-03, 01-05 | COVERED | Unchanged. |
| CONTEXT | D-25 | Scene-local overlay outside panel map | 01-03, 01-05 | COVERED | Unchanged. |
| CONTEXT | D-26 | Synchronous block before animation | 01-03, 01-05 | COVERED | Unchanged. |
| CONTEXT | D-27 | Working slice → isolated refactor → final verification | 01-01→01-06 | COVERED | Gap closure follows verified implementation. |
| CONTEXT | D-28 | Preserve completed commits/artifacts | 01-01→01-06 | COVERED | Plans 01-01 through 01-05 and summaries remain untouched. |

Deferred ideas and Phase 2/3 rule/UI work remain excluded rather than planned.

<verification>
- Task 1: focused 1-case XML-backed trace diagnostic gate.
- Task 2: full 14-case XML-backed lifecycle gate with the timeout, real restart, and forfeit-policy cases each present exactly once.
- Task 3: separate full fixture gates proving 4 trace + 14 lifecycle = 18, updated single ledger, and no tracked raw output.
- End-of-phase: a fresh same-PID D-13 forfeit/menu/restart observation is still required for the visible selection reset before Phase 1 may be marked PASS; timeout record ordering is proven by the automated value regression.
</verification>

<success_criteria>
- All three gaps in `01-VERIFICATION.md` have a concrete production/test closure path.
- The timeout new record is rendered and persisted consistently while Forfeit remains no-save.
- A real scene-root restart selects only tile 6 and retains prior fresh-state/handler guarantees.
- Trace length divergence emits D-03 first-mismatch evidence.
- No new package, descriptor, production abstraction, scene, prefab, generalized input system, or deferred feature is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-executable-baseline/01-06-SUMMARY.md` when done.
</output>
