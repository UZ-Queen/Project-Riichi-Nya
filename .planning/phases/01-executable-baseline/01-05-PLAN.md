---
phase: 01-executable-baseline
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - Assets/Editor/Tests/SoloSessionLifecycleTests.cs
  - .planning/phases/01-executable-baseline/01-BASELINE.md
autonomous: false
requirements: [BASE-01, BASE-02, BASE-03, BASE-04, BASE-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01~D-05/BASE-01~04: the preserved tag and seed-1557 trace still pass without new assembly descriptors, runtime UnityEditor imports, or score-correctness claims."
    - "D-06~D-09/D-20~D-26/BASE-05: thirteen lifecycle cases prove one-shot forfeit, no high-score save, timer-continuing modal, timeout priority, same-frame input blocking, native Cancel selection, root symmetry, and clean restart."
    - "D-10~D-13: the final Windows Player is built and launched without `-quit`, then the exact same PID completes the fail-closed GUI sequence before PASS is recorded."
    - "D-14~D-19/D-27/D-28: final evidence names the refactored owners and proves the behavior-preserving sequence after the completed 01-01 boundary."
  artifacts:
    - path: "Assets/Editor/Tests/SoloSessionLifecycleTests.cs"
      provides: "Thirteen exact post-refactor lifecycle regressions"
      contains: "ForfeitRequested_IsSeparateAndReturnsBeforeDiscard"
    - path: ".planning/phases/01-executable-baseline/01-BASELINE.md"
      provides: "Single tracked before/after ledger backed by live XML/build/PID/checkpoint evidence"
      contains: "GUI status"
  key_links:
    - from: "Assets/Editor/Tests/SoloSessionLifecycleTests.cs"
      to: "Logs/UnityTestGate"
      via: "global unity-test-gate XML-backed GREEN run"
      pattern: "SoloSessionLifecycleTests"
    - from: ".planning/phases/01-executable-baseline/01-BASELINE.md"
      to: "Builds/phase1/RiichiNya.exe"
      via: "live BuildReport, executable, PID, and human checkpoint fields"
      pattern: "BuildReport|Player PID|Human checkpoint|GUI status"
---

## Phase Goal

**As a** 플레이어와 개발자, **I want to** 기존 솔로 경험을 반복 재현하고, **so that** 안전하게 변경 여부를 판별할 수 있다.

<objective>
D-27의 구조 변경 이후 회귀 범위를 확장하고, 최종 Windows Player에서 같은 프로세스 포기·메뉴·재시작을 다시 확인해 Phase 1 증거를 봉인한다.

Purpose: 자동 검증과 실제 GUI 관찰을 분리해 새 소유권 구조가 01-01의 동작을 보존했음을 거짓 양성 없이 증명한다.
Output: 3 trace + 13 lifecycle GREEN XML, successful Windows x64 BuildReport/exe, live PID, approved same-process GUI result, refreshed 01-BASELINE.md.
</objective>

<execution_context>
@C:/Users/user/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-executable-baseline/01-CONTEXT.md
@.planning/phases/01-executable-baseline/01-01-SUMMARY.md
@.planning/phases/01-executable-baseline/01-04-SUMMARY.md
@.planning/phases/01-executable-baseline/01-BASELINE.md
@C:/Users/user/.codex/skills/unity-test-gate/SKILL.md
@Assets/Editor/Tests/MahjongRoundTraceTests.cs
@Assets/Editor/Tests/SoloSessionLifecycleTests.cs
@Assets/Editor/Phase1Build.cs
@Assets/Scenes/SampleScene.unity

<interfaces>
The global helper is the only Test Runner entry: `Invoke-UnityTests.ps1 -ProjectPath $PWD -TestPlatform EditMode -ExpectedGate Green`. It runs without `-quit`, emits a compressed JSON status, and writes project-local XML/logs below `Logs/UnityTestGate`. Build separately through `Phase1Build.BuildWindowsPlayer`, also without `-quit`; the method exits explicitly after flushing the durable report.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand lifecycle regression coverage and launch the final Player</name>
  <precondition>A licensed Unity 2022.3.29f1 host can run the global unity-test-gate helper; if it returns BLOCKED for login/activation, keep Unity Hub visible and pause for the user to authenticate without requesting or handling credentials.</precondition>
  <files>Assets/Editor/Tests/SoloSessionLifecycleTests.cs, .planning/phases/01-executable-baseline/01-BASELINE.md</files>
  <behavior>
    - Preserve `ConfirmForfeit_FinalizesOnceWithoutSavingHighScore` and `StartNewGame_Twice_DetachesAndResetsSession`.
    - Add `ForfeitRequested_IsSeparateAndReturnsBeforeDiscard`, `OpenConfirmation_SynchronouslyBlocksGameplay`, `SecondEscape_CancelsAndRestoresGameplay`, `Confirmation_DoesNotPauseTimer`, and `TimeoutDuringConfirmation_WinsAndRejectsLateActions`.
    - Add `PlayerController_Subscriptions_AreSymmetricAcrossRootCycles`, `UiController_Subscriptions_AreSymmetricAcrossRootCycles`, `ForfeitOverlay_IsOutsidePanelMapAndSelectsCancel`, `GameOver_Forfeit_RendersReasonAndDistance`, `ReturnToLobby_DisablesSoloModeRoot`, and `RestartAfterLobby_UsesFreshStateAndSingleHandlers`.
  </behavior>
  <action>Extend the existing fixture to exactly the thirteen named lifecycle cases above. Exercise production methods and Editor scene serialization; do not duplicate the implementation in tests. The same-frame test must invoke the controller's actual Escape branch and prove neither discard nor mahjong-call intent fires afterward. Scene tests must load `SampleScene.unity`, prove one inactive `SoloScoringModeRoot`, one EventSystem outside it, one scene-local overlay absent from `GameUIState`, preserved script GUIDs, and Cancel default navigation. Run the unfiltered project gate through the global helper and require JSON status GREEN plus exact discovery of 3 trace and 13 lifecycle cases from its XML; do not use `-quit` or treat compilation/fixture/XML/license errors as RED/GREEN. Revalidate the annotated tag target, descriptor path/blob equality with `b18320e`, and absence of runtime editor imports. Run `Phase1Build.BuildWindowsPlayer` without `-quit`, require Succeeded report and executable, visibly launch it, write the live PID under `Temp/phase1/player.pid`, and set the ledger checkpoint fields to PENDING/NOT OBSERVED before Task 2. Preserve raw XML/logs under `Logs/UnityTestGate` and build outputs locally without committing them.</action>
  <verify>
    <automated>$gateJson = &amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -ExpectedGate Green; $gate = $gateJson | ConvertFrom-Json; $expected = @('SameSeedAndActions_ProduceIdenticalTrace','ActionCap_ReportsFirstMismatch','TraceContract_ContainsOnlyDecisionFields','ConfirmForfeit_FinalizesOnceWithoutSavingHighScore','StartNewGame_Twice_DetachesAndResetsSession','ForfeitRequested_IsSeparateAndReturnsBeforeDiscard','OpenConfirmation_SynchronouslyBlocksGameplay','SecondEscape_CancelsAndRestoresGameplay','Confirmation_DoesNotPauseTimer','TimeoutDuringConfirmation_WinsAndRejectsLateActions','PlayerController_Subscriptions_AreSymmetricAcrossRootCycles','UiController_Subscriptions_AreSymmetricAcrossRootCycles','ForfeitOverlay_IsOutsidePanelMapAndSelectsCancel','GameOver_Forfeit_RendersReasonAndDistance','ReturnToLobby_DisablesSoloModeRoot','RestartAfterLobby_UsesFreshStateAndSingleHandlers') | Sort-Object; $cases = @(([xml](Get-Content -Raw -LiteralPath $gate.resultsPath)).SelectNodes('/test-run//test-case')); $actual = @($cases | ForEach-Object { $_.name } | Sort-Object); if ($gate.status -ne 'GREEN' -or $gate.total -ne 16 -or $cases.Count -ne 16 -or ($actual | Group-Object | Where-Object Count -ne 1) -or (Compare-Object $expected $actual)) { $gateJson; exit 1 }; $unity = 'C:\Program Files\Unity\Hub\Editor\2022.3.29f1\Editor\Unity.exe'; &amp; $unity -batchmode -nographics -projectPath $PWD -executeMethod Phase1Build.BuildWindowsPlayer -logFile "$PWD\Temp\phase1\build.log"; if ($LASTEXITCODE -ne 0 -or -not (Test-Path 'Builds\phase1\RiichiNya.exe') -or -not (Select-String -LiteralPath 'Temp\phase1\build-report.txt' -Pattern 'Succeeded' -Quiet)) { exit 1 }; $exe = (Resolve-Path 'Builds\phase1\RiichiNya.exe').Path; $player = Start-Process -FilePath $exe -WorkingDirectory (Split-Path $exe) -PassThru; for ($i = 0; $i -lt 20; $i++) { Start-Sleep -Milliseconds 500; $player.Refresh(); if ($player.HasExited) { exit 1 }; if ($player.MainWindowHandle -ne 0 -and -not [string]::IsNullOrWhiteSpace($player.MainWindowTitle)) { break } }; $player.Refresh(); if ($player.HasExited -or $player.MainWindowHandle -eq 0 -or [string]::IsNullOrWhiteSpace($player.MainWindowTitle)) { exit 1 }; New-Item -ItemType Directory -Force 'Temp\phase1' | Out-Null; Set-Content -LiteralPath 'Temp\phase1\player.pid' -Value $player.Id -NoNewline; $recordedPid = [int](Get-Content -Raw -LiteralPath 'Temp\phase1\player.pid'); $live = Get-Process -Id $recordedPid -ErrorAction Stop; if ($live.HasExited -or $live.MainWindowHandle -eq 0) { exit 1 }</automated>
  </verify>
  <done>Exact 3+13 project cases are XML-backed GREEN, the fixed Windows build succeeds and launches visibly, and the ledger remains explicitly pending human observation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify the refactored same-process Windows Player lifecycle</name>
  <what-built>The post-refactor Windows Player at `Builds/phase1/RiichiNya.exe`, launched after the exact GREEN test/build gate.</what-built>
  <how-to-verify>
    1. Confirm the PID in `Temp/phase1/player.pid` remains unchanged for the whole check.
    2. At MainMenu, confirm the solo gameplay root/panels are not visible; start solo through the existing menu button.
    3. Confirm the hand, score/distance, round, and 180-second timer appear and the timer decreases.
    4. Press Esc; confirm the Korean forfeit overlay appears over the unchanged game screen, Cancel/No is selected, gameplay navigation/discard is blocked, and the timer keeps decreasing.
    5. Cancel using keyboard submit, confirm gameplay input returns, open again, and press Esc a second time to confirm the same cancel behavior.
    6. Open again and choose Confirm/Yes; confirm the result reason is `포기`, current distance is visible, and no duplicate transition occurs.
    7. Return to MainMenu through the existing result route, then start solo again without closing the process.
    8. Confirm the second run has fresh timer/score/distance/round/hand/river/UI state and one response per input, with no stale result/overlay or duplicated event.
  </how-to-verify>
  <resume-signal>Reply `approved` only if all eight observations match. Otherwise report the first mismatch or uncertainty; it must be recorded as non-PASS before repair/retry.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the observed result and seal the baseline ledger</name>
  <files>.planning/phases/01-executable-baseline/01-BASELINE.md</files>
  <action>After Task 2 resumes, write its actual response to the single tracked ledger. Record `Human checkpoint: APPROVED` and `GUI status: PASS` only for an exact approval after all eight same-PID steps; otherwise record REJECTED and the causally correct FAIL, BLOCKED, or NOT OBSERVED plus the first mismatch, then exit nonzero. Refresh the baseline tag/commit, target commit, Unity version, exact test command and 16-case count, live seed-1557 accepted-action summary, build command/report/exe, PID, licensing status, raw paths, and before/after ownership names from live sources. Recheck that raw XML/log/build outputs are untracked and the unrelated working-tree modifications to `.planning/config.json`, package files, and `ProjectSettings.asset` are absent from this plan commit. Artifact existence never substitutes for GUI approval; a non-PASS result follows repair → rerun Task 1 → repeat Task 2 on one PID → rerun Task 3.</action>
  <verify>
    <automated>$text = Get-Content -Raw '.planning/phases/01-executable-baseline/01-BASELINE.md'; foreach ($required in @('portfolio-baseline','b18320ec1d9d647900d2173049819bab6bd47175','Test count: 16','seed=1557','BuildReport status: PASS','Licensing status: PASS','Human checkpoint: APPROVED','GUI status: PASS','SoloScoringGameManager','SoloScoringUIController','PlayerHandController','PlayerHandView')) { if ($text -notmatch [regex]::Escape($required)) { exit 1 } }; $trackedRaw = git ls-files -- 'Logs/UnityTestGate/**' 'Temp/phase1/**' 'Builds/phase1/**'; if ($trackedRaw) { exit 1 }</automated>
  </verify>
  <done>D-10~D-13 evidence is live-derived and fail-closed, and Phase 1 completes only with exact automated GREEN/build success plus APPROVED same-PID GUI PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Unity process/XML → automated verdict | Exit codes and logs alone cannot establish selected-test success. |
| Build artifact → visible Player | A successful build does not prove launch or user interaction. |
| Human observation → tracked ledger | Expected values must not be copied into observed PASS fields before approval. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-30 | Repudiation | Test evidence | high | mitigate | Use unity-test-gate status with parseable XML, positive count, exact 3+13 names, and project-local raw artifacts. |
| T-01-31 | Spoofing | Player process | medium | mitigate | Record launched executable PID and require the same live PID throughout all GUI steps. |
| T-01-32 | Repudiation | GUI evidence | high | mitigate | Keep PENDING/NOT OBSERVED until blocking human approval; record mismatch before failing and retrying. |
| T-01-33 | Tampering | User-owned dirty files | medium | mitigate | Limit commits to the fixture/ledger and explicitly exclude the four pre-existing unrelated modifications. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Repeat the existing solo experience and safely judge changes | 01-01→01-05 | COVERED | Trace, isolated ownership refactor, regression, build, and GUI form one reachable path. |
| REQ | BASE-01 | Exact annotated baseline tag | 01-01, 01-05 | COVERED | Created once, terminally revalidated. |
| REQ | BASE-02 | Fixed seed/action representative trace | 01-01, 01-05 | COVERED | Preserved 3-case seed-1557 trace. |
| REQ | BASE-03 | Windows Player without runtime UnityEditor refs | 01-01, 01-05 | COVERED | Final rebuild and launch follow refactor. |
| REQ | BASE-04 | Batch EditMode XML results | 01-01→01-05 | COVERED | Global helper supplies fail-closed XML verdicts. |
| REQ | BASE-05 | Start/end-or-forfeit/restart in one process | 01-01, 01-02→01-05 | COVERED | Existing slice, refactor gates, regression, final GUI. |
| RESEARCH | — | Fixed-seed public-API trace with bounded diagnostics | 01-01, 01-05 | COVERED | No score/wall golden claim. |
| RESEARCH | — | Predefined Assembly-CSharp-Editor tests; no asmdef/asmref | 01-01, 01-05 | COVERED | Descriptor equality is rechecked. |
| RESEARCH | — | Idempotent one-owner session lifecycle | 01-04, 01-05 | COVERED | Detach-before-replace and race tests. |
| RESEARCH | — | Fixed local build/evidence paths and truthful failure states | 01-01, 01-05 | COVERED | No packages or external paths. |
| CONTEXT | D-01 | Test-only fixed-seed reproduction | 01-01, 01-05 | COVERED | |
| CONTEXT | D-02 | Tsumogiri through next-round first draw | 01-01, 01-05 | COVERED | |
| CONTEXT | D-03 | Compact PASS/first-mismatch output | 01-01, 01-05 | COVERED | |
| CONTEXT | D-04 | No Phase 2 scoring claim | 01-01, 01-05 | COVERED | |
| CONTEXT | D-05 | Fresh production randomness | 01-01, 01-05 | COVERED | |
| CONTEXT | D-06 | Forfeit result/current distance/no high-score update | 01-01, 01-05 | COVERED | |
| CONTEXT | D-07 | Clean menu return and restart | 01-01, 01-04, 01-05 | COVERED | |
| CONTEXT | D-08 | Esc modal blocks gameplay while timer runs | 01-01, 01-02, 01-03, 01-05 | COVERED | |
| CONTEXT | D-09 | Cancel/second Esc and timeout priority | 01-01, 01-03, 01-05 | COVERED | |
| CONTEXT | D-10 | One tracked Markdown; raw local outputs | 01-01, 01-05 | COVERED | |
| CONTEXT | D-11 | Complete before/after evidence fields | 01-01, 01-05 | COVERED | |
| CONTEXT | D-12 | Codex-run tests/build/GUI; uncertainty is not PASS | 01-01, 01-05 | COVERED | |
| CONTEXT | D-13 | Exact same-process Player sequence | 01-01, 01-05 | COVERED | |
| CONTEXT | D-14 | SoloScoringGameManager ownership/name | 01-04 | COVERED | |
| CONTEXT | D-15 | SoloScoringUIController output ownership/name | 01-03 | COVERED | |
| CONTEXT | D-16 | Player→GameManager→UI direct flow | 01-02→01-04 | COVERED | |
| CONTEXT | D-17 | No generalized mode controllers | 01-02→01-04 | COVERED | |
| CONTEXT | D-18 | Single scene, inactive selected mode root | 01-04, 01-05 | COVERED | |
| CONTEXT | D-19 | Component/session lifecycle separation | 01-04, 01-05 | COVERED | |
| CONTEXT | D-20 | Separate ForfeitRequested intent | 01-02, 01-05 | COVERED | |
| CONTEXT | D-21 | PlayerHandController/View split only | 01-02 | COVERED | |
| CONTEXT | D-22 | Esc-first immediate return/input block | 01-02, 01-05 | COVERED | |
| CONTEXT | D-23 | Manager policy, timer continues, timeout wins | 01-03→01-05 | COVERED | |
| CONTEXT | D-24 | Existing EventSystem/navigation, Cancel default | 01-03, 01-05 | COVERED | |
| CONTEXT | D-25 | Scene-local overlay outside panel map | 01-03, 01-05 | COVERED | |
| CONTEXT | D-26 | Synchronous block before animation | 01-03, 01-05 | COVERED | |
| CONTEXT | D-27 | Working slice → isolated refactor → expanded/final verification | 01-01→01-05 | COVERED | Sequential waves encode the required order. |
| CONTEXT | D-28 | Preserve completed branch commits/artifacts | 01-01→01-05 | COVERED | No completed artifact is rewritten or replayed. |

Deferred ideas and later-phase rule/UI work are excluded, not gaps.

<verification>
Task 1 provides automated XML/build/process evidence; Task 2 provides only explicit human GUI observation; Task 3 joins them fail-closed. No result is inferred from process exit, artifact existence, or expected screen content.
</verification>

<success_criteria>
- BASE-01 through BASE-05 and D-01 through D-28 are traceable across completed 01-01 and replacement plans 01-02 through 01-05.
- Exact 3 trace + 13 lifecycle cases are GREEN, the Windows build/report/exe succeed, and the eight-step same-PID checkpoint is APPROVED with GUI PASS.
- All raw test/build outputs remain untracked and all user-owned dirty files remain untouched.
</success_criteria>

<output>
Create `.planning/phases/01-executable-baseline/01-05-SUMMARY.md` when done.
</output>
