---
phase: 01-executable-baseline
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - Assets/Scripts/UI-Kozeki/PlayerHand.cs
  - Assets/Scripts/UI-Kozeki/PlayerHand.cs.meta
  - Assets/Scripts/UI-Kozeki/PlayerHandController.cs
  - Assets/Scripts/UI-Kozeki/PlayerHandController.cs.meta
  - Assets/Scripts/UI-Kozeki/PlayerHandView.cs
  - Assets/Scripts/UI-Kozeki/PlayerHandView.cs.meta
  - Assets/Scripts/AL-1S/_Enums_Ecchi_Nun_Mari.cs
  - Assets/Scripts/MahjongGameManager.cs
  - Assets/Scenes/SampleScene.unity
  - Assets/Editor/Tests/SoloSessionLifecycleTests.cs
autonomous: true
requirements: [BASE-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-20/D-22: Esc emits a separate ForfeitRequested session intent before the gameplay-input guard and returns immediately; PlayerCallType contains only mahjong actions."
    - "D-21: PlayerHandController owns keyboard input, selected index, and discard/call/forfeit intents while PlayerHandView alone owns tile creation and selection/tsumo presentation."
    - "D-27/D-28: the approved 01-01 Player path stays behaviorally green throughout this isolated refactor, without replaying or reverting completed commits."
  artifacts:
    - path: "Assets/Scripts/UI-Kozeki/PlayerHandController.cs"
      provides: "Solo hand input and intent boundary"
      contains: "ForfeitRequested"
    - path: "Assets/Scripts/UI-Kozeki/PlayerHandView.cs"
      provides: "Existing hand tile and selection presentation"
      contains: "FillHand"
    - path: "Assets/Editor/Tests/SoloSessionLifecycleTests.cs"
      provides: "Behavior-preservation checks through the renamed player boundary"
      contains: "ConfirmForfeit_FinalizesOnceWithoutSavingHighScore"
  key_links:
    - from: "Assets/Scripts/UI-Kozeki/PlayerHandController.cs"
      to: "Assets/Scripts/MahjongGameManager.cs"
      via: "discard/call/ForfeitRequested subscriptions"
      pattern: "ForfeitRequested|OnPlayerDiscard|OnPlayerCall"
    - from: "Assets/Scripts/UI-Kozeki/PlayerHandController.cs"
      to: "Assets/Scripts/UI-Kozeki/PlayerHandView.cs"
      via: "selection and hand rendering calls"
      pattern: "PlayerHandView"
---

## Phase Goal

**As a** 플레이어와 개발자, **I want to** 기존 솔로 경험을 반복 재현하고, **so that** 안전하게 변경 여부를 판별할 수 있다.

<objective>
01-01에서 증명한 Player 입력→솔로 실행→UI 경로를 유지하면서 혼합된 PlayerHand 책임을 입력 Controller와 표현 View로 분리한다.

Purpose: D-20~D-22의 입력 경계를 마련하고 후속 모드 소유권 개명에서 UI와 입력이 다시 결합되지 않게 한다.
Output: PlayerHandController, PlayerHandView, 별도 ForfeitRequested intent, 보존된 scene GUID와 GREEN lifecycle gate.
</objective>

<execution_context>
@C:/Users/user/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-executable-baseline/01-CONTEXT.md
@.planning/phases/01-executable-baseline/01-01-SUMMARY.md
@C:/Users/user/.codex/skills/unity-test-gate/SKILL.md
@Assets/Scripts/UI-Kozeki/PlayerHand.cs
@Assets/Scripts/MahjongGameManager.cs
@Assets/Editor/Tests/SoloSessionLifecycleTests.cs

<interfaces>
Preserve the existing `OnPlayerDiscard(int)`, `OnPlayerCall(PlayerCallType)`, `FillHand(List&lt;MahjongTile&gt;)`, and `TsumoTile(TsumoInfo)` behaviors while moving them to their decided owners. Add `event Action ForfeitRequested` only on `PlayerHandController`. The manager continues to subscribe directly to the controller; no UI controller or event bus sits between them per D-16.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract hand rendering behind PlayerHandView</name>
  <files>Assets/Scripts/UI-Kozeki/PlayerHand.cs, Assets/Scripts/UI-Kozeki/PlayerHandView.cs, Assets/Scripts/UI-Kozeki/PlayerHandView.cs.meta, Assets/Scripts/MahjongGameManager.cs, Assets/Scenes/SampleScene.unity, Assets/Editor/Tests/SoloSessionLifecycleTests.cs</files>
  <behavior>
    - The two selected lifecycle cases remain GREEN after the rendering move.
    - PlayerHandView creates the thirteen hand tile objects and one tsumo tile, renders FillHand/TsumoTile, and updates selected highlighting.
    - The still-named PlayerHand owns input and selection state and delegates every visual mutation to one serialized PlayerHandView.
  </behavior>
  <action>First extend the existing two lifecycle cases with structural assertions for the decided D-21 responsibility boundary, then extract tile prefab/holders, instantiated tile objects, riichi/tsumo indicators, FillHand, TsumoTile, and selected highlighting into `PlayerHandView`. Create and commit `PlayerHandView.cs.meta`, and wire `SampleScene.unity` to that committed GUID so a fresh checkout resolves the same component. Keep `PlayerHand` as the temporary controller name for this task only, owning `Update`, DAS/ARR, selected index, discard/call events, and game-start/game-over input state. Wire one view reference using the existing objects and assets; do not clone tiles or alter their visual hierarchy. Change `MahjongGameManager` output calls to the view through the controller's explicit presentation methods only as needed to keep the approved flow running. Reuse the existing Korean TMP, tile prefab, scene objects, and DOTween assets. Add no generalized seat, variable-hand, pooling, input-context, or modal abstraction per D-17/D-21 and the deferred list.</action>
  <verify>
    <automated>$gateJson = &amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter SoloSessionLifecycleTests -ExpectedGate Green; $gate = $gateJson | ConvertFrom-Json; $expected = @('ConfirmForfeit_FinalizesOnceWithoutSavingHighScore','StartNewGame_Twice_DetachesAndResetsSession') | Sort-Object; $cases = @(([xml](Get-Content -Raw -LiteralPath $gate.resultsPath)).SelectNodes('/test-run//test-case')); $actual = @($cases | ForEach-Object { $_.name } | Sort-Object); if ($gate.status -ne 'GREEN' -or $gate.total -ne 2 -or $cases.Count -ne 2 -or (Compare-Object $expected $actual)) { $gateJson; exit 1 }</automated>
  </verify>
  <done>D-21 rendering is owned by PlayerHandView, input remains behaviorally unchanged, and the XML-backed helper returns GREEN for exactly the two selected lifecycle cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rename the input owner and separate ForfeitRequested</name>
  <files>Assets/Scripts/UI-Kozeki/PlayerHand.cs, Assets/Scripts/UI-Kozeki/PlayerHand.cs.meta, Assets/Scripts/UI-Kozeki/PlayerHandController.cs, Assets/Scripts/UI-Kozeki/PlayerHandController.cs.meta, Assets/Scripts/AL-1S/_Enums_Ecchi_Nun_Mari.cs, Assets/Scripts/MahjongGameManager.cs, Assets/Editor/Tests/SoloSessionLifecycleTests.cs</files>
  <behavior>
    - Esc is evaluated before the gameplay-input block, emits ForfeitRequested once, and returns before discard/navigation/call handling in the same frame.
    - A pending confirmation still accepts a second Esc while all mahjong gameplay intents remain blocked.
    - PlayerCallType no longer contains or transports a Forfeit value.
  </behavior>
  <action>Move `PlayerHand.cs` and its `.meta` together to `PlayerHandController.cs` so the existing serialized component GUID is preserved, rename the type, and update the manager reference. Remove the `Forfeit` member from `PlayerCallType` in `_Enums_Ecchi_Nun_Mari.cs` per D-20 and replace that route with `PlayerHandController.ForfeitRequested`; subscribe and unsubscribe it beside discard/call events using the same owner and handler. Migrate `ConfirmForfeit_FinalizesOnceWithoutSavingHighScore` away from the enum call so the fixture exercises the production ForfeitRequested path, and retain the restart case unchanged. In `Update`, handle Escape first, invoke ForfeitRequested, and return immediately; only then apply the manager-controlled gameplay-input gate to navigation, discard, riichi, and tsumo. Update Korean XML summaries for changed public members and remove obsolete commented input blocks in the touched class. Run the exact two-case lifecycle subset through the global helper; do not use `-quit`, add a package, or add an assembly descriptor. Preserve all 01-01 commits/evidence per D-28.</action>
  <verify>
    <automated>$gateJson = &amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter SoloSessionLifecycleTests -ExpectedGate Green; $gate = $gateJson | ConvertFrom-Json; $expected = @('ConfirmForfeit_FinalizesOnceWithoutSavingHighScore','StartNewGame_Twice_DetachesAndResetsSession') | Sort-Object; $cases = @(([xml](Get-Content -Raw -LiteralPath $gate.resultsPath)).SelectNodes('/test-run//test-case')); $actual = @($cases | ForEach-Object { $_.name } | Sort-Object); if ($gate.status -ne 'GREEN' -or $gate.total -ne 2 -or $cases.Count -ne 2 -or (Compare-Object $expected $actual)) { $gateJson; exit 1 }</automated>
  </verify>
  <done>D-20/D-22 are implemented at the player boundary, the old asset GUID still resolves in SampleScene, and exactly the two selected lifecycle cases remain GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Keyboard input → session intent | Untrusted same-frame key combinations cross from Unity Input into the solo manager. |
| Scene serialization → renamed component | Existing YAML must retain the MonoBehaviour GUID while responsibilities move. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-20 | Tampering | Player intent routing | medium | mitigate | A distinct ForfeitRequested event and one direct manager subscription prevent a session command from entering the mahjong-call enum. |
| T-01-21 | Denial of Service | Same-frame input | medium | mitigate | Escape executes first and returns immediately; automated behavior proves no discard follows the request. |
| T-01-22 | Tampering | Unity asset rename | medium | mitigate | Move `.cs` with `.meta`, validate the scene reference, and require a GREEN Unity compile/test gate after each task. |
</threat_model>

<verification>
Both tasks use the global unity-test-gate helper. Only its JSON `status: GREEN` with parseable NUnit XML and a positive selected count passes; BLOCKED, RED, or FAIL halts without source-success claims.
</verification>

<success_criteria>
- D-20 through D-22 and the D-27/D-28 preservation boundary are implemented without changing the approved player-visible path.
- No PlayerCallType.Forfeit session route, duplicated player event subscription, generalized hand system, package, asmdef, prefab extraction, or new scene is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-executable-baseline/01-02-SUMMARY.md` when done.
</output>
