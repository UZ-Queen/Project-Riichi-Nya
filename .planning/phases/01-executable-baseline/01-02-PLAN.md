---
phase: 01-executable-baseline
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - Assets/Scripts/GameEndReason.cs
  - Assets/Scripts/MahjongGameManager.cs
  - Assets/Scripts/UI-Kozeki/PlayerHand.cs
  - Assets/Scripts/UI-Kozeki/UiScoreDistanceInfo.cs
  - Assets/Scripts/UI-Kozeki/UiRemainingTimeIndicator.cs
  - Assets/Scripts/UI-Kozeki/GameUIManager.cs
  - Assets/Scripts/UI-Kozeki/UiManager.cs
  - Assets/Scripts/UI-Kozeki/UiGameOver.cs
  - Assets/Scenes/SampleScene.unity
  - Assets/Editor/Tests/SoloSessionLifecycleTests.cs
autonomous: true
requirements: [BASE-05]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-06/D-09: a forfeit finalizes once with reason Forfeit, retains the current distance, and never mutates the saved high score; timeout finalization wins when remaining time has reached zero."
    - "D-07: every StartNewGame detaches the prior round/timer/service callbacks before replacement and resets timer, score, distance, round, hand, river, input flags, and finalization state for one new session."
    - "D-08/D-09: the first Esc sets a private pending-forfeit flag and enters Processing without pausing the 180-second Timer; cancel or a second Esc works only while that flag is true, and Esc during unrelated Processing is ignored."
    - "Late callbacks and repeated confirmation cannot finalize or mutate a replacement session."
  artifacts:
    - path: "Assets/Scripts/GameEndReason.cs"
      provides: "Explicit TimeExpired and Forfeit finalization reason"
      contains: "GameEndReason"
    - path: "Assets/Scripts/MahjongGameManager.cs"
      provides: "Single idempotent session start, confirmation, and finalization owner"
      contains: "ConfirmForfeit"
    - path: "Assets/Editor/Tests/SoloSessionLifecycleTests.cs"
      provides: "BASE-05 reset, priority, finalization, and subscription regressions"
      contains: "SoloSessionLifecycleTests"
  key_links:
    - from: "Assets/Scripts/UI-Kozeki/PlayerHand.cs"
      to: "Assets/Scripts/MahjongGameManager.cs"
      via: "PlayerCallType.Forfeit is resolved by a private pending-forfeit flag while the existing Processing state remains shared by round transition, discard, and tsumo"
      pattern: "Forfeit|Processing|isForfeitPending"
    - from: "Assets/Scripts/MahjongGameManager.cs"
      to: "Assets/Scripts/Timer.cs"
      via: "one OnTimerFinished subscription per active session and RemainingTime priority check"
      pattern: "OnTimerFinished|RemainingTime"
    - from: "Assets/Editor/Tests/SoloSessionLifecycleTests.cs"
      to: "Assets/Scripts/MahjongGameManager.cs"
      via: "public confirmation callbacks plus fixture-observed state/events"
      pattern: "RequestForfeit|CancelForfeit|ConfirmForfeit"
---

<objective>
Expand the proven 01-01 production Player tracer and its two existing lifecycle cases to the final thirteen-case BASE-05 suite covering race, idempotency, stale-callback, subscription, input, result, and scene wiring per D-06 through D-09.

Purpose: make forfeit, timeout, and same-process restart deterministic before the scene-level GUI verification.
Output: explicit end reason, idempotent manager lifecycle, input/subscription guards, deterministic UI reset/menu wiring, and focused EditMode lifecycle tests.
</objective>

<execution_context>
@C:/Users/user/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-executable-baseline/01-CONTEXT.md
@.planning/phases/01-executable-baseline/01-RESEARCH.md
@.planning/phases/01-executable-baseline/01-PATTERNS.md
@.planning/phases/01-executable-baseline/01-01-SUMMARY.md
@Assets/Scripts/MahjongGameManager.cs
@Assets/Scripts/UI-Kozeki/PlayerHand.cs
@Assets/Scripts/UI-Kozeki/UiScoreDistanceInfo.cs
@Assets/Scripts/UI-Kozeki/UiRemainingTimeIndicator.cs
@Assets/Scripts/Timer.cs
@Assets/Scripts/ScoreManagerDistance.cs
@Assets/Scripts/Configs/SettingsManager.cs

<interfaces>
Preserve `UiManager.OnGameStartButton() -> MahjongGameManager.StartNewGame()` as the only production start path. Preserve `PlayerHand.OnPlayerCall(PlayerCallType.Forfeit)` as the Esc intent. `Timer.StartTimer(180)` resets and runs without pause; `ScoreManagerDistance.Initialize()` resets score-derived distance; `SettingsManager.Load/Save` remains the persistence boundary.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the two-case lifecycle fixture with finalization races and restart safety</name>
  <files>Assets/Scripts/GameEndReason.cs, Assets/Scripts/MahjongGameManager.cs, Assets/Editor/Tests/SoloSessionLifecycleTests.cs</files>
  <read_first>
    - `AGENTS.md` safety, English identifiers, C# 9, Allman, Korean XML summary, lifecycle grouping, and symmetric subscription rules
    - `.planning/phases/01-executable-baseline/01-CONTEXT.md` D-05 through D-09 and D-12/D-13
    - `.planning/phases/01-executable-baseline/01-PATTERNS.md` One Session Owner and Idempotent Event Lifecycle
    - `Assets/Scripts/MahjongGameManager.cs` StartNewGame, HandleGameOver, AttachRoundEvent, DetachRoundEvent, CallHandler, OnEnable, and OnDisable
    - `Assets/Scripts/Timer.cs` StartTimer, RemainingTime, OnTimerFinished, and TaimuSutopu
    - `Assets/Scripts/Configs/SettingsManager.cs` current high-score Load/Save boundary
  </read_first>
  <behavior>
    - Existing from `01-01-T2`: `ConfirmForfeit_FinalizesOnceWithoutSavingHighScore` and `StartNewGame_Twice_DetachesAndResetsSession` stay green without being recreated.
    - `RequestForfeit_EntersPendingWithoutPausingTimer`: PlayerTurn plus first forfeit request sets the private pending-forfeit flag and enters Processing; the Timer remains running and a cancel returns to PlayerTurn.
    - `SecondEscape_CancelsPendingForfeit`: a second Esc while the confirmation uses Processing follows the same cancellation path and cannot finalize the game.
    - `TimeoutAtConfirmation_WinsAndRejectsLateCallbacks`: if RemainingTime is zero when confirmation resolves, TimeExpired wins; later confirm/cancel or prior-session callbacks are inert per D-09.
  </behavior>
  <action>Preserve the two exact cases created by `01-01-T2`, add the three failing cases `RequestForfeit_EntersPendingWithoutPausingTimer`, `SecondEscape_CancelsPendingForfeit`, and `TimeoutAtConfirmation_WinsAndRejectsLateCallbacks`, then harden the same implementation. Keep `MahjongGameManager` as the single owner: `StartNewGame` detaches prior round/timer handlers before replacement, resets the finalization guard and private pending-forfeit flag, initializes existing score/timer dependencies, creates a fresh production seed per D-05, attaches the new round exactly once, and exposes PlayerTurn last. Do not treat `GameState.Processing` itself as proof that a confirmation is open; round transition, discard, and tsumo already use it. The first PlayerTurn request sets the private flag and Processing state without stopping the timer; cancellation and a second Esc return to PlayerTurn only when the flag is true; Esc during any other Processing work is ignored. `ConfirmForfeit` checks remaining time before choosing a reason so TimeExpired wins at zero. Route timer and confirmed forfeit through one idempotent finalization method that detaches before visible state changes, ignores late/repeated callbacks, retains current distance, and bypasses the existing save call for Forfeit per D-06. Keep normal timeout persistence. Add no replay, pause, settings, enum value, or generalized lifecycle abstraction. Use early returns, C# 9, Allman braces, grouped Unity lifecycle methods, and Korean XML summaries for every behavior-changing public type/member.</action>
  <acceptance_criteria>
    - Unity discovers each of the five exact lifecycle cases once from `Assets/Editor/Tests/SoloSessionLifecycleTests.cs`, all pass, and run-level failed/errors are zero.
    - A first forfeit request uses the existing Processing gate while the timer continues; cancel and second Esc restore PlayerTurn without finalization.
    - Confirm and timeout converge on one idempotent finalizer, timeout wins at zero, and late prior-session callbacks are inert.
    - Forfeit presents current distance but performs no high-score write; TimeExpired preserves the existing persistence behavior.
    - `StartNewGame` detaches before replacement and leaves one current round/timer handler set after repeated starts.
  </acceptance_criteria>
  <verify>
    <automated>New-Item -ItemType Directory -Force -Path 'Temp\phase1' | Out-Null; $unity = 'C:\Program Files\Unity\Hub\Editor\2022.3.29f1\Editor\Unity.exe'; &amp; $unity -batchmode -nographics -projectPath $PWD -runTests -testPlatform EditMode -testFilter SoloSessionLifecycleTests -testResults "$PWD\Temp\phase1\lifecycle.xml" -quit -logFile "$PWD\Temp\phase1\lifecycle.log"; if ($LASTEXITCODE -ne 0 -or -not (Test-Path 'Temp\phase1\lifecycle.xml')) { exit 1 }; [xml]$xml = Get-Content -Raw 'Temp\phase1\lifecycle.xml'; $expected = @('RequestForfeit_EntersPendingWithoutPausingTimer','SecondEscape_CancelsPendingForfeit','ConfirmForfeit_FinalizesOnceWithoutSavingHighScore','TimeoutAtConfirmation_WinsAndRejectsLateCallbacks','StartNewGame_Twice_DetachesAndResetsSession'); $cases = @($xml.SelectNodes("//test-case[contains(@fullname,'SoloSessionLifecycleTests')]")); $run = $xml.SelectSingleNode('/test-run'); if ($cases.Count -ne 5 -or @($cases | Where-Object { $_.result -ne 'Passed' }).Count -ne 0 -or $null -eq $run -or [int]$run.total -ne 5 -or [int]$run.passed -ne 5 -or [int]$run.failed -ne 0 -or [int]$run.skipped -ne 0 -or [int]$run.inconclusive -ne 0 -or ($run.HasAttribute('errors') -and [int]$run.errors -ne 0)) { exit 1 }; foreach ($name in $expected) { if (@($cases | Where-Object { $_.fullname -like "*.$name" }).Count -ne 1) { exit 1 } }</automated>
  </verify>
  <done>D-06 through D-09 finalization/reset behavior is implemented in the existing manager, all five focused cases pass, Forfeit cannot update high score, and timeout priority plus stale-callback resistance are observable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate player input and make reused presenter subscriptions symmetric</name>
  <files>Assets/Scripts/UI-Kozeki/PlayerHand.cs, Assets/Scripts/UI-Kozeki/UiScoreDistanceInfo.cs, Assets/Scripts/UI-Kozeki/UiRemainingTimeIndicator.cs, Assets/Editor/Tests/SoloSessionLifecycleTests.cs</files>
  <read_first>
    - `AGENTS.md` C# 9, Allman, lifecycle grouping, public XML summary, and deterministic unsubscription rules
    - `.planning/phases/01-executable-baseline/01-CONTEXT.md` D-07 through D-09
    - `.planning/phases/01-executable-baseline/01-PATTERNS.md` PlayerHand, UiScoreDistanceInfo, and UiRemainingTimeIndicator assignments
    - `Assets/Scripts/UI-Kozeki/PlayerHand.cs` Start, Update, OnPlayerCall, HandleGameStart, and HandleGameOver
    - `Assets/Scripts/UI-Kozeki/UiScoreDistanceInfo.cs` Construct, OnEnable, and OnDisable
    - `Assets/Scripts/UI-Kozeki/UiRemainingTimeIndicator.cs` Construct and OnTimerEnds
  </read_first>
  <behavior>
    - `PlayerHand_Subscriptions_AreSymmetricAndUnique`: PlayerHand subscribes HandleGameStart to OnGameStart and HandleGameOver to OnGameOver once, and OnDisable removes the same handlers.
    - `ForfeitPending_BlocksGameplayInputButAllowsEscape`: while the private pending-forfeit flag is true, discard/navigation/call inputs do nothing but Esc still reaches the manager to cancel; after cancel PlayerTurn restores normal input, while Esc during Processing with the flag false is ignored and does not change state or emit a visible effect.
    - `RemainingTimeIndicator_ReconstructsWithoutDuplicateSubscriptions`: constructing UiRemainingTimeIndicator twice detaches the old Timer and leaves one current tick/finish subscription; disable removes both.
    - `ScoreDistanceInfo_ReconstructsWithoutDuplicateSubscriptions`: constructing UiScoreDistanceInfo twice detaches the old score service and leaves one current boost/distance subscription; disable removes both.
  </behavior>
  <action>Extend the existing five-case fixture with these four cases before production edits. In `PlayerHand`, replace the incorrect Start subscriptions with paired OnEnable/OnDisable handlers: HandleGameStart subscribes to OnGameStart and HandleGameOver to OnGameOver. At the top of Update, let Esc emit the existing Forfeit intent from PlayerTurn or Processing, then return before navigation/discard/call processing whenever the manager is not in PlayerTurn. The manager's private pending-forfeit flag is the authority: it cancels only a real pending modal and early-returns for Esc during non-forfeit Processing. Extend `ForfeitPending_BlocksGameplayInputButAllowsEscape` with that negative assertion rather than adding a fourteenth case. Reset local selection, tsumo, and call-button flags on OnGameStart. In both reused presenters, centralize attach/detach helpers; `Construct` detaches from the previous non-null dependency before assignment, attaches at most once when active, and `OnDisable` safely detaches. Add no input abstraction or manager. Follow C# 9, Allman, lifecycle grouping, Korean XML summary updates for behavior-changing public `Construct` members, and exact owner/handler symmetry per `AGENTS.md`.</action>
  <acceptance_criteria>
    - Unity discovers each of the nine exact lifecycle/subscription cases once, all pass, and run-level failed/errors are zero.
    - `PlayerHand` pairs OnGameStart/OnGameOver subscriptions with the same handlers in OnDisable and emits no gameplay intent outside PlayerTurn except Esc cancellation during Processing.
    - Repeated `Construct` calls detach old Timer/score-service handlers before replacement and leave one active callback set; OnDisable is null-safe and symmetric.
    - A second session resets local input flags and one input produces one visible/domain reaction.
  </acceptance_criteria>
  <verify>
    <automated>$unity = 'C:\Program Files\Unity\Hub\Editor\2022.3.29f1\Editor\Unity.exe'; &amp; $unity -batchmode -nographics -projectPath $PWD -runTests -testPlatform EditMode -testFilter SoloSessionLifecycleTests -testResults "$PWD\Temp\phase1\lifecycle.xml" -quit -logFile "$PWD\Temp\phase1\lifecycle.log"; if ($LASTEXITCODE -ne 0 -or -not (Test-Path 'Temp\phase1\lifecycle.xml')) { exit 1 }; [xml]$xml = Get-Content -Raw 'Temp\phase1\lifecycle.xml'; $expected = @('RequestForfeit_EntersPendingWithoutPausingTimer','SecondEscape_CancelsPendingForfeit','ConfirmForfeit_FinalizesOnceWithoutSavingHighScore','TimeoutAtConfirmation_WinsAndRejectsLateCallbacks','StartNewGame_Twice_DetachesAndResetsSession','PlayerHand_Subscriptions_AreSymmetricAndUnique','ForfeitPending_BlocksGameplayInputButAllowsEscape','RemainingTimeIndicator_ReconstructsWithoutDuplicateSubscriptions','ScoreDistanceInfo_ReconstructsWithoutDuplicateSubscriptions'); $cases = @($xml.SelectNodes("//test-case[contains(@fullname,'SoloSessionLifecycleTests')]")); $run = $xml.SelectSingleNode('/test-run'); if ($cases.Count -ne 9 -or @($cases | Where-Object { $_.result -ne 'Passed' }).Count -ne 0 -or $null -eq $run -or [int]$run.total -ne 9 -or [int]$run.passed -ne 9 -or [int]$run.failed -ne 0 -or [int]$run.skipped -ne 0 -or [int]$run.inconclusive -ne 0 -or ($run.HasAttribute('errors') -and [int]$run.errors -ne 0)) { exit 1 }; foreach ($name in $expected) { if (@($cases | Where-Object { $_.fullname -like "*.$name" }).Count -ne 1) { exit 1 } }; $source = Get-Content -Raw 'Assets/Scripts/UI-Kozeki/PlayerHand.cs'; if ($source -notmatch 'OnGameStart\s*\+=' -or $source -notmatch 'OnGameStart\s*-=' -or $source -notmatch 'OnGameOver\s*\+=' -or $source -notmatch 'OnGameOver\s*-=' ) { exit 1 }</automated>
  </verify>
  <done>Gameplay input is blocked only during the pending/end states while Esc cancellation remains reachable, reused dependencies have one symmetric subscription set, and all BASE-05 lifecycle cases pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden repeat UI initialization and complete D-08/D-09 scene behavior</name>
  <files>Assets/Scripts/UI-Kozeki/GameUIManager.cs, Assets/Scripts/UI-Kozeki/UiManager.cs, Assets/Scripts/UI-Kozeki/UiGameOver.cs, Assets/Scenes/SampleScene.unity, Assets/Editor/Tests/SoloSessionLifecycleTests.cs</files>
  <read_first>
    - `AGENTS.md` encoding, English identifiers, C# 9, Allman, Korean XML summary, lifecycle grouping, and symmetric subscription rules
    - `.planning/phases/01-executable-baseline/01-CONTEXT.md` D-06 through D-09 and D-13
    - `.planning/phases/01-executable-baseline/01-PATTERNS.md` GameUIManager, UiManager, UiGameOver, and SampleScene assignments
    - `Assets/Scripts/UI-Kozeki/GameUIManager.cs` panel map, Initialize, HandleGameOver, HideAllPanels, and OnBackButton
    - `Assets/Scripts/UI-Kozeki/UiManager.cs` ShowPanel, ToInGameState, OnBBagguButton, and OnGameStartButton
    - `Assets/Scripts/UI-Kozeki/UiGameOver.cs` current score/high-score presenter
    - `Assets/Scenes/SampleScene.unity` GameUIManager panel list, game canvas, GameOver object, manager references, and existing button callbacks
  </read_first>
  <behavior>
    - `GameUiInitialize_Twice_UsesExplicitPanelState`: Initialize sets required gameplay panels active and all result/confirmation/transient panels inactive on both first and second calls; it never toggles reset state.
    - `ForfeitConfirmation_ShowHide_DoesNotPauseTimer`: showing and hiding ForfeitConfirmation affects only that mapped panel and leaves the Timer running; the same case opens `SampleScene.unity` and structurally verifies one confirmation object, one matching panel entry, and exact one-listener confirm/cancel Button targets.
    - `GameOver_Forfeit_RendersReasonAndDistance`: UiGameOver renders 포기 plus the supplied current distance for GameEndReason.Forfeit and retains normal timeout result text for TimeExpired.
    - `ReturnToMainMenu_ThenStart_UsesExistingRoute`: result back navigation clears in-game panels/history and lands on MainMenu; OnGameStartButton remains the sole first/second-session start route.
  </behavior>
  <action>Add the four exactly named failing UI cases, then harden the existing 01-01 serialized tracer path. Extend `ForfeitConfirmation_ShowHide_DoesNotPauseTimer` with an Editor scene-structure assertion: open `Assets/Scenes/SampleScene.unity` through `EditorSceneManager`, find exactly one active-or-inactive GameObject named `ForfeitConfirmation`, exactly one `GameUIManager.panels` entry whose state is `ForfeitConfirmation` and whose rect targets that object, and exactly two child Buttons representing confirm/cancel. Each Button must contain exactly one persistent listener; both targets must be the intended scene `MahjongGameManager` component, and the method names must be exactly `ConfirmForfeit` and `CancelForfeit`, once each. Restore/close test scene state deterministically. This extends an existing case, so the suite remains thirteen. Replace toggle-based initialization with explicit desired states and resolve active volatile tweens before the second session is exposed. Pair GameUIManager subscriptions in OnEnable/OnDisable. Ensure UiGameOver preserves the reason label/current-distance contract while persistence stays in MahjongGameManager. Make `UiManager.ReturnToMainMenuAfterGame` hide game panels, clear stale in-game history, and show MainMenu while keeping `OnGameStartButton -> StartNewGame` as the only first/second-session route. Correct only the scene entries proven wrong by the structural test. Reuse current fonts/button visuals and DOTween behavior. Add no scene, script, panel registry, modal framework, replay UI, or pause manager. Preserve Unity YAML references/meta data and follow the project C# conventions.</action>
  <acceptance_criteria>
    - Unity discovers all thirteen exact lifecycle/UI cases once, all pass, and run-level failed/errors are zero.
    - Repeated GameUIManager initialization sets explicit panel state and leaves no stale confirmation/result panel or active reset tween.
    - The Editor scene assertion proves exactly one confirmation object and matching panel entry, plus one persistent listener per confirm/cancel Button targeting the scene MahjongGameManager with exact method names; YAML substring presence is not accepted as proof.
    - Forfeit renders `포기` and current distance; TimeExpired retains its normal result; persistence remains manager-owned.
    - The result back route lands on MainMenu and both starts continue through `OnGameStartButton -> StartNewGame`.
  </acceptance_criteria>
  <verify>
    <automated>$unity = 'C:\Program Files\Unity\Hub\Editor\2022.3.29f1\Editor\Unity.exe'; &amp; $unity -batchmode -nographics -projectPath $PWD -runTests -testPlatform EditMode -testFilter SoloSessionLifecycleTests -testResults "$PWD\Temp\phase1\ui-lifecycle.xml" -quit -logFile "$PWD\Temp\phase1\ui-lifecycle.log"; if ($LASTEXITCODE -ne 0 -or -not (Test-Path 'Temp\phase1\ui-lifecycle.xml')) { exit 1 }; [xml]$xml = Get-Content -Raw 'Temp\phase1\ui-lifecycle.xml'; $expected = @('ConfirmForfeit_FinalizesOnceWithoutSavingHighScore','StartNewGame_Twice_DetachesAndResetsSession','RequestForfeit_EntersPendingWithoutPausingTimer','SecondEscape_CancelsPendingForfeit','TimeoutAtConfirmation_WinsAndRejectsLateCallbacks','PlayerHand_Subscriptions_AreSymmetricAndUnique','ForfeitPending_BlocksGameplayInputButAllowsEscape','RemainingTimeIndicator_ReconstructsWithoutDuplicateSubscriptions','ScoreDistanceInfo_ReconstructsWithoutDuplicateSubscriptions','GameUiInitialize_Twice_UsesExplicitPanelState','ForfeitConfirmation_ShowHide_DoesNotPauseTimer','GameOver_Forfeit_RendersReasonAndDistance','ReturnToMainMenu_ThenStart_UsesExistingRoute'); $cases = @($xml.SelectNodes("//test-case[contains(@fullname,'SoloSessionLifecycleTests')]")); $run = $xml.SelectSingleNode('/test-run'); if ($cases.Count -ne 13 -or @($cases | Where-Object { $_.result -ne 'Passed' }).Count -ne 0 -or $null -eq $run -or [int]$run.failed -ne 0 -or ($run.HasAttribute('errors') -and [int]$run.errors -ne 0)) { exit 1 }; foreach ($name in $expected) { if (@($cases | Where-Object { $_.fullname -like "*.$name" }).Count -ne 1) { exit 1 } }; if ((rg -n 'OnGameStartButton' 'Assets/Scripts/UI-Kozeki/UiManager.cs' | Measure-Object).Count -lt 1) { exit 1 }</automated>
  </verify>
  <done>The actual scene owns one minimal confirmation panel, existing result/menu/start UI implements D-06 through D-09, repeat initialization is explicit, and all thirteen lifecycle/UI regression cases pass.</done>
</task>

</tasks>

## Topology and commit boundaries

Scope-risk acceptance: the user reviewed and retained this tracer-first integration boundary and the three-plan topology. The ten-file union is high because the existing single-scene lifecycle crosses the existing manager, input, presenter, scene, and shared `SoloSessionLifecycleTests` owners; each of the three tasks remains a narrow cohesive commit and adds no new abstraction. Confidence remains low because no completed-phase actuals exist. Splitting again would serialize the same fixture and manager/scene edits without enabling parallel execution, so the retained topology remains the smallest dependency graph that preserves tracer-first flow.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Prior local session -> replacement session | Old round, timer, service, and UI callbacks must not mutate the new session. |
| Player input -> session manager | Esc, confirm, cancel, discard, and timeout can race at the local state boundary. |
| Session result -> local save | A forfeit result may display existing high score but must not write a new one. |

## STRIDE Threat Register — ASVS L1, block threshold high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-05 | Tampering | prior round/timer callbacks | high | mitigate | Detach-before-replace, one active handler per owner, session finalization guard, and regression cases in `SoloSessionLifecycleTests`. |
| T-01-06 | Tampering | confirm versus timeout | high | mitigate | Check RemainingTime at confirmation, make TimeExpired win at zero, and make finalization idempotent. |
| T-01-07 | Tampering | forfeit high-score persistence | high | mitigate | Keep the save decision in MahjongGameManager and bypass Save for GameEndReason.Forfeit under test. |
| T-01-08 | Denial of Service | duplicate presenter subscriptions | medium | mitigate | Detach old dependency before replacement and detach symmetrically on disable; assert one callback set after repeated Construct. |
</threat_model>

<verification>
The focused EditMode fixture must contain at least nine discovered project cases, every case must pass, and Unity failure/error totals must remain zero. Licensing failure is a hard environment BLOCKED gate, never a PASS.
</verification>

<success_criteria>
- BASE-05 lifecycle rules D-06 through D-09 are automated before scene/UI integration.
- Forfeit, timeout, restart, and subscription behavior have runnable regression checks.
- Public additions follow Korean XML summary, C# 9, Allman, lifecycle grouping, and symmetric unsubscription rules.
</success_criteria>

<output>
Create `.planning/phases/01-executable-baseline/01-02-SUMMARY.md` when done.
</output>
