---
context: phase
phase: 01-executable-baseline
task: 0
total_tasks: 0
status: paused
last_updated: 2026-08-29T08:49:24.709Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Plan verification is not complete — the latest checker returned 3 BLOCKER and 3 WARNING findings — do not run `$gsd-execute-phase 1` until a fresh checker returns `## VERIFICATION PASSED` and coverage gates pass.
- [ ] CONSTRAINT: Unity predefined assembly boundary — a new asmdef cannot reference `Assembly-CSharp` in Unity 2022.3 — keep Phase 1 tests under the predefined `Assembly-CSharp-Editor` route in `Assets/Editor/Tests`, and fail if Phase 1 adds any new `.asmdef` or `.asmref` relative to the repository baseline.
- [ ] CONSTRAINT: Evidence must fail closed — XML/report/exe/PID/checkpoint absence or Unity licensing failure must produce `BLOCKED` or `NOT OBSERVED`, never `PASS` — every status must be independently tied to a live source.
- [ ] CONSTRAINT: Scene wiring needs structural proof — YAML string occurrence is insufficient — load `Assets/Scenes/SampleScene.unity` in an Editor test and assert exactly one confirmation object/panel entry and exact persistent Button targets/methods.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Treating draft files as verified plans | Planner files existed on disk after missing completion markers, but repeated checker passes still found executable-contract defects. | blocking | Always run a fresh typed `gsd-plan-checker`; accept only its explicit `## VERIFICATION PASSED` marker. |
| Single-path descriptor check | Checking only the previously proposed asmdef path allows another `.asmdef`/`.asmref` to bypass the plan contract. | blocking | Enumerate all `Assets/**/*.asmdef` and `Assets/**/*.asmref` and compare against the repository baseline. |
| GUI-coupled evidence validation | Automated EditMode/build/commit/PID claims were checked only when GUI status was PASS, allowing false data when GUI was FAIL/BLOCKED. | blocking | Validate each automated claim against its live source regardless of GUI status, then validate cross-status relationships. |
| Scene YAML substring proof | Presence of names does not prove uniqueness or correct persistent callback targets. | blocking | Use an Editor test that opens the scene and inspects objects, panel map, Button target objects, and method names. |
| Long Windows subagent with no return marker | Planner/research agents repeatedly ran about 9–10 minutes, wrote files, and failed to return a marker. | advisory | Interrupt at the bounded threshold, inspect mtimes/files, then send recovered artifacts to a fresh checker; do not duplicate work blindly. |

<current_state>
Phase 1 planning is paused after the user chose option 3 because this context is saturated. No source implementation has begun. Three draft plans and a Walking Skeleton exist. The automatic planner/checker loop ran three revisions; the final checker still reports 3 BLOCKER and 3 WARNING issues. `.planning/STATE.md` must not be advanced to execution readiness based only on plan file existence.
</current_state>

<completed_work>

Completed planning work:
- Phase 1 discussion context captured in `01-CONTEXT.md` with D-01 through D-13.
- `01-RESEARCH.md` created; original research commit `513bf50`.
- `01-VALIDATION.md` created; original validation commit `c5ae504`.
- `01-PATTERNS.md` created; original pattern-map commit `60fe303`.
- `01-SKELETON.md` and three draft PLAN files created.
- Three planner/checker revision cycles completed. Issue counts moved 9 → 6 → 5 → final 6; the final pass therefore did not converge.
- No gameplay/runtime source files were edited.
</completed_work>

<remaining_work>

Final checker findings to repair:

1. **BLOCKER — descriptor scan is incomplete.**
   - Affected: `01-01-T1`, `01-03-T1` automated verification.
   - Current check only rules out `Assets/Tests/EditMode/ProjectRiichiNya.EditModeTests.asmdef`.
   - Required: enumerate every `Assets/**/*.asmdef` and `Assets/**/*.asmref` and require no Phase 1 additions, or compare against a recorded baseline list.

2. **BLOCKER — terminal evidence validator is not independently live-sourced.**
   - Affected: `01-03-T3` automated verification.
   - Required: reuse `01-01-T3` live Git/Unity/status validation, extend XML validation to the full 16-case suite, validate every automated PASS regardless of GUI state, compare `Test count: 16`, seeded trace seed/action summary, summary PID to `Temp/phase1/player.pid`, and PID to the live process.

3. **BLOCKER — scene wiring proof is too weak.**
   - Affected: `01-02-T3`, threat `T-01-09`.
   - Required: extend the Editor UI test to open `SampleScene.unity`, assert exactly one `ForfeitConfirmation` object and panel-map entry, and assert both Button persistent targets/methods are exactly the manager's `ConfirmForfeit` and `CancelForfeit`.

4. **WARNING — `01-VALIDATION.md` task mappings are stale.**
   - Required ownership: build/full-suite `01-03-T1`; final human checkpoint `01-03-T2`; ledger sealing `01-03-T3`; lifecycle 13-case creation maps to `01-02-T1`, `01-02-T2`, and `01-02-T3`.

5. **WARNING — `01-RESEARCH.md` still recommends the superseded asmdef approach.**
   - Update or mark superseded: Primary recommendation, Recommended Project Structure, Pattern 2, Validation Architecture, Wave 0 Gaps.
   - Remove the asmdef example and state the predefined `Assets/Editor/Tests` route consistently with PLAN/PATTERNS/VALIDATION/SKELETON.

6. **WARNING — plan file counts remain high.**
   - `01-01` modifies 11 files; `01-02` modifies 10; estimate confidence is low.
   - Re-check whether nonessential hardening can move later, but do not break the required end-to-end D-13 tracer merely to hit a number.

After repair:
- Run a fresh typed `gsd-plan-checker` against all three plans and supporting artifacts.
- If it passes, run requirements coverage for BASE-01 through BASE-05 and decision coverage for D-01 through D-13.
- Only then update STATE/ROADMAP annotations and create the final planning commit.
</remaining_work>

<decisions_made>

- Stop with option 3 and resume in a fresh context; the current context is saturated.
- Do not implement or execute Phase 1 until planning verification passes.
- Use predefined Editor compilation under `Assets/Editor/Tests`; do not introduce a Phase 1 asmdef/asmref that references `Assembly-CSharp`, and do not broaden into a runtime assembly migration.
- Keep fixed seeds test-only and preserve fresh production randomness.
- Keep raw XML/log/build output project-local and ignored; commit one concise human-readable baseline ledger.
- Treat Unity LicensingClient IPC failure as an external execution blocker, not a source-code failure and never a PASS.
</decisions_made>

<blockers>

- Planning blocker: final checker has 3 BLOCKER and 3 WARNING findings; exact repairs are listed above.
- Later execution blocker: Unity batch mode exited on LicensingClient IPC timeout before project load. Repair licensing or use a licensed Windows host before claiming BASE-03 through BASE-05 PASS.
- Later human checkpoint: D-13 requires observed Player build → launch → solo start → Esc confirmation → forfeit result → menu → same-process restart. Uncertain visual control must be escalated to the user.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable resume state and exact uncommitted files.
2. `.planning/phases/01-executable-baseline/.continue-here.md` — this handoff and final checker findings.
3. `.planning/phases/01-executable-baseline/01-CONTEXT.md` — locked D-01 through D-13 decisions.
4. `.planning/phases/01-executable-baseline/01-01-PLAN.md` — current tracer draft.
5. `.planning/phases/01-executable-baseline/01-02-PLAN.md` — lifecycle/UI hardening draft.
6. `.planning/phases/01-executable-baseline/01-03-PLAN.md` — full verification and evidence sealing draft.
7. `.planning/phases/01-executable-baseline/01-VALIDATION.md` — Nyquist map that needs task-ID correction.
8. `.planning/phases/01-executable-baseline/01-PATTERNS.md` — predefined Editor test boundary and analogs.
9. `.planning/phases/01-executable-baseline/01-RESEARCH.md` — research with stale asmdef sections that must be superseded.
10. `AGENTS.md` — project safety, C#, Unity, and evidence rules.

## Critical Anti-Patterns (do NOT repeat these)

- Do not accept a plan because files exist or a planner wrote many details; only the plan-checker pass is authoritative.
- Do not restore the `Assembly-CSharp`-referencing asmdef proposal.
- Do not validate UI wiring with `rg`/YAML string presence alone.
- Do not couple automated evidence truth to GUI PASS.
- Do not mark Unity licensing failure or unobserved GUI behavior as PASS.
- Do not start Phase 1 implementation from these drafts before the remaining checker issues are fixed.

## Infrastructure State

- Running subagents/processes: none.
- Outstanding async jobs: none.
- Git branch: existing branch unchanged; no branch create/switch occurred.
- Committed planning bases: `513bf50` research, `c5ae504` validation, `60fe303` pattern map.
- Uncommitted planning state: three PLAN files and SKELETON are untracked; RESEARCH/PATTERNS/VALIDATION have later modifications.
- Unity host: Editor 2022.3.29f1 and Windows support installed; LicensingClient IPC blocked the observed batch run.

## Pre-Execution Critique Required

- Design artifacts: `01-01-PLAN.md`, `01-02-PLAN.md`, `01-03-PLAN.md`, `01-SKELETON.md`.
- Critique focus: final six checker findings, fail-closed evidence relations, exact Editor test discovery, structural scene callback verification, and tracer scope.
- Gate: **Do NOT begin execution until a fresh typed plan-checker returns `## VERIFICATION PASSED` and both coverage gates pass.**

<context>
The correct next move is one small, targeted planning repair pass in a fresh context, not another broad rewrite. Preserve all already-passing requirements/decision/security work. Fix the three blockers first, align VALIDATION/RESEARCH, then ask the checker to inspect the entire disk state from scratch. If issue count does not improve, stop and report rather than silently accepting the drafts.
</context>

<next_action>
Start with: run `$gsd-resume-work`, acknowledge the four blocking constraints above, repair only the six final checker findings, then spawn a fresh typed `gsd-plan-checker`. Do not run `$gsd-execute-phase 1` yet.
</next_action>
