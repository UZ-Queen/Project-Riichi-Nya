---
phase: 02-shared-rules-core
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - Assets/Editor/Tests/Phase2RegressionTests.cs
  - Assets/Editor/Tests/Phase2ConformanceTests.cs
  - Assets/Scripts/AL-1S/MahjongHandDecomposer.cs
  - Assets/Scripts/AL-1S/MahjongUtilities.cs
  - Assets/Scripts/AL-1S/MahjongWinInfo.cs
autonomous: true
requirements: [RULE-04, RULE-05]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "RULE-04 edge coverage [adjacency, empty, ordering]: empty/malformed/non-winning input returns explicit no-result, while standard, chiitoitsu and kokushi checks are non-exclusive and every legal standard decomposition plus every winning-tile attribution is returned in stable order without duplicates."
    - "RULE-05 edge coverage [boundary, adjacency, empty, ordering, precision]: zero-candidate input returns no-result; adjacent candidate incomes and limit boundaries compare exact integer payments; winner income, then Han, then Fu, then stable enumeration index selects exactly one result without precision loss."
    - "D-10~D-12에 따라 runtime은 최선 결과 하나만 소비하고 전체 후보는 test/debug 경계에만 노출되며 yaku-frequency 또는 별도 canonical rank table을 사용하지 않는다."
  artifacts:
    - path: "Assets/Scripts/AL-1S/MahjongHandDecomposer.cs"
      provides: "34-kind count-vector 기반 일반형/치또이츠/국사무쌍 완전 열거"
      contains: "internal static class MahjongHandDecomposer"
    - path: "Assets/Scripts/AL-1S/MahjongUtilities.cs"
      provides: "기존 public win-check 진입점을 exhaustive candidate 경계로 연결"
      contains: "GetHighestWinInfo"
  key_links:
    - from: "Assets/Scripts/AL-1S/MahjongUtilities.cs"
      to: "Assets/Scripts/AL-1S/MahjongHandDecomposer.cs"
      via: "기존 CheckWinnable/GetHighestWinInfo가 exhaustive candidates를 사용"
      pattern: "MahjongHandDecomposer"
    - from: "Assets/Scripts/AL-1S/MahjongHandDecomposer.cs"
      to: "Assets/Scripts/AL-1S/MahjongTileAndBlock.cs"
      via: "red identity와 분리된 TileID kind-count mapping"
      pattern: "TileID"
  prohibitions:
    - requirement_id: "RULE-04"
      category: "scope-integrity"
      statement: "MUST NOT introduce a speculative generalized rules interpreter, plugin system, or external profile parser for decomposition."
---

## Phase Goal

**As a** gameplay developer and player, **I want to** use one accurate and automatically verified rules core across solo and four-player modes, **so that** both modes produce consistent mahjong results.

<objective>
D-10~D-12의 모든 합법 분해와 결정론적 최선 결과 선택을 기존 win-check API 뒤에 연결한다.

Purpose: 탐욕 분해와 han/fu-only 선택 때문에 합법 화료 또는 최고 수입 해석을 놓치는 근본 원인을 제거한다.
Output: `MahjongHandDecomposer`, 기존 facade의 exhaustive wiring, Regression/Conformance 사례.
</objective>

<execution_context>
@C:/Users/user/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-rules-core/02-CONTEXT.md
@.planning/phases/02-shared-rules-core/02-RESEARCH.md
@.planning/phases/02-shared-rules-core/02-PATTERNS.md
@.planning/phases/02-shared-rules-core/02-01-SUMMARY.md

<interfaces>
기존 `MahjongUtility.CheckWinnable*`, `GetHighestWinInfo`, `MahjongWin.WinType`을 caller compatibility 경계로 사용한다. 새 `MahjongHandDecomposer.Enumerate(...)`는 internal candidate collection을 반환하며 runtime caller에는 최종 `MahjongWinInfo` 하나만 노출한다.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 탐욕 분해 누락을 RED로 고정하고 세 화료형을 완전 열거</name>
  <files>Assets/Editor/Tests/Phase2RegressionTests.cs, Assets/Editor/Tests/Phase2ConformanceTests.cs, Assets/Scripts/AL-1S/MahjongHandDecomposer.cs, Assets/Scripts/AL-1S/MahjongUtilities.cs, Assets/Scripts/AL-1S/MahjongWinInfo.cs</files>
  <read_first>Assets/Editor/Tests/Phase2RegressionTests.cs, Assets/Editor/Tests/Phase2ConformanceTests.cs, Assets/Scripts/AL-1S/MahjongHandDecomposer.cs, Assets/Scripts/AL-1S/MahjongUtilities.cs, Assets/Scripts/AL-1S/MahjongWinInfo.cs, Assets/Scripts/AL-1S/MahjongTileAndBlock.cs, .planning/phases/02-shared-rules-core/02-CONTEXT.md, .planning/phases/02-shared-rules-core/02-RESEARCH.md, .planning/phases/02-shared-rules-core/02-PATTERNS.md</read_first>
  <behavior>
    - Regression RED: 첫 머리/두 greedy pass가 놓치는 알려진 ambiguous standard hand가 현행 공개 API에서 실패한다.
    - GREEN: 가장 작은 남은 kind에서 triplet/sequence 양쪽으로 재귀 분기하고 standard/chiitoitsu/kokushi를 각각 검사한다.
    - 각 구조에서 winning tile이 pair/triplet/sequence를 완성할 수 있는 모든 attribution이 stable order로 생성된다.
  </behavior>
  <action>D-27의 다음 known defect로 greedy-miss case를 현행 public API에 먼저 추가하고 assertion RED XML/commit을 남긴다. 그 뒤 34-kind count-vector를 사용해 가능한 head를 순회하고 가장 작은 남은 kind의 triplet/sequence를 모두 분기하는 `MahjongHandDecomposer`를 구현한다. chiitoitsu와 kokushi는 standard와 독립 검사한다. D-06에 따라 normal/red equality를 분해에 사용하지 않고 TileID kind key로 묶는다. declared meld는 shape/count/openness를 검증하고 필요한 concealed meld 수만 줄이며 실제 call state는 만들지 않는다. `MahjongWin`에 stable enumeration index와 winning attribution을 보존해 후속 fu/yaku가 같은 구조를 평가할 수 있게 한다.</action>
  <verify>
    <automated>&amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter Phase2RegressionTests -ExpectedGate Green</automated>
  </verify>
  <acceptance_criteria>RED는 greedy-miss assertion만으로 실패하고 GREEN은 standard ambiguity의 expected candidate set, chiitoitsu, kokushi, red-containing decomposition을 exact set/order로 통과한다. empty/malformed/5-copy input은 exception 없이 explicit no-result다.</acceptance_criteria>
  <done>모든 합법 구조와 화료패 귀속이 결정론적으로 열거되고 기존 공개 win-check가 이를 사용한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 후보 전체를 점수 입력으로 보존하고 최선 선택 tie-break를 고정</name>
  <files>Assets/Editor/Tests/Phase2ConformanceTests.cs, Assets/Scripts/AL-1S/MahjongUtilities.cs, Assets/Scripts/AL-1S/MahjongWinInfo.cs</files>
  <read_first>Assets/Editor/Tests/Phase2ConformanceTests.cs, Assets/Scripts/AL-1S/MahjongUtilities.cs, Assets/Scripts/AL-1S/MahjongWinInfo.cs, Assets/Scripts/AL-1S/MahjongHandDecomposer.cs, .planning/phases/02-shared-rules-core/02-CONTEXT.md, .planning/phases/02-shared-rules-core/02-RESEARCH.md</read_first>
  <behavior>
    - higher `WinnerIncome` wins; equal income compares Han, then Fu; all equal preserves first stable candidate.
    - zero candidates returns no-result and test/debug enumeration does not become a second runtime scorer.
  </behavior>
  <action>D-10~D-12에 따라 candidate selector를 실제 payment result의 `WinnerIncome`, Han, Fu, stable enumeration index 순으로 정의한다. payment 값은 Plan 04가 완성하기 전 기존 score table에서 계산되는 동일 의미 값을 adapter로 사용하되 selector public surface를 두 벌로 만들지 말고 Plan 04가 같은 result field를 채우게 한다. 기존 `CompareTo`의 han/fu-only 의미를 제거하고 result equality와 ordering을 분리한다. yaku 빈도 가중치나 수기 조합 우선순위를 넣지 않는다.</action>
  <verify>
    <automated>&amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter Phase2ConformanceTests -ExpectedGate Green</automated>
  </verify>
  <acceptance_criteria>literal synthetic candidates가 income/Han/Fu/stable-order 네 비교 경계를 모두 통과하고 runtime API가 한 결과만 반환한다. candidate enumeration은 internal/test 경계다.</acceptance_criteria>
  <done>완전 후보 집합에서 D-11 순서로 최선 결과 하나가 결정된다.</done>
</task>

</tasks>

## Artifacts this phase produces

- `Assets/Scripts/AL-1S/MahjongHandDecomposer.cs` — 새 internal exhaustive decomposer.
- `MahjongWin`과 `MahjongUtility`는 기존 symbols이며 winning attribution/candidate wiring만 확장한다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compact hand/meld input → decomposer | malformed count와 meld shape가 재귀 탐색으로 들어온다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | decomposer input | high | mitigate | 13/14 count, kind≤4, winning membership, meld shape/count를 재귀 전에 거절한다. |
| T-02-05 | Denial of Service | recursive enumeration | medium | mitigate | 매 단계 가장 작은 남은 kind를 반드시 소비하고 duplicate permutations를 생성하지 않는다. |
| T-02-06 | Repudiation | candidate selection | medium | mitigate | literal candidate table로 income/Han/Fu/stable tie를 독립 검증한다. |
</threat_model>

<verification>
Regression과 Conformance fixture를 각각 GREEN으로 실행하고 exact selected names, positive count, zero non-passed를 XML에서 확인한다.
</verification>

<success_criteria>
- RULE-04/05의 8개 edge 후보가 두 명시적 truth로 빠짐없이 닫힌다.
- ambiguous standard, chiitoitsu, kokushi와 winning attribution이 exhaustive하다.
- 최선 결과는 D-11 순서이며 D-12의 금지 comparator가 없다.
</success_criteria>

<output>
Create `.planning/phases/02-shared-rules-core/02-02-SUMMARY.md` when done.
</output>
