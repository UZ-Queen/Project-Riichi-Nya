---
phase: 02-shared-rules-core
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - Assets/Editor/Tests/Phase2RegressionTests.cs
  - Assets/Editor/Tests/Phase2ConformanceTests.cs
  - Assets/Scripts/AL-1S/MahjongRound.cs
  - Assets/Scripts/SoloScoringGameManager.cs
  - Assets/Scripts/ScoreManagerDistance.cs
autonomous: true
requirements: [RULE-10]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "RULE-10 edge coverage [adjacency]: identical non-empty tile/context inputs passed directly or through the solo adapter produce equal decomposition, yaku, Han, Fu, and payment."
    - "RULE-10 edge coverage [empty]: invalid or empty tile/context input is rejected identically by direct evaluator and solo adapter with no score delivery."
    - "RULE-10 edge coverage [ordering]: valid score delivery occurs exactly once after win resolution and before result/distance presentation."
    - "D-19~D-24에 따라 solo는 one wall/one hand, 18-draw cap, -8000 invalid-tsumo policy, dealer/round progression and East→South→West→North→East cycle을 소유하며 hidden opponents/honba/sticks/noten payments를 만들지 않는다."
    - "교정된 tsumo `WinnerIncome`은 기존 `IScoreDistanceService.GetBoostAndDistance`에 정확히 한 번 전달되고 기존 거리·부스트 공식은 바뀌지 않는다."
  artifacts:
    - path: "Assets/Scripts/AL-1S/MahjongRound.cs"
      provides: "공유 core를 소비하는 one-wall solo round와 18-draw/progression 정책"
      contains: "remainingTsumoCount"
    - path: "Assets/Scripts/SoloScoringGameManager.cs"
      provides: "corrected winner income을 기존 distance service에 한 번 전달하는 adapter"
      contains: "GetBoostAndDistance"
  key_links:
    - from: "Assets/Scripts/SoloScoringGameManager.cs"
      to: "Assets/Scripts/AL-1S/MahjongRound.cs"
      via: "start/discard/win/draw round replacement와 event subscription"
      pattern: "StartNextRound|AttachRoundEvent|DetachRoundEvent"
    - from: "Assets/Scripts/SoloScoringGameManager.cs"
      to: "Assets/Scripts/IScoreDistanceService.cs"
      via: "MahjongPaymentResult.WinnerIncome을 exactly once 전달"
      pattern: "WinnerIncome|GetBoostAndDistance"
---

## Phase Goal

**As a** gameplay developer and player, **I want to** use one accurate and automatically verified rules core across solo and four-player modes, **so that** both modes produce consistent mahjong results.

<objective>
완성된 core result를 기존 solo lifecycle에 최소 연결하고 D-19~D-24의 독립 solo round 정책을 정확히 유지한다.

Purpose: 4인 상태를 미리 만들지 않고도 같은 core가 실제 gameplay 결과를 구동함을 증명한다.
Output: solo core adapter, exact one-delivery integration tests, deterministic solo round progression.
</objective>

<execution_context>
@C:/Users/user/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-rules-core/02-CONTEXT.md
@.planning/phases/02-shared-rules-core/02-RESEARCH.md
@.planning/phases/02-shared-rules-core/02-PATTERNS.md
@.planning/phases/02-shared-rules-core/02-04-SUMMARY.md

<interfaces>
기존 `SoloScoringGameManager.StartNextRound`, `AttachRoundEvent`, `DetachRoundEvent`, `UpdatePlayerScore`, `IScoreDistanceService.GetBoostAndDistance`를 유지한다. `MahjongRound`는 core의 best `MahjongWinInfo`/`MahjongPaymentResult`를 event로 전달하며 solo manager가 `WinnerIncome`을 소비한다.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 실제 solo 화료가 core winner income을 한 번만 소비하게 연결</name>
  <files>Assets/Editor/Tests/Phase2RegressionTests.cs, Assets/Editor/Tests/Phase2ConformanceTests.cs, Assets/Scripts/AL-1S/MahjongRound.cs, Assets/Scripts/SoloScoringGameManager.cs, Assets/Scripts/ScoreManagerDistance.cs</files>
  <read_first>Assets/Editor/Tests/Phase2RegressionTests.cs, Assets/Editor/Tests/Phase2ConformanceTests.cs, Assets/Scripts/AL-1S/MahjongRound.cs, Assets/Scripts/SoloScoringGameManager.cs, Assets/Scripts/IScoreDistanceService.cs, Assets/Scripts/ScoreManagerDistance.cs, Assets/Editor/Tests/SoloSessionLifecycleTests.cs, .planning/phases/02-shared-rules-core/02-CONTEXT.md, .planning/phases/02-shared-rules-core/02-PATTERNS.md</read_first>
  <behavior>
    - same hand/context direct evaluator result equals solo adapter result.
    - valid tsumo forwards `WinnerIncome` once; dora-only/invalid tsumo forwards nothing, applies -8000 once, ends round once, starts next round.
    - existing distance formula receives corrected score unchanged.
  </behavior>
  <action>D-19/D-21에 따라 `MahjongPlayer.IsTsumoAble`와 `MahjongRound.HandlePlayerWin`을 최종 core evaluator result 하나로 연결하고 solo manager가 `MahjongPaymentResult.WinnerIncome`을 기존 `UpdatePlayerScore`/distance seam에 정확히 한 번 전달하게 한다. 기존 ron amount compatibility compensation은 두지 않는다. invalid win은 core에서 invalid만 반환하고 `-8000`, round end, next round는 solo policy에 남긴다. `PLAYER-OBS-01`과 같은 literal row를 재사용하는 제거 가능한 observation entry를 기존 `SoloScoringGameManager`/`MahjongRound`에 `#if UNITY_EDITOR || DEVELOPMENT_BUILD`로 한정한다: command line `--phase2-observation`일 때만 hand `1m2m3m4m0m6m2p3p4p2s3s5p5p`, winning tile `4s`, 비친/동장/리치/멘젠쯔모, visible indicator `2p`, ura indicator `3s`를 실제 production scorer/UI path에 주입한다. 일반 build에는 entry body와 호출이 컴파일되지 않아야 하며 범용 debug framework나 scene asset을 추가하지 않는다. 같은 guard 안에서 `ScoreManagerDistance`의 이동/감쇠를 0으로 고정해 score 반영 직후 관찰값이 `DistanceWithAccumulated=80.00`, `BoostLevel=6`으로 결정론적이게 하고, 이 값은 `WinnerIncome=12000`의 기존 `/150f`, `/1900f` 공식에서 나온다. Phase 1 subscription symmetry와 sessionFinalized early return을 그대로 유지하고 동작이 바뀌는 public score/lifecycle member의 한국어 XML summary를 갱신한다.</action>
  <verify>
    <automated>&amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter Phase2RegressionTests -ExpectedGate Green</automated>
  </verify>
  <acceptance_criteria>spy score service가 valid tsumo에서 corrected WinnerIncome 한 호출, invalid tsumo에서 zero 호출을 기록한다. invalid penalty/next-round event는 각 한 번이며 direct/core-via-solo result equality가 통과한다.</acceptance_criteria>
  <done>실제 solo 화료/오화료 경로가 공유 core와 기존 distance service를 중복 없이 사용한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: one-wall 18-draw solo와 친·국풍 진행 계약을 확장</name>
  <files>Assets/Editor/Tests/Phase2ConformanceTests.cs, Assets/Scripts/AL-1S/MahjongRound.cs, Assets/Scripts/SoloScoringGameManager.cs</files>
  <read_first>Assets/Editor/Tests/Phase2ConformanceTests.cs, Assets/Scripts/AL-1S/MahjongRound.cs, Assets/Scripts/SoloScoringGameManager.cs, Assets/Editor/Tests/MahjongRoundTraceTests.cs, .planning/phases/02-shared-rules-core/02-CONTEXT.md, .planning/phases/02-shared-rules-core/02-RESEARCH.md</read_first>
  <behavior>
    - one round has one shared wall and player hand, exactly 18 allowed draws, zero triggers exhaustive draw independent of wall remainder.
    - dealer win/tenpai draw repeats dealer/round; dealer noten advances; non-dealer win/draw advances virtual dealer/round without opponent state.
    - round wind cycles E/S/W/N/E and North 4 advances to East 1; session still ends only by 3-minute timer.
  </behavior>
  <action>D-20/D-22/D-23/D-24에 따라 `remainingTsumoCount`를 default 18로 실제 draw path에 연결하고 zero에서 wall remainder와 무관하게 draw resolution을 한 번 발생시킨다. `graveyard`와 opponent storage를 solo rule에서 제거한다. player dealer/non-dealer result와 tenpai input만으로 round/dealer advancement를 계산하며 session은 East 1의 player dealer로 시작하고 opponents는 always no-win/noten인 policy input으로만 취급한다. honba, riichi sticks, noten payments와 hanchan end를 넣지 않고 East/South/West/North의 four-round cycle을 고정한다.</action>
  <verify>
    <automated>&amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter Phase2ConformanceTests -ExpectedGate Green</automated>
  </verify>
  <acceptance_criteria>18→0 boundary, wall-remains-at-zero case, dealer win/tenpai/noten, non-dealer win/draw, North4→East1 literal scenarios가 GREEN이며 no opponent collection is constructed.</acceptance_criteria>
  <done>독립 solo round 정책이 shared core 위에서 결정론적으로 진행된다.</done>
</task>

</tasks>

## Artifacts this phase produces

- 새 top-level runtime symbol은 없다. 기존 `MahjongRound`와 `SoloScoringGameManager`가 shared core adapter로 교정된다.
- `Phase2RegressionTests`/`Phase2ConformanceTests`에 mode-independence와 solo policy cases가 추가된다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure score result → solo mutable lifecycle | 중복 event 또는 잘못된 delta가 score/distance를 두 번 적용할 수 있다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Tampering | score forwarding | high | mitigate | spy service exact-one assertion와 existing subscription symmetry를 검증한다. |
| T-02-14 | Denial of Service | draw limit/progression | medium | mitigate | 18→0 boundary와 single-resolution guard로 repeated transition을 차단한다. |
| T-02-15 | Elevation of Privilege | invalid win intent | medium | mitigate | core invalid result 후 solo-owned single -8000/next-round policy만 실행한다. |
</threat_model>

<verification>
두 Phase 2 fixtures와 기존 trace/lifecycle fixtures를 분리 실행해 parseable XML, positive count, exact unique names, zero non-passed를 확인한다.
</verification>

<success_criteria>
- RULE-10의 3개 edge 후보가 3개 explicit truth로 일대일 닫힌다.
- D-19~D-24의 solo ownership과 progression이 자동 검증된다.
- corrected WinnerIncome이 distance service에 정확히 한 번 전달된다.
</success_criteria>

<output>
Create `.planning/phases/02-shared-rules-core/02-05-SUMMARY.md` when done.
</output>
