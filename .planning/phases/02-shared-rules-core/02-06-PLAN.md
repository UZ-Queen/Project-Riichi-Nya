---
phase: 02-shared-rules-core
plan: 06
type: execute
wave: 6
depends_on: [02-05]
files_modified:
  - Assets/Editor/Tests/DurableSaveTestScope.cs
  - Assets/Editor/Tests/DurableSaveTestScope.cs.meta
  - Assets/Editor/Tests/SoloSessionLifecycleTests.cs
  - Assets/Editor/Tests/Phase2ConformanceTests.cs
  - Assets/Scripts/Configs/Settings.cs
  - Assets/Scripts/Configs/SettingsManager.cs
  - .planning/phases/02-shared-rules-core/02-EVIDENCE.md
autonomous: true
requirements: [RULE-10]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-25/D-26에 따라 현재 scoring rules version은 build version과 분리된 hardcoded int 1이고 save mismatch는 old version/highScore를 경고한 뒤 highScore와 version만 갱신한다."
    - "same-version, missing-version legacy JSON, mismatch, interrupted test recovery 모두 sound/input/statistics bytes or values를 보존하며 기존 Phase 1 lifecycle test count 15와 save-safety behavior가 변하지 않는다."
  artifacts:
    - path: "Assets/Editor/Tests/DurableSaveTestScope.cs"
      provides: "Phase 1/2 real-save tests가 공유하는 same-directory durable backup/absent-marker scope"
      contains: "internal sealed class DurableSaveTestScope"
    - path: "Assets/Scripts/Configs/Settings.cs"
      provides: "PetitGameSaveData.scoringRulesVersion"
      contains: "scoringRulesVersion"
    - path: "Assets/Scripts/Configs/SettingsManager.cs"
      provides: "high-score-only version migration and diagnostic"
      contains: "MahjongScoringRules.CurrentVersion"
  key_links:
    - from: "Assets/Scripts/Configs/SettingsManager.cs"
      to: "Assets/Scripts/AL-1S/MahjongYaku.cs"
      via: "Load compares DTO version with MahjongScoringRules.CurrentVersion"
      pattern: "CurrentVersion"
    - from: "Assets/Editor/Tests/Phase2ConformanceTests.cs"
      to: "Assets/Editor/Tests/DurableSaveTestScope.cs"
      via: "real persistent path mutation protected by the same idempotent recovery contract"
      pattern: "DurableSaveTestScope"
  prohibitions:
    - requirement_id: "RULE-10"
      category: "data-preservation"
      statement: "MUST NOT replace the entire save DTO or wipe sound, input, statistics, or other unrelated user data during scoring-version migration."
      status: resolved
      verification: test
---

## Phase Goal

**As a** gameplay developer and player, **I want to** use one accurate and automatically verified rules core across solo and four-player modes, **so that** both modes produce consistent mahjong results.

<objective>
교정 전 high score가 새 규칙 기록으로 섞이지 않게 D-25/D-26의 narrow migration을 추가하면서 Phase 1 save durability를 보존한다.

Purpose: 점수 의미 변경을 안전하게 식별하되 사용자 설정과 통계를 잃지 않는다.
Output: scoring version field, high-score-only migration, shared durable test scope, persistence evidence.
</objective>

<execution_context>
@C:/Users/user/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-rules-core/02-CONTEXT.md
@.planning/phases/02-shared-rules-core/02-RESEARCH.md
@.planning/phases/01-executable-baseline/01-07-SUMMARY.md
@.planning/phases/02-shared-rules-core/02-05-SUMMARY.md

<interfaces>
기존 `PetitGameSaveData`, `SettingsManager.Load/Save`, `Application.persistentDataPath/yaml.json`을 유지한다. 새 `scoringRulesVersion` int field는 `MahjongScoringRules.CurrentVersion == 1`과 비교하며 version history/registry를 만들지 않는다.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 기존 durable save guard를 공유하고 high-score-only migration을 RED→GREEN으로 구현</name>
  <files>Assets/Editor/Tests/DurableSaveTestScope.cs, Assets/Editor/Tests/DurableSaveTestScope.cs.meta, Assets/Editor/Tests/SoloSessionLifecycleTests.cs, Assets/Editor/Tests/Phase2ConformanceTests.cs, Assets/Scripts/Configs/Settings.cs, Assets/Scripts/Configs/SettingsManager.cs</files>
  <read_first>Assets/Editor/Tests/DurableSaveTestScope.cs, Assets/Editor/Tests/SoloSessionLifecycleTests.cs, Assets/Editor/Tests/Phase2ConformanceTests.cs, Assets/Scripts/Configs/Settings.cs, Assets/Scripts/Configs/SettingsManager.cs, Assets/Scripts/AL-1S/MahjongYaku.cs, .planning/phases/01-executable-baseline/01-07-SUMMARY.md, .planning/phases/02-shared-rules-core/02-CONTEXT.md, .planning/phases/02-shared-rules-core/02-PATTERNS.md</read_first>
  <behavior>
    - RED: legacy/mismatched JSON currently retains an incompatible high score because no scoring rules version exists.
    - GREEN: mismatch logs old version/value, resets only highScore, stamps version 1, preserves sound/input/statistics and saves safely.
    - same-version load changes nothing; interrupted test recovery restores exact prior bytes or original absence.
  </behavior>
  <action>Phase 1 `SoloSessionLifecycleTests`의 same-directory backup/originally-absent guard가 Phase 2 migration test에도 필요해진 시점에만 `DurableSaveTestScope`로 그대로 추출하고 Unity가 생성한 `.meta`를 함께 보존하며 15개 기존 named cases와 recovery semantics를 보존한다. D-27에 따라 migration absence를 `Phase2ConformanceTests`의 real public Load path에서 RED로 남긴 뒤, D-25의 int version 1 field와 D-26의 selective migration을 구현한다. mismatch는 old version/highScore를 `Debug.LogWarning`으로 기록하고 default highScore 및 current version만 갱신한다. `PetitGameSaveData.scoringRulesVersion`과 동작이 바뀌는 public load/save member에는 한국어 XML summary를 작성·갱신한다. DTO 전체 교체, history, per-profile records, save-path override는 만들지 않는다.</action>
  <verify>
    <automated>&amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter Phase2ConformanceTests -ExpectedGate Green</automated>
  </verify>
  <acceptance_criteria>legacy/mismatch/same-version cases가 exact expected 값을 통과하고 sound/input/statistics는 sentinel values 그대로다. lifecycle fixture는 정확히 기존 15 names를 유지하며 interrupted recovery case도 GREEN이다.</acceptance_criteria>
  <done>점수 규칙 버전 불일치가 high score만 안전하게 초기화한다.</done>
</task>

<task type="auto">
  <name>Task 2: persistence 회귀와 ledger를 기존 4+15 계약과 함께 잠금</name>
  <files>.planning/phases/02-shared-rules-core/02-EVIDENCE.md</files>
  <read_first>.planning/phases/02-shared-rules-core/02-EVIDENCE.md, Assets/Editor/Tests/SoloSessionLifecycleTests.cs, Assets/Editor/Tests/Phase2ConformanceTests.cs, .planning/phases/01-executable-baseline/01-VALIDATION.md, .planning/phases/02-shared-rules-core/02-VALIDATION.md</read_first>
  <action>D-28~D-30에 따라 Phase 2 Conformance migration names/count와 Phase 1 lifecycle exact 15 names/count를 독립 실행 결과로 기록한다. shared helper 추출 전후 Phase 1 `4 + 15 = 19` 의미가 변하지 않았음을 명시하고 raw XML/log는 local ignored path에 둔다.</action>
  <verify>
    <automated>&amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter SoloSessionLifecycleTests -ExpectedGate Green</automated>
  </verify>
  <acceptance_criteria>lifecycle XML은 exact 15 unique passed names와 zero non-passed이고 ledger의 migration/lifecycle SHA, counts, paths가 실제 결과와 일치한다.</acceptance_criteria>
  <done>Phase 2 migration과 Phase 1 durable save evidence가 서로 분리되어 추적된다.</done>
</task>

</tasks>

## Artifacts this phase produces

- `Assets/Editor/Tests/DurableSaveTestScope.cs` — 새 fixture-private shared save protection helper.
- `PetitGameSaveData.scoringRulesVersion` — 기존 DTO의 새 int field.
- `SettingsManager`와 `SoloSessionLifecycleTests`는 기존 symbols/fixture이며 narrow migration/helper reuse만 적용한다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted JSON → current scoring rules | legacy/corrupt/version-mismatched data가 사용자 기록과 설정으로 들어온다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16 | Tampering | scoring version migration | high | mitigate | highScore/version field만 변경하고 unrelated sentinel fields를 integration assertions로 보존한다. |
| T-02-17 | Denial of Service | test save interruption | high | mitigate | idempotent same-directory durable guard를 SetUp/TearDown 양쪽에서 복구한다. |
| T-02-18 | Repudiation | migration diagnostic | low | accept | local warning에 old version/value를 남기며 원격 감사 로그는 이 offline game에 필요하지 않다. |
</threat_model>

<verification>
Conformance와 lifecycle fixtures를 독립 실행하고 lifecycle exact 15 및 Phase 1 trace exact 4를 확인한다. missing XML/zero tests는 BLOCKED다.
</verification>

<success_criteria>
- D-25/D-26의 version/migration이 high-score-only로 검증된다.
- Phase 1 durable save recovery와 exact 4+15 evidence가 보존된다.
- 다른 save fields는 mismatch에서도 유지된다.
</success_criteria>

<output>
Create `.planning/phases/02-shared-rules-core/02-06-SUMMARY.md` when done.
</output>
