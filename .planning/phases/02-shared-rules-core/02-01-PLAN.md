---
phase: 02-shared-rules-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Editor/Tests/Phase2RegressionTests.cs
  - Assets/Editor/Tests/Phase2RegressionTests.cs.meta
  - Assets/Editor/Tests/Phase2ConformanceTests.cs
  - Assets/Editor/Tests/Phase2ConformanceTests.cs.meta
  - Assets/Scripts/AL-1S/MahjongTileAndBlock.cs
  - Assets/Scripts/AL-1S/Utilities.cs
  - Assets/Scripts/AL-1S/MahjongWall.cs
  - Assets/Scripts/AL-1S/MahjongWall.cs.meta
  - Assets/Scripts/AL-1S/MahjongRound.cs
  - .planning/phases/02-shared-rules-core/02-EVIDENCE.md
autonomous: true
requirements: [RULE-01, RULE-02, RULE-03]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "RULE-01 edge coverage [boundary]: 공유 generator의 결과 길이는 정확히 136이고 135장과 137장 경계 결과는 허용하지 않는다."
    - "RULE-01 edge coverage [adjacency]: 34종 각각은 정확히 4장이며 어느 한 종류도 3장 또는 5장이 아니다."
    - "RULE-01 edge coverage [empty]: generator는 빈 wall을 반환하지 않고 생성된 모든 항목은 유효한 tile이다."
    - "RULE-01 edge coverage [ordering]: 구성 검사는 shuffle 순서와 무관하게 원본 34종 multiset을 정확히 보존한다."
    - "RULE-01 edge coverage [precision]: 적5는 만·통·삭 각 1장으로 정확히 3장이고 각 수패 일반 5는 정확히 3장이다."
    - "RULE-02 edge coverage [adjacency]: Fisher-Yates의 각 단계는 현재 위치를 포함하는 0..i 전체 범위를 사용하며 마지막 index도 선택 가능하다."
    - "RULE-02 edge coverage [empty]: 비어 있지 않은 136장 입력은 136장 permutation을 반환하고 빈 입력은 예외 없이 빈 결과를 반환한다."
    - "RULE-02 edge coverage [ordering]: 같은 seed는 같은 136장 순서를 반환하고 다른 실행에서도 stable order를 보존한다."
    - "RULE-03 edge coverage [adjacency]: 같은 무늬 normal 5와 red 5는 TileID 기본 equality/hash에서 같고 서로 다른 종류는 다르다."
    - "RULE-03 edge coverage [empty]: default/invalid tile도 ==, !=, typed/object Equals, GetHashCode, CompareTo 사이의 법칙을 위반하지 않는다."
    - "RULE-03 edge coverage [ordering]: 기본 정렬은 TileID만 사용하여 같은 종류의 normal/red CompareTo는 0이고 패산 재현 비교는 적색 위치까지 명시적으로 검사한다."
    - "D-27~D-30에 따라 현행 공개 솔로 시작 경로의 wall/identity 결함이 assertion-failure XML로 RED 기록된 뒤 같은 테스트가 GREEN이 되며, 기존 Phase 1 trace 4 + lifecycle 15는 그대로 통과한다."
  artifacts:
    - path: "Assets/Scripts/AL-1S/MahjongWall.cs"
      provides: "한 곳에서 136장 구성과 seeded Fisher-Yates를 수행하는 공유 wall"
      contains: "public static class MahjongWall"
    - path: "Assets/Editor/Tests/Phase2RegressionTests.cs"
      provides: "현행 결함 RED와 교정 GREEN을 같은 공개 경로에서 검증하는 Regression fixture"
      contains: "public class Phase2RegressionTests"
    - path: "Assets/Editor/Tests/Phase2ConformanceTests.cs"
      provides: "새 공유 wall/identity 계약의 literal Conformance 사례"
      contains: "public class Phase2ConformanceTests"
    - path: ".planning/phases/02-shared-rules-core/02-EVIDENCE.md"
      provides: "RED/GREEN commit, exact case count, XML/log 경로 ledger"
      contains: "Phase 2 Rules Evidence"
  key_links:
    - from: "Assets/Scripts/AL-1S/MahjongRound.cs"
      to: "Assets/Scripts/AL-1S/MahjongWall.cs"
      via: "GenerateYama(seed-backed PRNG)가 공유 CreateShuffled 호출"
      pattern: "MahjongWall"
    - from: "Assets/Editor/Tests/Phase2RegressionTests.cs"
      to: "Assets/Scripts/SoloScoringGameManager.cs"
      via: "실제 HandleGameStart -> MahjongRound.GenerateYama 경로로 136장 wall을 관찰"
      pattern: "SoloScoringGameManager|HandleGameStart"
  prohibitions:
    - requirement_id: "RULE-01"
      category: "scope-integrity"
      statement: "MUST NOT keep a copied legacy wall or scoring engine beside the corrected production path."
      status: resolved
      verification: test
    - requirement_id: "RULE-03"
      category: "identity-integrity"
      statement: "MUST NOT mutate tile values with dora state after they can be used as hash keys."
      status: resolved
      verification: test
---

## Phase Goal

**As a** gameplay developer and player, **I want to** use one accurate and automatically verified rules core across solo and four-player modes, **so that** both modes produce consistent mahjong results.

<objective>
하나의 실제 솔로 시작을 통해 공유 136장 wall과 일관된 패 정체성이 끝까지 사용되는 production tracer를 만든다. D-05~D-08의 wall/종류 비교 기반을 검증하고 D-27~D-30 증거를 남긴다. D-09의 우라 공개·표시 완료는 02-07~08에서 검증한다. D-06은 P2-CR-01 후속 정정을 적용한다.

Purpose: 이후 분해·점수 작업이 잘못된 패산과 hash identity 위에 쌓이지 않게 한다.
Output: `MahjongWall`, 교정된 `MahjongTile` identity, 실제 solo-to-wall tracer, Phase 2 두 fixture의 시작점, `02-EVIDENCE.md`.
</objective>

<execution_context>
@C:/Users/user/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-rules-core/02-CONTEXT.md
@.planning/phases/02-shared-rules-core/02-RESEARCH.md
@.planning/phases/02-shared-rules-core/02-PATTERNS.md
@.planning/phases/01-executable-baseline/01-07-SUMMARY.md

<interfaces>
기존 `MahjongRound.NewRound(int seed, out MahjongPlayer player)`, `GenerateYama()`, `MahjongTile.TileID`, `MahjongTile.isAkaDora`, `SoloScoringGameManager.HandleGameStart` 경로를 유지한다. 새 `MahjongWall.CreateShuffled(Random random)`은 정확한 136장 새 collection을 반환하고 입력 `Random` 외 상태를 보유하지 않는다.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 현행 솔로 wall 결함을 RED로 고정하고 공유 136장 경로를 GREEN으로 연결</name>
  <files>Assets/Editor/Tests/Phase2RegressionTests.cs, Assets/Editor/Tests/Phase2RegressionTests.cs.meta, Assets/Scripts/AL-1S/MahjongTileAndBlock.cs, Assets/Scripts/AL-1S/MahjongWall.cs, Assets/Scripts/AL-1S/MahjongWall.cs.meta, Assets/Scripts/AL-1S/MahjongRound.cs, Assets/Scripts/AL-1S/Utilities.cs</files>
  <read_first>Assets/Editor/Tests/Phase2RegressionTests.cs, Assets/Scripts/AL-1S/MahjongTileAndBlock.cs, Assets/Scripts/AL-1S/MahjongWall.cs, Assets/Scripts/AL-1S/MahjongRound.cs, Assets/Scripts/AL-1S/Utilities.cs, Assets/Scripts/SoloScoringGameManager.cs, Assets/Editor/Tests/MahjongRoundTraceTests.cs, Assets/Editor/Tests/SoloSessionLifecycleTests.cs, .planning/phases/02-shared-rules-core/02-CONTEXT.md, .planning/phases/02-shared-rules-core/02-PATTERNS.md, .planning/phases/01-executable-baseline/01-VALIDATION.md</read_first>
  <behavior>
    - Regression RED: 실제 `SoloScoringGameManager` 시작 경로가 만든 round wall은 136장이 아니므로 parseable assertion-failure XML을 남긴다.
    - Regression RED: 현 shuffle의 마지막 위치 배제와 `MahjongTile` operator/Equals 불일치가 각각 관찰된다.
    - GREEN: 34종×4, 적5 3장 치환, 같은 seed 같은 순서, permutation, 마지막 위치 포함 범위, normal5/red5 종류 equality/hash/compare 법칙 및 적색 보존이 모두 통과한다.
  </behavior>
  <action>D-27에 따라 production 수정 전에 `Phase2RegressionTests`를 현행 public API와 실제 solo start 경로에 작성하고 Unity가 생성한 test/runtime `.meta`를 함께 보존하며, non-empty exact test-name filter를 `-ExpectedGate Red`로 실행한다. compile/license/missing XML은 RED로 기록하지 않는다. RED commit과 XML/log를 확보한 다음 D-05/D-07의 `MahjongWall.CreateShuffled(Random)`은 136장 구성만 소유하고 기존 `Utilities.ShuffleArray`를 재사용한다. 먼저 해당 공유 helper를 descending Fisher-Yates `Next(i + 1)`로 교정하여 직접 helper를 호출하는 마지막-index regression도 같은 테스트로 GREEN이 되게 하고 `MahjongRound.GenerateYama()`가 이것만 사용하게 한다. D-06에 따라 `MahjongTile`에 `IEquatable<MahjongTile>`를 적용하고 operator, object/typed Equals, hash, CompareTo가 `TileID` 한 기준에 위임되게 한다. `isAkaDora`는 원본 속성으로 보존한다. 모든 tile 컬렉션 `Remove`, `Contains`, `IndexOf`, `Distinct`, `GroupBy`, `HashSet`, `Dictionary`, `SequenceEqual` 호출의 종류/적색 비교 의도를 점검한다. 위치를 아는 `MahjongPlayer.IsRiichiAble`의 `tenpaiCheckHand.Remove(fullHand[i])`는 `RemoveAt(i)`로 계획하고 실제 타패도 선택 인덱스의 원본 항목을 제거한다. 기본 Equals 기반 집합을 실제 손패로 재사용하지 않는다. 같은 seed의 패산은 `(TileID, isAkaDora)` 시퀀스로 비교하여 적5·일반5 위치만 바꾼 잘못된 순서도 검출한다. `isDora`/`doraCount`는 점수 identity에서 제거하고 후속 resolver 전환을 위해 더 이상 wall 생성에서 변이하지 않는다. 새 public `Phase2RegressionTests`/`MahjongWall` 타입과 public factory, 동작이 바뀌는 `MahjongTile` equality/comparison member에는 AGENTS.md에 맞는 한국어 XML summary를 작성·갱신한다. 기존 `NewRound(seed, out player)`와 solo ownership을 보존하고 새 package/asmdef/copy ID를 만들지 않는다.</action>
  <reversibility rating="costly">P2-CR-01은 기본 종류 비교를 유지하고 Equals를 정렬하는 승인된 정정이다. 원래 위치를 아는 제거 호출과 적색 보존 검증을 함께 수정하며 새로운 copy ID나 표시 정렬 기능은 추가하지 않는다.</reversibility>
  <verify>
    <automated>&amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter Phase2RegressionTests -ExpectedGate Green</automated>
  </verify>
  <acceptance_criteria>RED commit에는 parseable XML의 의도한 assertion failure와 양수 selected count가 있고, GREEN에서는 같은 named cases가 모두 passed이며 non-passed=0이다. solo start wall은 정확히 136장이고 종류별4/적5각1을 만족한다. 적5==일반5 참을 RED로 삼지 않고 기존 ==/Equals 불일치를 RED로 고정한다. 일반5 인덱스 제거 시 적5가, 적5 인덱스 제거 시 일반5가 남는 actual discard/리치 후보 경로와 적색 교환 패산 검출을 통과한다.</acceptance_criteria>
  <done>실제 솔로 시작 한 경로가 공유 `MahjongWall`을 사용하고 wall/identity Regression이 GREEN이다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 공유 wall 계약을 Conformance로 확장하고 RED/GREEN ledger를 고정</name>
  <files>Assets/Editor/Tests/Phase2ConformanceTests.cs, Assets/Editor/Tests/Phase2ConformanceTests.cs.meta, .planning/phases/02-shared-rules-core/02-EVIDENCE.md</files>
  <read_first>Assets/Editor/Tests/Phase2ConformanceTests.cs, .planning/phases/02-shared-rules-core/02-EVIDENCE.md, Assets/Scripts/AL-1S/MahjongWall.cs, Assets/Scripts/AL-1S/MahjongTileAndBlock.cs, .planning/phases/02-shared-rules-core/02-CONTEXT.md, .planning/phases/02-shared-rules-core/02-RESEARCH.md, .planning/phases/01-executable-baseline/01-BASELINE.md</read_first>
  <behavior>
    - Conformance rows independently assert wall composition and tile equality laws without deriving expected counts from production code.
    - Ledger records target commit, exact command/filter/name/count, expected/actual gate and raw artifact paths for RED and GREEN separately.
  </behavior>
  <action>D-02/D-28/D-29/D-30에 맞춰 `Phase2ConformanceTests`를 별도 fixture로 만들고 Unity가 생성한 `.meta`를 함께 보존하며 wall/identity expected values를 literal assertion으로 둔다. 새 public fixture type에는 한국어 XML summary를 작성한다. `02-EVIDENCE.md`에는 Task 1 RED/GREEN commit SHA, exact selected names/counts, helper status, XML/log 경로를 기록하며 Phase 1의 trace 4 + lifecycle 15를 Phase 2 count에 합치지 않는다. 정상 개발은 최신 HEAD에서 두 Phase 2 fixture를 실행하고 구형 비교는 이미 보존된 RED commit 또는 별도 임시 worktree에서만 수행한다. 원본 gate 산출물은 `Logs/UnityTestGate`에 남기고 commit하지 않는다.</action>
  <verify>
    <automated>&amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter Phase2ConformanceTests -ExpectedGate Green</automated>
  </verify>
  <acceptance_criteria>Conformance XML은 양수 selected count, unique names, zero non-passed이며 ledger의 SHA/filter/count/path가 실제 Git 및 결과 파일과 일치한다.</acceptance_criteria>
  <done>Regression과 Conformance가 이름으로 독립 실행되고 wall/identity 증거가 추적된다.</done>
</task>

</tasks>

## Artifacts this phase produces

- `Assets/Scripts/AL-1S/MahjongWall.cs` — 새 `MahjongWall.CreateShuffled(Random)` 공유 generator.
- `Assets/Editor/Tests/Phase2RegressionTests.cs` — 새 Regression fixture.
- `Assets/Editor/Tests/Phase2ConformanceTests.cs` — 새 Conformance fixture.
- `.planning/phases/02-shared-rules-core/02-EVIDENCE.md` — 새 Phase 2 evidence ledger.
- `MahjongTile`은 기존 symbol이며 새 symbol로 오인하지 않고 equality 구현만 교정한다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed/compact tile input → pure domain | 비신뢰 seed와 tile 값이 wall 및 hash identity로 들어온다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `MahjongTile` hash identity | high | mitigate | immutable identity fields와 typed equality 단일 경로, HashSet stability regression으로 검증한다. |
| T-02-02 | Denial of Service | `MahjongWall` output | medium | mitigate | fixed 136 construction과 permutation invariant를 public boundary에서 검증한다. |
| T-02-03 | Repudiation | Unity RED/GREEN evidence | high | mitigate | assertion-failure XML만 RED, parseable XML/positive count/zero non-passed만 GREEN으로 ledger에 기록한다. |
</threat_model>

<verification>
두 Phase 2 fixture를 각각 실행하고 기존 `MahjongRoundTraceTests` 4개와 `SoloSessionLifecycleTests` 15개도 별도로 GREEN 확인한다. exit code, missing XML, zero tests만으로 PASS를 판정하지 않는다.
</verification>

<success_criteria>
- D-05~D-08, D-27~D-30이 추적되고 D-09의 최종 표시는 02-07~08로 연결된다.
- 실제 solo start에서 136장 공유 wall과 일관된 tile identity가 동작한다.
- Regression/Conformance/Phase 1 fixtures가 독립된 fail-closed gate로 통과한다.
</success_criteria>

<output>
Create `.planning/phases/02-shared-rules-core/02-01-SUMMARY.md` when done.
</output>
