# Phase 2: Shared Rules Core — 정확한 공유 규칙 코어 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-04
**Phase:** 2-Shared Rules Core — 정확한 공유 규칙 코어
**Areas discussed:** 기준 규칙, 규칙 확장 경계, 패 정체성과 도라, 복수 화료 해석, 점수·정산, 회귀 증거, 점수 버전, Phase 경계, 솔로 국 진행과 쯔모 한도, 깡 점수와 플레이 분리

---

## 기준 규칙

| Option | Description | Selected |
|--------|-------------|----------|
| 프로젝트 고유 규칙만 유지 | 현재 구현을 기준으로 오류만 제한적으로 고친다. | |
| 공개 대회 PDF를 그대로 사용 | 사용자가 열어 본 작혼 대회 PDF를 전체 규칙으로 간주한다. | |
| 작혼 4인 남장 랭크전 동작 | 대회별 변형과 구분하고 작혼 랭크전 동작을 고정 목표로 삼는다. | ✓ |

**User's choice:** 웹게임 작혼의 규칙을 그대로 가져오되 4인 남장 랭크전 동작을 목표로 한다.
**Notes:** 작혼 공식 대회 자료는 대회별로 달라질 수 있으므로 단독 규칙집으로 사용하지 않고, 공개 공식 자료가 없는 항목은 출처가 있는 fixture로 동결한다.

---

## 규칙 확장 경계

| Option | Description | Selected |
|--------|-------------|----------|
| 완전 하드코딩 | 현재 규칙 외 변경 지점을 고려하지 않는다. | |
| 개발자 작성 프로필 경계 | 고정 프로필 하나를 사용하되 코드에서 값을 바꿀 작은 경계만 둔다. | ✓ |
| 인게임 설정·외부 JSON·플러그인 | 여러 규칙과 mod를 지금부터 동적으로 읽는다. | |

**User's choice:** 기본 규칙은 하나지만 증강마작 가능성을 위해 수정 가능한 방향으로 설계한다.
**Notes:** 플레이어 설정과 외부 프로필은 MVP 이후다. 현재 구현은 범용 규칙 언어나 사용되지 않는 인터페이스로 확장하지 않는다.

---

## 패 정체성과 패산

| Option | Description | Selected |
|--------|-------------|----------|
| 물리적 copy ID | 동일한 네 장도 각각 별도 ID로 추적한다. | |
| 패 종류 + 적5 여부 | 게임에서 관찰되는 적5만 구분하고 일반패 copy는 구분하지 않는다. | ✓ |
| 기존 mutable 플래그 유지 | `isDora`와 `doraCount`까지 패의 equality 상태로 둔다. | |

**User's choice:** 현재 의도대로 패 종류와 적5를 구분하되 일반패 copy ID는 두지 않는다.
**Notes:** 136장 패산에서 적5는 일반 5를 대체한다. solver는 패 종류로 비교하고 equality/hash/operator 계약은 일치시킨다.

---

## 도라 계산과 표현

| Option | Description | Selected |
|--------|-------------|----------|
| 패 값 변이 | 표시패가 바뀔 때 손패의 `isDora`와 `doraCount`를 갱신한다. | |
| 표시패 resolver | 점수 계산과 화면 표시가 같은 표시패→도라 계산을 호출한다. | ✓ |

**User's choice:** 도라는 화료 시 표시패에서 계산하고 인게임 도라패에는 반짝임을 줄 수 있어야 한다.
**Notes:** 적5는 고유 속성이다. 우라도라는 공개 전 표시하지 않는다. 화면 검증은 자동 규칙 PASS와 별도로 실제 Player에서 관찰한다.

---

## 복수 화료 해석

| Option | Description | Selected |
|--------|-------------|----------|
| 첫 분해만 사용 | 최초로 찾은 합법 분해를 바로 반환한다. | |
| 역 빈도 가중치 | 실제 화료율 통계로 동일 점수 후보의 패턴 순위를 정한다. | |
| 모든 분해 후 최고 지불액 | 전체 후보를 점수화하고 실제 수입, 판, 부 순으로 한 결과를 고른다. | ✓ |

**User's choice:** 동일 후보 선택 로직을 복잡하게 하지 않는다.
**Notes:** 실제 지불액·판·부까지 같으면 결정론적 순회의 첫 후보를 사용한다. 통계 가중치와 별도 역 순위표는 도입하지 않는다.

---

## 역·부·지불 계산

| Option | Description | Selected |
|--------|-------------|----------|
| 현재 솔로에서 발생하는 조건만 구현 | 쯔모와 닫힌 손 위주로 최소 교정한다. | |
| 모든 표준 점수 조건 구현 | 후속 모드가 명시적 맥락을 공급할 수 있도록 전체 표준 역·역만과 지불을 계산한다. | ✓ |

**User's choice:** Phase 2에서 모든 역의 조건을 구현하고 친/자와 쯔모/론에 범용적인 점수 체계를 만든다.
**Notes:** 상황역은 명시적 context를 사용한다. 도라는 역이 아니며, 론/친 쯔모/자 쯔모의 지불자별 delta와 화료자 총수입을 모두 반환한다. 본장과 리치봉은 모드 정산이다.

---

## 깡 관련 점수와 플레이

| Option | Description | Selected |
|--------|-------------|----------|
| 모든 깡 지식을 연기 | 깡 플레이와 깡 관련 역 판정을 함께 나중으로 미룬다. | |
| 점수 판정만 Phase 2 | 삼깡자·사깡자·영상개화·창깡은 합성 맥락으로 평가하고 플레이는 미룬다. | ✓ |
| 솔로 MVP에 안깡 포함 | 리치 후 합법 안깡까지 Phase 3에서 구현한다. | |

**User's choice:** 상황 플래그가 필요한 역도 많으므로 점수 판정 자체는 지금 구현하지만 솔로에는 깡을 넣지 않는다.
**Notes:** 깡 입력과 패산 상태 전이는 Phase 6 또는 MVP 이후다. 부분 깡 UI를 미리 노출하지 않는다.

---

## 구형/신형 회귀 증거

| Option | Description | Selected |
|--------|-------------|----------|
| 구형 엔진 소스 복제 | 제품 코드에 구형과 신형 엔진을 함께 둔다. | |
| Git RED/GREEN 비교 | 실패 테스트가 있는 구형 커밋과 교정 후 최신 커밋을 증거로 사용한다. | ✓ |
| 신형 테스트만 유지 | 교정 뒤 통과 결과만 남긴다. | |

**User's choice:** 구형 엔진 커밋에서는 회귀 테스트의 RED를, 신형에서는 회귀+전체 conformance를 실행한다.
**Notes:** 최종 suite는 `Regression`과 `Conformance` 두 그룹이지 중복된 전체 suite 두 개가 아니다. 원본 XML/log는 로컬에, 추적되는 ledger에는 커밋·테스트·결과·경로를 남긴다.

---

## 점수 버전과 고득점

| Option | Description | Selected |
|--------|-------------|----------|
| 엔진 업데이트마다 모든 기록 삭제 | 버전 의미를 구분하지 않고 기록을 초기화한다. | |
| 점수 규칙 버전 저장 | 점수 의미가 바뀔 때만 버전 불일치로 고득점을 초기화한다. | ✓ |
| 규칙별 전체 기록 이력 | 모든 과거 규칙과 설정 조합별 고득점을 계속 보존한다. | |

**User's choice:** 점수 계산 규칙 버전을 하드코딩하고 고득점 비교 요소로 사용한다.
**Notes:** 불일치 시 이전 버전·값을 로그에 남기고 고득점만 초기화한다. 다른 설정과 통계는 보존하며 전체 이력은 공개 배포나 복수 규칙이 생길 때 검토한다.

---

## Phase 책임 분리

| Option | Description | Selected |
|--------|-------------|----------|
| Phase 2에서 솔로와 4인전을 모두 구현 | 공유 코어와 두 게임 흐름을 동시에 완성한다. | |
| 로드맵 순서대로 분리 | Phase 2 점수 코어, Phase 3 솔로 리치, Phase 4 헤드리스 4인전을 순차 구현한다. | ✓ |

**User's choice:** Phase 2는 모든 역과 공유 점수 코어, Phase 3은 리치와 솔로 보조 설정, 실제 4인 로직은 다음 전용 Phase에서 구현한다.
**Notes:** Phase 2에서는 기존 솔로 호출만 최소 교체한다. 4인전이 없으므로 같은 시드의 좌석별 플레이 흐름까지 비교하지 않고 공유 패산 생성 순서를 검증한다.

---

## 솔로 리치 경제와 친 진행

| Option | Description | Selected |
|--------|-------------|----------|
| 4인 경제를 그대로 적용 | 솔로에서도 1,000점 공탁, 리치봉과 노텐벌부를 운용한다. | |
| 무료 연습 리치와 실제 친 연장 | 공탁 없이 리치 점수 조건과 손 고정만 적용하고 텐파이 친 연장을 전략 요소로 둔다. | ✓ |
| 솔로는 항상 고정 친/자 | 국 결과와 무관하게 역할을 고정한다. | |

**User's choice:** 솔로에는 상대 경제를 만들지 않지만 친 화료·친 텐파이 연장은 유지한다.
**Notes:** 플레이어 친 노텐에서 친이 넘어가며, 자일 때 국 결과 뒤 가상 친이 진행된다. 국풍은 동·남·서·북·동으로 순환하고 3분 종료만 세션을 끝낸다.

---

## 솔로 쯔모 한도

| Option | Description | Selected |
|--------|-------------|----------|
| 숨겨진 네 좌석 상태 유지 | 보이지 않는 상대 손패와 강을 실제 4인 흐름처럼 운용한다. | |
| 18회 고정 한도 | 하나의 패산에서 플레이어만 쯔모하고 횟수로 유국을 결정한다. | ✓ |
| 남은 패산 전부 사용 | 플레이어가 라이브 월 전체를 혼자 소비한다. | |

**User's choice:** 솔로에는 타가 손패·강·쯔모 로직이 필요 없으며 쯔모 횟수만 제한한다.
**Notes:** Phase 2 기본 한도는 18회다. Phase 3에서 리치 시 남은 횟수를 `×4`하고, 이 계산 한 곳만 바꾸어 추후 `×3`이나 고정값 추가로 조정할 수 있게 한다.

---

## 잘못된 쯔모 선언

| Option | Description | Selected |
|--------|-------------|----------|
| 입력 자체를 숨기고 무시 | 잘못된 호출은 아무 상태 변화 없이 거부한다. | |
| 감점 후 다음 국 | 현행처럼 점수를 잃고 현재 국을 끝낸다. | ✓ |

**User's choice:** 감점하고 다음 국으로 넘어간다.
**Notes:** Phase 2에서는 현행 `-8,000점`을 보존한다. 감점에 솔로 난이도 배율을 적용할지와 최종 수치는 Phase 3 discuss에서 결정한다.

---

## 솔로 난이도와 보조 기능

| Option | Description | Selected |
|--------|-------------|----------|
| 고정 난이도 | 모든 플레이어가 같은 제한과 힌트를 사용한다. | |
| 독립 보조 옵션 + 가산 배율 | 시간·리치 표시·텐파이 표시를 조합하고 어려운 선택의 보너스를 더한다. | ✓ |
| 옵션별 배율 곱셈 | 각 제한 배율을 곱해 최종 보상을 만든다. | |

**User's choice:** 초보자는 보조 기능을 사용하고 숙련자는 보조 기능을 꺼 고득점을 노릴 수 있게 한다.
**Notes:** Phase 3 범위다. 타패 제한 `없음/30/15/10/5초`, 리치 가능 표시, 텐파이·대기 표시를 후보로 두고 실제 보너스·상한·timeout 동작은 Phase 3 discuss에서 결정한다.

---

## the agent's Discretion

- 값 객체·맥락 타입·fixture helper의 구체적인 이름과 파일 배치는 결정된 책임 경계를 바꾸지 않는 선에서 planner가 정한다.
- 공개 공식 문서가 비어 있는 세부 규칙은 researcher가 출처와 확인일을 남기고 표 기반 테스트 사례로 동결한다.
- RED/GREEN 테스트의 정확한 category 이름과 evidence ledger 파일명은 기존 Phase 1 검증 경로에 맞춰 정한다.

## Deferred Ideas

- Phase 3: 무료 솔로 리치, 남은 쯔모 횟수 `×4`, 타패 시간·힌트 옵션과 가산식 도전 배율.
- Phase 4: 네 좌석 상태와 작혼 남장 랭크전의 실제 대국 진행·종료 규칙.
- Phase 6 또는 v2: 치·퐁·세 깡의 완전한 플레이 묶음. 솔로 깡은 MVP에 포함하지 않는다.
- 장기: 증강마작 규칙 선택, 외부 프로필, 아이템·퀘스트 기반 추가 쯔모와 규칙별 기록 이력.
