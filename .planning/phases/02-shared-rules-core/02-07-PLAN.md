---
phase: 02-shared-rules-core
plan: 07
type: execute
wave: 7
depends_on: [02-06]
files_modified:
  - Assets/Editor/Tests/Phase2ConformanceTests.cs
  - Assets/Scripts/UI-Kozeki/MahjongTileGameObject.cs
  - Assets/Scripts/UI-Kozeki/PlayerHandView.cs
  - Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs
  - Assets/Scripts/UI-Kozeki/UiWinInfo.cs
  - Assets/Scripts/UI-Kozeki/UiScoreDistanceInfo.cs
  - Assets/Editor/Phase1Build.cs
  - .planning/phases/02-shared-rules-core/02-EVIDENCE.md
autonomous: true
requirements: [RULE-07, RULE-10]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-09에 따라 visible dora indicator list와 aka identity를 shared resolver가 판정하고 `PlayerHandView`는 resolved bool만 `MahjongTileGameObject.SetDora`에 전달하며 unrevealed ura는 화료 전 표시 입력에 없다."
    - "D-19/D-31에 따라 result presentation과 distance/boost는 corrected `WinnerIncome`을 소비하고 automated XML/build evidence는 visible Windows Player observation과 별도 상태로 기록된다."
    - "Phase gate는 Phase2 Regression/Conformance, Phase1 trace 4/lifecycle 15, fresh successful BuildReport/exe를 독립 확인하고 missing XML, zero selected tests, license/IPC, compile failure를 GREEN으로 분류하지 않는다."
  artifacts:
    - path: "Assets/Scripts/UI-Kozeki/PlayerHandView.cs"
      provides: "shared resolver 결과를 tile view에 전달하는 hand presentation"
      contains: "FillHand"
    - path: "Assets/Scripts/UI-Kozeki/MahjongTileGameObject.cs"
      provides: "resolved dora bool만 표현하는 existing SetDora view"
      contains: "SetDora"
    - path: ".planning/phases/02-shared-rules-core/02-EVIDENCE.md"
      provides: "final automated build/test evidence와 NOT OBSERVED human fields"
      contains: "Automated Evidence"
  key_links:
    - from: "Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs"
      to: "Assets/Scripts/AL-1S/MahjongDoraResolver.cs"
      via: "round visible indicators를 resolver에 전달하고 resolved flags로 hand를 render"
      pattern: "MahjongDoraResolver"
    - from: "Assets/Scripts/UI-Kozeki/PlayerHandView.cs"
      to: "Assets/Scripts/UI-Kozeki/MahjongTileGameObject.cs"
      via: "FillHand/tsumo refresh가 SetDora(bool)를 호출"
      pattern: "SetDora"
  prohibitions:
    - requirement_id: "RULE-10"
      category: "verification-integrity"
      statement: "MUST NOT record automated test or build success as a substitute for an unobserved Windows Player dora, ura-visibility, result, or distance display."
      status: resolved
      verification: test
---

## Phase Goal

**As a** gameplay developer and player, **I want to** use one accurate and automatically verified rules core across solo and four-player modes, **so that** both modes produce consistent mahjong results.

<objective>
shared dora resolver 결과를 기존 hand view에 연결하고 모든 자동 test/build 증거를 fail-closed 상태로 준비한다.

Purpose: 점수와 화면이 서로 다른 dora source를 사용하지 않게 하고, 사람 관찰 전에 자동 근거를 완결한다.
Output: resolver-driven dora presentation, fresh full phase test/build evidence, human fields left NOT OBSERVED.
</objective>

<execution_context>
@C:/Users/user/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-rules-core/02-CONTEXT.md
@.planning/phases/02-shared-rules-core/02-RESEARCH.md
@.planning/phases/02-shared-rules-core/02-PATTERNS.md
@.planning/phases/01-executable-baseline/01-VALIDATION.md
@.planning/phases/02-shared-rules-core/02-06-SUMMARY.md

<interfaces>
기존 `MahjongTileGameObject.SetDora(bool)`, `PlayerHandView.FillHand(List<MahjongTile>)`, `SoloScoringUIController.UpdatePlayerHand/ShowWinInfo`를 최소 signature 변경으로 유지한다. presenter는 `MahjongDoraResolver` 결과만 전달하고 indicator cycle을 재구현하지 않는다.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hand dora 표시를 shared resolver 결과로 교체</name>
  <files>Assets/Editor/Tests/Phase2ConformanceTests.cs, Assets/Scripts/UI-Kozeki/MahjongTileGameObject.cs, Assets/Scripts/UI-Kozeki/PlayerHandView.cs, Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs, Assets/Scripts/UI-Kozeki/UiWinInfo.cs</files>
  <read_first>Assets/Editor/Tests/Phase2ConformanceTests.cs, Assets/Scripts/UI-Kozeki/MahjongTileGameObject.cs, Assets/Scripts/UI-Kozeki/PlayerHandView.cs, Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs, Assets/Scripts/AL-1S/MahjongDoraResolver.cs, Assets/Scripts/AL-1S/MahjongRound.cs, .planning/phases/02-shared-rules-core/02-CONTEXT.md, .planning/phases/02-shared-rules-core/02-PATTERNS.md</read_first>
  <behavior>
    - normal visible dora and aka tile activate indicator; non-dora does not.
    - unrevealed ura indicator list is never passed to pre-win hand rendering.
    - repeated FillHand/tsumo refresh recomputes resolved bool and clears stale indicator state.
  </behavior>
  <action>D-09에 따라 `SoloScoringUIController`가 current round의 공개 dora indicators만 받아 `MahjongDoraResolver.IsDora`로 각 tile의 flag를 계산하고 `PlayerHandView`에 전달하게 한다. `PlayerHandView`는 resolved bool을 existing `SetDora`로 넘기며 `MahjongTileGameObject.SetTile`은 tile의 mutable dora field를 읽지 않는다. aka는 tile identity에서 resolver가 true로 취급한다. pre-win 경로에는 ura indicators를 전달하지 않고 refresh마다 true/false를 명시해 stale highlight를 지운다. `UiWinInfo`는 legacy ron table이 아니라 `MahjongPaymentResult.WinnerIncome`을 표시하고 `PLAYER-OBS-01`에서 화료 후 UraDora 1, 6판 20부, 12000을 보여준다. 동작이 바뀌는 public `SetDora`, hand-fill, result-display member의 한국어 XML summary를 갱신한다. 새로운 shader/material/animation 또는 UI hierarchy 변경은 하지 않는다.</action>
  <verify>
    <automated>&amp; 'C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1' -ProjectPath $PWD -TestPlatform EditMode -TestFilter Phase2ConformanceTests -ExpectedGate Green</automated>
  </verify>
  <acceptance_criteria>EditMode presentation cases가 normal dora/aka/non-dora/stale-clear/unrevealed-ura inputs를 검증하고 모두 GREEN이다. UI code contains no indicator-cycle calculation or tile dora mutation.</acceptance_criteria>
  <done>기존 hand UI가 scoring과 같은 resolver의 공개 dora 결과만 표시한다.</done>
</task>

<task type="auto">
  <name>Task 2: 네 fixture와 fresh Windows build의 자동 evidence를 fail-closed로 확정</name>
  <files>.planning/phases/02-shared-rules-core/02-EVIDENCE.md, Assets/Editor/Phase1Build.cs, Assets/Scripts/UI-Kozeki/UiScoreDistanceInfo.cs, Assets/Scripts/UI-Kozeki/SoloScoringUIController.cs</files>
  <read_first>.planning/phases/02-shared-rules-core/02-EVIDENCE.md, .planning/phases/02-shared-rules-core/02-VALIDATION.md, Assets/Editor/Phase1Build.cs, .planning/phases/01-executable-baseline/01-VALIDATION.md, .planning/phases/02-shared-rules-core/02-CONTEXT.md</read_first>
  <action>D-28~D-31에 따라 `Phase2RegressionTests`, `Phase2ConformanceTests`, `MahjongRoundTraceTests`, `SoloSessionLifecycleTests`를 각각 helper로 실행하고 helper GREEN, parseable XML, positive selected count, unique names, zero non-passed를 기록한다. Phase 1은 exact trace 4 + lifecycle 15를 유지한다. `Phase1Build`에 한국어 XML summary가 있는 `BuildPhase2ObservationWindowsPlayer`를 추가해 `BuildOptions.Development | BuildOptions.StrictMode`, `Builds/phase2-observation/RiichiNya.exe`, durable report를 사용하고 `--phase2-observation` guard가 포함된 fresh StandaloneWindows64 Player를 만든다. `UiScoreDistanceInfo`의 development-only snapshot은 실제 bound service의 `DistanceWithAccumulated`과 `BoostLevel`을 표시하며 `SoloScoringUIController`가 PLAYER-OBS-01 win 뒤 한 번 호출한다. 일반 Player에는 이 setup/snapshot path가 컴파일되지 않아야 한다. BuildReport Result Succeeded, Errors 0, exe 존재를 확인하고 GUI 필드는 `NOT OBSERVED`로 남겨 automated PASS와 분리한다.</action>
  <verify>
    <automated>$gate='C:\Users\user\.codex\skills\unity-test-gate\scripts\Invoke-UnityTests.ps1'; $fixtures=@(@{Name='Phase2RegressionTests';Expected=$null},@{Name='Phase2ConformanceTests';Expected=$null},@{Name='MahjongRoundTraceTests';Expected=4},@{Name='SoloSessionLifecycleTests';Expected=15}); foreach($fixture in $fixtures){$raw=&amp; $gate -ProjectPath $PWD -TestPlatform EditMode -TestFilter $fixture.Name -ExpectedGate Green; if($LASTEXITCODE -ne 0){throw "$($fixture.Name) gate failed"}; $result=$raw | ConvertFrom-Json; if($result.status -ne 'GREEN' -or !(Test-Path -LiteralPath $result.resultsPath)){throw "$($fixture.Name) missing GREEN/XML"}; [xml]$xml=Get-Content -Raw -LiteralPath $result.resultsPath; $cases=@($xml.SelectNodes('//test-case')); $names=@($cases | ForEach-Object {$_.fullname}); if($cases.Count -le 0 -or @($names | Sort-Object -Unique).Count -ne $cases.Count -or @($cases | Where-Object {$_.result -ne 'Passed'}).Count -ne 0){throw "$($fixture.Name) XML failed closed"}; if($null -ne $fixture.Expected -and $cases.Count -ne $fixture.Expected){throw "$($fixture.Name) expected $($fixture.Expected), got $($cases.Count)"}}; $unity='C:\Program Files\Unity\Hub\Editor\2022.3.29f1\Editor\Unity.exe'; &amp; $unity -batchmode -nographics -projectPath $PWD -executeMethod Phase1Build.BuildPhase2ObservationWindowsPlayer -logFile "$PWD\Temp\phase2\observation-build.log"; if($LASTEXITCODE -ne 0){throw 'observation build process failed'}; $report=Get-Content -Raw "$PWD\Builds\phase2-observation\build-report.txt"; if($report -notmatch 'Result: Succeeded' -or $report -notmatch 'Errors: 0' -or !(Test-Path "$PWD\Builds\phase2-observation\RiichiNya.exe")){throw 'BuildReport/executable failed closed'}</automated>
  </verify>
  <acceptance_criteria>ledger에 네 filtered commands/status/count/names/XML/log와 observation build command/report/exe가 실제 값으로 기록된다. trace=4, lifecycle=15이고 Phase 2 counts는 실행 결과에서 기록되며 BuildReport는 Succeeded/Errors 0, executable은 존재하고 GUI rows는 NOT OBSERVED다.</acceptance_criteria>
  <done>사람 관찰을 제외한 Phase 2 자동 test/build evidence가 모두 fail-closed로 준비된다.</done>
</task>

</tasks>

## Artifacts this phase produces

- 새 runtime symbol은 없다. 기존 `SoloScoringUIController`, `PlayerHandView`, `MahjongTileGameObject.SetDora`가 shared resolver consumer로 교정된다.
- `02-EVIDENCE.md`에 final automated evidence section이 추가되며 human observation은 아직 `NOT OBSERVED`다.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure dora result → Unity presentation | stale/hidden indicator state가 화면을 잘못 표시할 수 있다. |
| local XML/build artifacts → evidence ledger | 빈/오래된 산출물을 PASS로 오인할 수 있다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-19 | Tampering | dora presentation | medium | mitigate | every refresh writes explicit bool from shared resolver and excludes ura before win. |
| T-02-20 | Repudiation | test/build evidence | high | mitigate | parseable XML/positive exact count/zero non-passed and fresh successful BuildReport are all required. |
| T-02-21 | Spoofing | GUI evidence state | high | mitigate | human fields remain NOT OBSERVED until Plan 08 explicit same-PID approval. |
</threat_model>

<verification>
네 filtered fixture와 Windows build를 실행한다. 이 plan은 UI visual PASS를 선언하지 않는다.
</verification>

<success_criteria>
- scoring/presentation이 one dora resolver를 사용하고 unrevealed ura를 숨긴다.
- Phase 2 Regression/Conformance와 Phase 1 exact 4+15가 GREEN이다.
- fresh Windows BuildReport/exe가 있으며 visual fields는 NOT OBSERVED다.
</success_criteria>

<output>
Create `.planning/phases/02-shared-rules-core/02-07-SUMMARY.md` when done.
</output>
